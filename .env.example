# Шаблон окружения. Скопировать и заполнить:
#
#     cp .env.example .env
#
# `.env` в git не попадает, этот файл — попадает. Реальных значений здесь быть
# не должно. `docker compose` подхватывает `.env` из этого каталога сам.
# Официальный образ всегда требует два разных токена. Пустое значение
# останавливает `docker compose config` до запуска контейнера.

# Каталог данных на машине Docker. Compose намеренно не создаёт его сам:
# на обычном Linux он мог бы стать root-owned и быть недоступным процессу
# контейнера uid=10001 gid=10001. Локальное значение подходит после явного
# `mkdir`; на сервере используйте абсолютный путь, например `/srv/mcp1c/data`.
MCP1C_DATA_DIR=./data

# Локальный порт хоста. Он всегда публикуется только на 127.0.0.1.
MCP1C_PORT=5001

# Готовый release-образ. Для локальной проверки разработчик может собрать тот
# же target `runtime` под локальным тегом и указать его здесь.
MCP1C_IMAGE=ghcr.io/azeevan/mcp-1c:2.0.0

# Современный дашборд: `on` либо `off`; серверного HTML-режима нет.
MCP1C_DASHBOARD=on

# `local` игнорирует X-Forwarded-*; `https-proxy` разрешён только за внешним
# доверенным HTTPS reverse proxy. Порт backend в обоих случаях остаётся loopback.
MCP1C_ACCESS=local

# Пароль администратора: включает всё, что меняет данные, — загрузку и удаление
# источников, словарь и POST /admin/reload. Для образа обязателен, отличается от
# API_TOKEN и содержит не менее 32 печатных ASCII-символов без пробелов.
#
# Читается на каждый запрос, так что менять можно без пересборки образа —
# достаточно `docker compose up -d --force-recreate`.
#
# Задавать длинным и случайным, а не осмысленным: сравнение идёт по всей
# строке, а доступ даёт полный.
#
#     python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
ADMIN_TOKEN=

# Пароль чтения: инструменты MCP, страницы дашборда и карточки. Для образа
# обязателен, отличается от ADMIN_TOKEN и содержит не менее 32 печатных
# ASCII-символов без пробелов. Сгенерируйте его той же командой отдельно.
#
# Отдельный от ADMIN_TOKEN потому, что этот лежит в конфиге каждого
# MCP-клиента и утекает вместе с ним; прав удалять источники у агента быть не
# должно. Админский токен годится и для чтения — держать два заголовка не надо.
#
# Клиенты передают его заголовком:
#
#     "headers": {"X-Api-Token": "..."}      в .mcp.json
#     Authorization: Bearer ...              равнозначно
#
# Только ASCII: заголовки HTTP кодируются latin-1, кириллица в них не дойдёт
# (через форму входа в браузере — дойдёт, но клиент MCP так не умеет).
API_TOKEN=

# Необязательная общая справка по умолчанию читается из подписанного файла
# /data/reference/reference.mcp1cref, который можно загрузить из дашборда.
# `off` полностью отключает адаптер; путь внутри контейнера выбирает внешний
# read-only файл и одновременно выключает загрузку из дашборда.
MCP1C_REFERENCE_ARTIFACT=

# Compose запускает сервер с `restart: unless-stopped`, поэтому разрешает
# дашборду завершить процесс после успешного административного ответа: Docker
# поднимет тот же контейнер и том заново. Поставьте 0, чтобы скрыть кнопку.
# Для запуска без внешнего supervisor код считает возможность выключенной.
MCP1C_ALLOW_SELF_RESTART=1
