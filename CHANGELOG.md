# Changelog

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версии — [SemVer](https://semver.org/lang/ru/).

Раздел **Найдено** — сверх стандарта: в нём фиксируются выясненные факты об
устройстве 1С и её форматов, вместе с опровергнутыми гипотезами. Правила —
в публичном [CONTRIBUTING.md](CONTRIBUTING.md).

## [Unreleased]

## [1.3.0] — 2026-08-30

Минорный выпуск добавляет полностью опциональную общую справку: подписанный
read-only `.mcp1cref` проходит fail-closed проверку до открытия SQLite и только
в состоянии `ready` добавляет `search_reference` и `get_reference`. Classic и
SPA получили единый административный lifecycle и отдельную read-only страницу
ручной проверки. Сам справочный артефакт и технология его сборки в выпуск не
входят; без базы сервер сохраняет прежние одиннадцать инструментов.

### Добавлено

- **Опциональный адаптер канонической общей справки schema v1.** Валидная
  подписанная read-only SQLite добавляет `search_reference` и `get_reference`;
  отсутствие, несовместимость, повреждение или недоверенная подпись оставляют
  основной MCP с прежними одиннадцатью операциями. Поиск возвращает короткие
  карточки и версионную доступность, точное чтение поддерживает разделы и
  непрозрачный курсор продолжения.
- **Fail-closed подпись общего справочного артефакта.** `.mcp1cref` содержит
  ровно канонический `manifest.json`, raw detached-подпись Ed25519
  `manifest.sig` и `reference.sqlite3`. Manifest связывает версию формата,
  `key_id`, имя и SHA-256 всей SQLite, schema v1 и логический SHA-256. Неизвестный
  ключ, отсутствие или подмена любого файла отвергаются до открытия SQLite;
  runtime-обхода подписи нет. Первый встроенный release-ключ —
  `reference-2026-01`, SHA-256 fingerprint raw public key:
  `509d077f669ebf935aa03453bdb1e904f95e0192fdf265b2c45b239678615c2e`.
- **Одинаковая административная загрузка в classic и SPA.** Bundle до 33 МиБ
  сначала проходит подпись и файловый SHA-256, затем строгую схему,
  `integrity_check`, внешние ключи, JSON-поля и логический SHA-256; только после
  этого он атомарно заменяет `data/reference/reference.mcp1cref` для следующего
  запуска. Внешний `MCP1C_REFERENCE_ARTIFACT` отключает управляемую загрузку.
- **Расходный постоянный индекс общей справки.** Тёплый старт поднимает его из
  `data/index/reference/`; замена SQLite, изменение кода, неверный штамп или
  битый кэш приводят к пересборке, но не к отказу сервера.
- **Управляемое удаление и применение общей базы из дашборда.** Точное
  подтверждение снимает `.mcp1cref`, извлечённую SQLite и расходный индекс;
  активный снимок остаётся доступным до перезапуска, после которого две
  условные MCP-операции исчезают. Ожидающую активации загрузку можно отменить
  без рестарта.
- **Controlled self-restart без Docker socket.** Compose явно разрешает
  административной кнопке завершить процесс после ответа `202`, а
  `restart: unless-stopped` возвращает тот же контейнер. SPA ждёт новый
  `runtime_id`; bare-запуск по умолчанию не может погасить себя.
- **Read-only проверка общей справки из дашборда.** Страница из навигации
  classic и SPA использует тот же провайдер для поиска и карточки, показывает
  пустую выдачу и безопасные причины всех неактивных состояний. Загрузка,
  удаление и прочие административные действия на ней отсутствуют.

### Найдено

- **`cryptography` уже находилась в runtime-образе через MCP SDK.** На образе
  `mcp1c:dashboard` размером 78 657 525 байт явная зависимость
  `cryptography==50.0.1` не добавила пакетов или слоя. Альтернативная
  `PyNaCl==1.6.2` дала слой 79 518 413 байт, то есть +860 888 байт, и новый
  прямой пакет. На синтетическом файле 32 МиБ SHA-256 файла плюс Ed25519 verify
  за 100 итераций дали `cryptography` p50 13,685 мс, p95 15,237 мс; PyNaCl —
  p50 13,659 мс, p95 14,932 мс. Разницы времени для выбора PyNaCl нет, поэтому
  используется уже изолированная в образе проверенная реализация PyCA.
  Воспроизводящий прогон (2026-08-30):

  ```bash
  tmp=$(mktemp -d)
  python3 -m venv "$tmp/venv"
  "$tmp/venv/bin/pip" install cryptography==50.0.1 pynacl==1.6.2
  dd if=/dev/zero of="$tmp/reference.sqlite3" bs=1m count=32
  "$tmp/venv/bin/python" - "$tmp/reference.sqlite3" <<'PY'
  import hashlib, json, statistics, sys, time
  from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey
  from nacl.signing import SigningKey
  payload = open(sys.argv[1], "rb").read()
  manifest = json.dumps({"sha256": hashlib.sha256(payload).hexdigest()},
                        sort_keys=True, separators=(",", ":")).encode() + b"\n"
  pyca = Ed25519PrivateKey.generate()
  pyca_sig = pyca.sign(manifest)
  nacl = SigningKey.generate()
  nacl_sig = nacl.sign(manifest).signature
  for name, verify in (
      ("cryptography", lambda: pyca.public_key().verify(pyca_sig, manifest)),
      ("PyNaCl", lambda: nacl.verify_key.verify(manifest, nacl_sig)),
  ):
      samples = []
      for _ in range(100):
          started = time.perf_counter()
          hashlib.sha256(payload).digest(); verify()
          samples.append((time.perf_counter() - started) * 1000)
      print(name, "p50", statistics.median(samples),
            "p95", sorted(samples)[94])
  PY
  docker image inspect mcp1c:dashboard --format '{{.Size}}'
  candidate=$(docker build -q - <<'DOCKER'
  FROM mcp1c:dashboard
  USER root
  RUN python -m pip install --no-cache-dir pynacl==1.6.2
  USER 10001:10001
  DOCKER
  )
  docker image inspect "$candidate" --format '{{.Size}}'
  ```

### Изменено

- **Карточка общей справки отделена от остальных административных блоков.**
  Собственный цветовой акцент и внешний интервал отделяют её от общей загрузки
  и incoming; сообщение после удаления больше не сливается с кнопкой рестарта,
  а точное имя базы копируется той же кнопкой, что и имена других источников.
- **Загрузка источников получила один пользовательский вход.** Общая форма на
  странице «Источники» принимает `.zip`, `.hbk`, `.json` и `.mcp1cref`, а затем
  направляет файл в Registry или справочный адаптер по типу. Отдельная карточка
  общей справки оставлена только для статуса, подписи, кэша и требования
  перезапуска; classic поддерживает тот же путь без JavaScript.
- **`/health` различает процессы.** Безопасный непрозрачный `runtime_id`
  меняется при каждом старте и не даёт SPA принять ещё старый процесс за
  завершивший перезапуск.

## [1.2.0] — 2026-08-30

Минорный выпуск убирает временный источник языка запросов из основного
сервера перед разработкой отдельной общей справки. Набор и входные схемы MCP-
инструментов остаются прежними; меняются состав принимаемых HBK и содержимое
синтаксической выдачи.

### Изменено

- **`search_syntax` и `get_syntax` снова имеют одну границу источника.** Оба
  инструмента читают только версионную справку платформы `shcntx_ru.hbk`.
  Публичные имена инструментов, их входные схемы и общее число одиннадцать не
  изменились; виды `query_table` и `query_field` сохранены, потому что это
  описания таблиц запросов из самой справки платформы, а не отдельный язык.
- **Дашборд показывает фактический контракт.** Область поиска теперь называется
  «Справка платформы», из API состояния и health удалён отдельный признак языка
  запросов, а загрузка HBK описывает только `shcntx_ru.hbk`.

### Удалено

- **Язык запросов исключён из основного MCP-сервера перед разработкой общего
  справочного контура.** Удалены приём `shquery_ru.hbk`, его продуктовый
  парсер, жёсткая таблица версий, специальные поисковые подсказки и подъёмы
  ранжирования, отдельный bench-набор и публичный design-документ. Это убирает
  конфликт одноимённых элементов и не выдаёт временную частичную реализацию за
  будущую общую справку.
- **Старый источник больше не удерживается совместимостью.** При первом старте
  новый Registry снимает неподдерживаемую учётную запись и удаляет её
  производные индексы обычной уборкой; остальные конфигурации, справки
  платформы, код и снимки расширений восстанавливаются без переразбора.

## [1.1.1] — 2026-08-29

Обратно совместимый patch-релиз устраняет пустую причину частичного
совпадения без изменения оценки и порядка поиска, расширяет измеряемый корпус
запросов процедур и добавляет безопасную публичную форму обратной связи.

### Добавлено

- **README получил нейтральную форму обратной связи.** Пользователь может
  сообщить о неверном, неполном или бесполезном MCP-ответе через GitHub issue,
  указав воспроизводимые вход и ожидание без публикации выгрузок, исходного
  кода, рабочих имён, токенов и `data/`. Раздел не раскрывает внутренние
  задачи, накопленные случаи, гипотезы или направления будущей реализации.

### Изменено

- **Стенд поиска процедур получил парные русские и английские формулировки.**
  Три прежних намерения теперь измеряются шестью запросами с одной
  семантической целью в каждой паре. На read-only прогоне 2026-08-29 русские
  запросы сохранили baseline `[промах, промах, 7-е место]`, все три английских
  запроса честно отмечены как известные промахи без лексического пересечения с
  русским именем процедуры. Общий прогон дал P@10 1/6 и MRR 0,024; остальные 61
  513 ручных и автоматических запросов не менялись. Воспроизводящая команда:

  ```bash
  PYTHONPATH=src .venv/bin/python -m mcp1c.bench \
    --data data --config ИмяКонфигурации \
    --auto --sets query-language,roznica-metadata,modules-procedures \
    --check-notes
  ```

### Исправлено

- **Счётчик тестов в публичных документах снова согласован.** README уже
  показывал актуальные 1 808 тестов, а CONTRIBUTING остался на 1 806.
  Существующая контрактная проверка теперь сверяет с реальной collection оба
  документа, поэтому следующее расхождение не останется незамеченным.

- **Частичное совпадение больше не показывается без причины.**
  `SearchIndex.search()` теперь присваивает найденному по части токенов
  результату причину `совпала часть слов запроса`; точное совпадение,
  псевдоним и полное покрытие сохранили прежние формулировки. Причина
  вычисляется после сортировки и не участвует в оценке. Синтетический тест
  сначала воспроизвёл две пустые причины, затем закрепил общий контракт ядра и
  JSON API. Полный прогон 61 519 ручных и автоматических сценариев не показал
  ни одной смены места относительно baseline; пометки известных промахов
  согласованы. Воспроизводящая команда:

  ```bash
  PYTHONPATH=src .venv/bin/python -m mcp1c.bench \
    --data data --config ИмяКонфигурации \
    --auto --sets query-language,roznica-metadata,modules-procedures \
    --check-notes --baseline ПутьКПредыдущемуПрогону.json
  ```

## [1.1.0] — 2026-08-29

Обратно совместимый минорный выпуск усиливает работу с расширениями: агент
получает независимый снимок их фактической активности и доказанное
происхождение собственной структуры, а ожидаемые конфликты Registry больше не
превращаются в HTTP 500 или исключения MCP SDK. Сводка источников показывает
только смысловые состояния без декоративных связей.

### Добавлено

- **Доказанное происхождение структуры расширений.** Selection v5 во время
  приёма основной файловой выгрузки сохраняет компактный gzip-каталог
  семантических адресов, а вместе с каждым расширением — только собственные
  объекты и прямые нессылочные реквизиты, доказанные относительно конкретного
  SHA-256 базы. `get_object(fields/full)` ставит пометку рядом с объектом или
  полем, перечисляет все источники конфликта и fail-closed отвечает
  `происхождение неизвестно`, если база отсутствует или её поколение сменилось.
  Каталог заменяется, удаляется и восстанавливается атомарно вместе с кодом;
  исходный ZIP при ответе не читается. Schema v1 и runtime-снимок активности
  расширений не менялись.

- **Отдельный runtime-источник фактической активности расширений.** Новая
  управляемая обработка для платформы 8.3.8+ формирует малый
  `СнимокРасширений_*.json` со списками расширений базы, действующих и не
  применённых расширений текущего сеанса, UTC-временем, `snapshot_id`, областью
  наблюдения и признаком изменения набора после старта сеанса. `reg-add`, обе
  версии дашборда и восстановление Registry принимают JSON независимо от
  schema v1 и корпусов кода; замена и каскадное снятие привязаны к identity
  конфигурации.
- **MCP-инструмент `list_extensions`.** Ответ различает `snapshot`, `stale` и
  `unknown`, показывает SHA-256 источника, активные и не применённые
  расширения, а загруженный код связывает с runtime-состоянием только при
  однозначном имени. Позиция сохраняется как порядок ответа API и прямо не
  объявляется порядком исполнения модулей.

### Изменено

- **Сводка источников больше не изображает декоративные связи и неизвестную
  «активность».** Три значка `GitFork`, не несущие данных, убраны. Источник
  назван «Снимок активности расширений», вместо `unknown` показано
  «не загружен»; без кода расширений и без снимка показатель скрыт. Сетка
  автоматически распределяет три или четыре смысловых колонки.

### Исправлено

- **Временный конфликт снимка источников больше не становится HTTP 500.**
  `/api/v1/sources` отвечает `409`, если публикация нового поколения дважды
  обогнала generation-safe снимок, а SPA ограниченно повторяет чтение.
- **Ожидаемые ошибки Registry больше не становятся исключениями MCP SDK.**
  Все инструменты возвращают их как `tools/call` с `isError=true`; ошибка
  выбора `config` не пишет traceback и имена локальных конфигураций в журнал.

### Найдено

- **Платформа даёт три независимых представления состава расширений.** Начиная
  с 8.3.8 `РасширенияКонфигурации.Получить()` принимает
  `ИсточникРасширенийКонфигурации`: `БазаДанных`, `СеансАктивные` и
  `СеансОтключенные`. `РасширенияКонфигурацииИзменены()` появился позже, в
  8.3.22, и отдельно сообщает, что новый сеанс получил бы уже другой набор;
  на 8.3.8–8.3.21 снимок хранит здесь `null`. Справка метода возвращает массив,
  но не называет порядок его элементов порядком исполнения модулей. Поэтому
  снимок сохраняет позицию наблюдения, а runtime-приоритет конкурирующих
  `&Вместо` по ней не выводится.

- **Принадлежность расширению теряется не только у добавленных полей, но и у
  собственных объектов.** В трёх измеренных расширениях найдено 22 собственных
  корневых объекта без `ObjectBelonging`; лишь восемь имён имеют префикс
  расширения. Четыре объекта поддерживаемых schema v1 видов — два общих модуля,
  регламентное задание и константа — уже присутствуют в соответствующей schema
  v1 как обычные объекты, причём ни одно из четырёх имён не имеет префикса.
  Вместе с шестью ранее найденными реквизитами агент видит десять структурных
  единиц расширений, но не их происхождение. Действовавший тогда selection v4
  сохранял
  модули и формы, однако отбрасывает `Configuration.xml` и XML объектов;
  `Source.origin` хранит только имя исходного архива, сам архив сервер не
  копирует. Поэтому после удаления оператором файла из `incoming/` восстановить
  принадлежность из опубликованного корпуса нельзя. Гипотезы «достаточно
  `ObjectBelonging`», «достаточно префикса» и «можно вычислить при ответе»
  опровергнуты. Публичной границей выбран доказанный источник объекта или поля
  по семантическому адресу; сырой `ObjectBelonging`, UUID и предположение об
  активности расширения наружу не выдаются.

- **Собственный реквизит расширения не обязан иметь
  `ObjectBelonging = Own`.** Read-only разбор обезличенного корпуса из основной
  файловой выгрузки и трёх расширений на платформе 8.3.27.1936 разрешил все 458
  реально заимствованных узлов в базу по виду, имени и иерархическому пути;
  UUID расширения во всех 458 случаях отличался от UUID соответствующего узла
  базы и ключом склейки быть не может. В одном расширении найдены шесть
  `Attribute` внутри заимствованного `Catalog`: у собственных узлов тег
  `ObjectBelonging` отсутствует, UUID присутствует, все имена имеют префикс
  расширения, а в родителе основной файловой выгрузки этих реквизитов нет.
  Соответствующая schema v1 уже содержит все шесть как обычные реквизиты
  справочника — структура была доступна агенту, но происхождение до selection
  v5 терялось. Пять корневых заимствованных объектов встречаются в нескольких
  расширениях, однако одинаковых собственных добавлений в корпусе нет;
  табличных частей и ссылочных типов среди шести реквизитов также нет, поэтому
  эти две границы измерение не доказывает. Корневой `Configuration` каждого
  расширения с `ObjectBelonging = Adopted` является манифестом самого
  расширения, а не неразрешённым заимствованием из основной конфигурации.

- **Само наличие нескольких расширений не доказывает порядок конкурирующих
  `&Вместо`.** Платформа применяет группы назначений в порядке `Patch` →
  `Customization` → `AddOn`, но порядок нескольких расширений с одинаковым
  назначением не определён. Файловая выгрузка содержит
  `ConfigurationExtensionPurpose`, однако активность и область действия относятся
  к информационной базе и текущему сеансу. Поэтому статический корпус пригоден
  для вывода о runtime-порядке только при общей цели `модуль + процедура`, а
  окончательное доказательство требует снимка действующих расширений и
  наблюдения выполнения на безопасной базе.

## [1.0.0] — 2026-08-28

Первый стабильный выпуск фиксирует публичный контракт проекта: MCP-сервер,
источники schema v1 и существующий каталог `data/` сохраняют совместимость с
`v0.8.0`, а рабочая SPA, три явных Docker-режима и полный Linux-runbook
становятся поддерживаемым способом эксплуатации.

### Добавлено

- **Docker-запуск получил обязательную проверку записи в `data/`.** Образ
  включает `--require-writable-data`: до создания `Registry` сервер фактически
  создаёт и удаляет пробный файл в корне и семи рабочих подкаталогах. Ошибка
  называет точный путь, UID/GID процесса и безопасную команду исправления;
  `chmod 777` и запуск от root явно не предлагаются. Ручной `stdio` без флага
  сохраняет возможность осознанно читать готовый read-only Registry.
- **Классический дашборд получил явный Compose-override.** Базовый
  `docker-compose.yml` теперь означает сервер без UI,
  `docker-compose.classic.yml` включает стабильный HTML, а прежний
  `docker-compose.dashboard.yml` — SPA. Все три режима используют один service,
  порт и каталог данных; remote override добавляется последним к любому из них.
- **Публичная документация разделена по владельцам функциональности.** Полный
  Linux/Docker runbook остаётся в корневом README, описание интерфейсов живёт
  в `dashboard/README.md`, а клиенты, MCP-инструменты, эксплуатация и
  архитектура — в четырёх самостоятельных документах `docs/`. Корневой README
  сократился с 2 507 до 827 строк без удаления списка инструментов, CLI,
  источников, переменных, измерений и копируемых клиентских конфигураций.
- **Заложен опциональный React-дашборд без второго runtime-процесса.** Режим
  `MCP1C_DASHBOARD=off` оставляет только MCP и служебный HTTP API, переходный
  `classic` сохраняет прежний серверный HTML, а `spa` заменяет страницы
  React-интерфейсом и открывает версионированный
  `/api/v1/dashboard/bootstrap`. Во всех трёх режимах данные принадлежат одному
  `Registry` в MCP-процессе; браузер не получает прямой доступ к `data/`.
  Общая оболочка включает светлую рабочую область с тёмной навигацией, живую
  сводку, различимые состояния, устойчивые прямые ссылки, сохранение
  компактности меню и React-страницу входа с прежней серверной проверкой
  токена.
- **Первым предметным срезом SPA стала read-only страница «Источники».**
  `GET /api/v1/sources` публикует единый согласованный снимок конфигураций,
  основного кода, расширений и общих справочников, а
  `GET /api/v1/sources/coverage` — актуальный полный JSON-журнал корпуса. Оба
  маршрута защищены прежним токеном чтения. В master-detail для выбранной
  конфигурации видны связанные корпуса, семантическое состояние разбора,
  предупреждения и три таблицы покрытия; выбор хранится в URL, переключение не
  сбрасывает прокрутку. Административные операции намеренно оставлены на
  следующий отдельный срез.
- **SPA подключена к двум прежним уровням авторизации.** При заданном
  `API_TOKEN` любая прямая ссылка сначала открывает React-форму входа и после
  успешной проверки возвращает на исходный адрес. Токен чтения даёт режим
  «Только чтение», `ADMIN_TOKEN` — «Администратор»; уровень, повышение прав и
  выход видны в общей верхней панели. Токен не хранится во frontend: сервер
  выдаёт прежнюю `HttpOnly`, `SameSite=Strict` cookie. До входа открыты только
  `/login`, `/health` и JS/CSS формы; API с данными остаются закрыты. Живая
  проверка поймала внешний `mcp_guard`, который пропускал `/login`, но закрывал
  её статику и оставлял белый экран; открыта только ветка `/assets/`, не API.
- **Страница «Источники» SPA получила рабочий административный контур.**
  Версионированный JSON API принимает `.zip` и `.hbk`, запускает прежний
  фоновый разбор, показывает журнал и входящие ZIP, разбирает выбранный архив
  в явно указанную конфигурацию, удаляет источники и неиспользуемые исходные
  файлы. Это тот же `Registry`, те же ограничения 500 МБ, защита multipart,
  единственный слот incoming и прежний каскад удаления, а не параллельная
  реализация во frontend. Читатель не получает административный снимок; все
  записи требуют `ADMIN_TOKEN`, same-origin для cookie-сессии и точное
  подтверждение идентификатора при удалении. В интерфейсе загрузка и входящие
  выгрузки идут полноширинно один под другим, передача файла имеет процент,
  разбор — устойчивое фоновое состояние, `truncated=true` объяснён отдельным
  переключателем, а вид кода определяется содержимым архива. Живая визуальная
  проверка нашла и убрала искусственную высоту, которая отталкивала блок
  управления вниз и оставляла пустое полотно.
- **Вторым предметным срезом SPA стала read-only страница «Запросы».**
  `GET /api/v1/queries` публикует конфигурации, три области поиска и общие
  лимиты, `POST` прогоняет до 32 фраз через прежние `Registry.resolve()`,
  `SearchIndex.search()` и фильтр версии без отдельного ранжирования во
  frontend. Для каждого из пяти видимых попаданий API возвращает адрес
  карточки, вид, оценку и причину, а отфильтрованные элементы — с причиной
  недоступности в версии платформы; неизвестный `scope` отклоняется вместо
  молчаливой подмены. React сохраняет конфигурацию и область в URL, фразы,
  последний ответ и положение страницы — в `sessionStorage`, поэтому возврат
  с карточки не стирает рабочий прогон. Пустой Registry, отсутствующая справка,
  лимиты и токен чтения проверяются синтетически.
- **SPA получила read-only карточки объекта и синтаксиса.**
  `GET /api/v1/cards/object` и `GET /api/v1/cards/syntax` вызывают общий с
  classic UI и MCP путь `get_object()` / `get_syntax()` и отдают одновременно
  буквальный Markdown и HTML из прежнего безопасного рендерера. Поэтому
  `brief / fields / full`, виртуальные таблицы, сведения о коде, сигнатуры,
  доступность по версии, замены и одноимённые элементы не пересобираются во
  frontend. React сохраняет конфигурацию, уровень и режим «как есть» в прямой
  ссылке, объясняет адрес без имени и позволяет вернуться к сохранённой выдаче
  «Запросов». Синтаксическая карточка остаётся доступной без конфигурации,
  объектная честно требует Registry; оба API закрыты токеном чтения.
- **SPA получила интерактивную страницу «Связи».** `GET /api/v1/graph`
  вызывает общий с классическим UI `graph_view.neighbourhood()` и возвращает
  готовые координаты, границы, степени узлов, направления и подписи рёбер —
  frontend не отбирает соседей и не вводит вторую раскладку. Из меню без
  параметров выбирается первая конфигурация, глубина всегда равна одному шагу,
  предел 15 / 30 / 60 / 150 / 400 и усечение показаны явно. SVG поддерживает
  панорамирование, масштабирование и перестроение по клику; боковой список
  дублирует направление и смысл связи без зависимости от hover. Пустой
  Registry, адрес без объекта, опечатка с предложениями и изолированный объект
  различаются отдельными состояниями. API закрыт прежним токеном чтения.
  Визуальная проверка обнаружила два дефекта временного состояния: предыдущий
  ответ API откатывал только что выбранную конфигурацию, а пассивный React
  `wheel` масштабировал SVG вместе с прокруткой страницы. URL теперь
  нормализует только завершённый ответ, а холст использует собственный
  непассивный обработчик колеса с остановкой события. Масштабирование круга
  при hover также заменено неподвижной зоной наведения и отдельным внешним
  контуром: курсор больше не вызывает смещение или передёргивание узла.
- **SPA получила страницу «Словарь» с теми же правилами, что classic UI и
  CLI.** `GET /api/v1/dictionary` показывает эффективные псевдонимы вместе с
  точной областью и происхождением, локальные группы синонимов и сводку
  встроенного слоя. Четыре POST-маршрута добавляют и снимают локальный
  псевдоним или группу через общий `Registry.mutate_dictionary()`, требуют
  `ADMIN_TOKEN` и same-origin и атомарно сохраняют `data/dictionary.json`.
  Поиск видит правку сразу без пересборки индексов. Переход из промаха на
  «Запросах» сохраняет конфигурацию, подставляет фразу и оставляет редактор над
  длинным списком встроенных правил; прямой путь из меню, читатель и пустой
  Registry имеют отдельные состояния.

### Изменено

- **Compose больше не создаёт bind source `data/` молча.** Короткая запись
  `./data:/data` заменена длинной с `MCP1C_DATA_DIR`,
  `create_host_path: false` и приватной SELinux-меткой `Z`. Пользователь и
  группа образа стабилизированы как `10001:10001`; процесс дополнительно
  запускается с `no-new-privileges`, без capabilities, через init и с
  ротацией JSON-логов. Loopback bind, токены и HTTP-пределы не ослаблены.
- **Production-сборка получила раздельные targets `runtime-core` и
  `runtime-dashboard`.** Node используется только в build-stage и не входит в
  рабочий образ. На 2026-08-28 образы одного и того же кода занимали
  78 530 226 и 78 667 548 байт соответственно: React-вариант добавил 137 322
  байта. Готовая статика содержит 473 452 байта; JS — 413 352 байта
  (`125,17 kB gzip` в выводе Vite), CSS — 59 495 байт (`11,41 kB gzip`).
  Локальные build-зависимости занимали 155 408 КиБ, но в runtime не
  копируются. Точный `package-lock.json`
  фиксирует React Router для прямых ссылок, TanStack Query для серверного
  состояния, Zustand для локального UI-state и Lucide для единой системы
  иконок. Отдельные библиотеки drag-and-drop, progress bar и графа не добавлены:
  панорамирование и масштабирование работают браузерными SVG API, а общую
  предметную раскладку считает Python.

  ```bash
  cd dashboard
  npm ci
  npm test
  npm run typecheck
  npm run build
  du -sk node_modules dist
  find dist -type f -maxdepth 2 -print0 | xargs -0 wc -c
  cd ..
  docker build --target runtime-core -t mcp1c:dashboard-core-test .
  docker build --target runtime-dashboard -t mcp1c:dashboard-test .
  docker image inspect mcp1c:dashboard-core-test mcp1c:dashboard-test \
    --format '{{.RepoTags}} {{.Size}}'
  ```

### Исправлено

- **MCP и SPA получают версию из одного исполняемого источника.** SPA раньше
  читала `importlib.metadata`, хотя Docker копирует `src/` без установки самого
  пакета. Локальный образ случайно находил игнорируемый `mcp1c.egg-info` и
  показывал прежнюю версию, а чистая сборка могла вернуть `0.0.0+local`.
  Теперь `mcp1c.__version__` используют оба контура, его совпадение с
  `pyproject.toml` проверяется тестом, а `*.egg-info` исключён из Docker-контекста.
- **SPA загружает `.zip` и `.hbk` с тем же пределом 500 МиБ, что classic UI.**
  Общая ASGI-граница узнавала загрузку только по старому `POST /sources`,
  поэтому новый `POST /api/v1/sources/upload` ошибочно получал общий предел
  2 МиБ и отклонял обычную справку платформы ответом `413` до multipart-разбора.
  Оба маршрута теперь получают 501 МиБ на файл и служебную обвязку; лимиты
  входа, пакетных запросов, MCP и остальных HTTP-маршрутов не изменились.
- **Страница «Источники» обновляет выбранную конфигурацию после завершения
  фонового разбора.** Раньше снимок конфигураций запрашивался сразу после
  передачи файла, когда новый корпус ещё не был опубликован в `Registry`;
  административный журнал затем переходил в `готово`, но счётчик расширений и
  карточки оставались прежними до полного reload. Теперь первый переход
  задания в `готово` или `ошибка` инвалидирует основной снимок и bootstrap
  ровно один раз, поэтому структура, основной код и расширения обновляются на
  открытой странице.
- **Длинный идентификатор конфигурации больше не расширяет рабочую колонку.**
  Внутренний grid явно допускает сжатие до доступной ширины, заголовок получает
  многоточие, а полное значение остаётся в подсказке при наведении. Статус,
  удаление, таблица фактов и корпуса сохраняют общую ширину страницы без
  горизонтальной прокрутки.
- **«Запросы» сохраняют конфигурацию и область после возврата из карточки.**
  Пользовательская ссылка «К результатам запросов» ведёт на чистый `/queries`,
  поэтому раньше фраза и выдача восстанавливались из `sessionStorage`, а
  отсутствующие в адресе `config` и `scope` подменялись серверными значениями
  по умолчанию. Теперь вкладка хранит весь прогон, а старые сохранённые ответы
  восстанавливают настройки из собственного запроса.
- **Удаление на странице «Источники» различает источник и оставшийся файл.**
  Для зарегистрированной конфигурации, корпуса или справки точный идентификатор
  по-прежнему нужно ввести, но теперь его можно скопировать соседней кнопкой.
  Файл вне реестра удаляется после обычного диалога без копирования длинного
  служебного пути; API сохраняет прежнюю проверку точного пути и допускает
  только элемент из актуального списка `orphans`.

### Найдено

- **Два ZIP из 1С с похожими названиями являются разными источниками.**
  Внешняя обработка проекта создаёт `СтруктураКонфигурации_*.zip` по schema v1:
  его загружают через дашборд, `bootstrap/` или CLI, и он создаёт конфигурацию
  в Registry. Штатная команда Конфигуратора «Конфигурация → Выгрузить
  конфигурацию в файлы… → Архив» создаёт отдельный ZIP с модулями и формами:
  его кладут только в `data/incoming/` и привязывают к уже загруженной
  конфигурации. Без родителя кнопки «Разобрать» нет намеренно. `.cf`, `.dt` и
  выбранный вместо архива каталог XML-файлов не заменяют ни один из этих
  источников.

## [0.8.0] — 2026-08-27

Защитный релиз закрывает подтверждённые границы входных данных и конкурентного
доступа, синхронизирует публичные контракты и делает цепочку поставки
воспроизводимой. Реестр теперь публикует единый неизменяемый снимок, schema v1,
архивы, HTTP и поиск отказывают до дорогого разбора при превышении явных
лимитов, а локальный и удалённый сетевые профили разделены безопасно.

### Добавлено

- **Воспроизводимый обезличенный замер снимка кода для `get_object`.** Новый
  `tools/lab/measure_object_snapshot.py` формирует в памяти сценарии с 0, 1 и
  3 расширениями одной конфигурации, раздельно снимает p50/p95 latency и
  `tracemalloc` current/peak для `fields` и `full`. Сценарий с одним
  расширением берёт крупнейшее по `items_total`; JSON не содержит имён
  конфигурации, объекта, расширений и физических путей. Три синтетических
  теста проверяют состав сценариев, возврат исходных карт Registry и публичную
  границу CLI.

### Изменено

- **Комментарии поискового движка синхронизированы с исполняемым кодом.**
  Вводное описание больше не называет полноценный стеммер избыточным и
  фиксирует реально используемый Snowball вместо прежнего усечения до шести
  символов. Комментарий у `predicate` теперь называет живого потребителя:
  `search_procedures` отбирает им категории до общего top-N, чтобы нужный
  scope не потерялся за более сильными кандидатами. Исполняемая логика не
  менялась; структурная сверка AST без docstrings это подтверждает. Тесты
  ранжирования и поиска процедур, а также полный набор из 1 710 тестов
  зелёные 2026-08-27:

  ```bash
  .venv/bin/python -m pytest -q \
    tests/test_search_ranking.py tests/test_tools_modules.py
  .venv/bin/python -m pytest
  ```

- **Сборка четырёх BSL-вариантов стала двухфазной.** Раньше
  `exporter-1c/build_modules.py` писал результаты прямо в `dist/`, поэтому
  поздняя ошибка исходника или проверки совместимости с 8.3.5 оставляла там
  уже изменённые файлы. Теперь полный набор сначала формируется, проверяется и
  записывается во временный каталог рядом с `dist/`; после этого каждый файл
  публикуется через атомарную замену. Сбой публикации откатывает уже заменённые
  цели, а ручные `.epf` остаются нетронутыми. Пять синтетических регрессионных
  тестов проверяют ошибку 8.3.5, отсутствующий поздний вариант, сбой временной
  записи, откат частичной публикации и успешное сохранение `.epf`. Контрольная
  пересборка 2026-08-27 сохранила SHA-256 всех четырёх BSL-файлов и чистый
  tracked diff:

  ```bash
  python3 exporter-1c/build_modules.py
  shasum -a 256 exporter-1c/dist/*.bsl
  git diff --exit-code -- exporter-1c/dist
  ```

- **Цепочка поставки стала воспроизводимой и отдельно проверяемой.** Нижние
  границы runtime-зависимостей сохранены в публичных входах пакета, а Docker,
  тесты, package job, audit и сам генератор lock получили раздельные полные
  lock-файлы с точными транзитивными версиями и SHA-256; все установки в CI и
  образе используют `--require-hashes`. `python:3.12-slim` закреплён digest,
  все GitHub Actions — commit SHA с читаемой версией рядом, `setuptools`,
  `build`, `wheel` и `twine` — точными версиями. Новый workflow запускает
  `pip-audit` на PR, push в `main`, вручную и еженедельно, выдаёт CycloneDX
  JSON SBOM и хранит artifact 14 дней. Локальная проверка 2026-08-27 не нашла
  известных уязвимостей в runtime lock; это снимок базы, а не утверждение об
  отсутствии неизвестных дефектов.

  Генератор выбран после замера. `pip-tools==7.6.1` добавил к пустому venv
  10 060 КиБ, но за 178,88 с не закончил первый hash-lock: скачивал каждый
  платформенный wheel NumPy для локального вычисления hashes. `uv==0.12.5`
  добавил 42 344 КиБ, зато получил универсальный runtime lock за 9,68 с из
  индексных метаданных. Оба инструмента запускаются только при обновлении и в
  runtime-образ не входят. После обновления runtime-пакетов и добавления lock
  внутренний размер образа изменился с 78 362 226 до 78 446 486 байт
  (+84 260 байт); новый образ собран, живой контейнер на него не переключался.
  Команды замера:

  ```bash
  python3 -m venv /tmp/mcp1c-pip-tools-measure
  /usr/bin/time -p /tmp/mcp1c-pip-tools-measure/bin/pip install pip-tools==7.6.1
  du -sk /tmp/mcp1c-pip-tools-measure
  /usr/bin/time -p /tmp/mcp1c-pip-tools-measure/bin/python -m piptools compile \
    --generate-hashes --output-file /tmp/mcp1c-runtime-pip-tools.txt requirements.txt

  python3 -m venv /tmp/mcp1c-lock-measure
  du -sk /tmp/mcp1c-lock-measure
  /usr/bin/time -p /tmp/mcp1c-lock-measure/bin/pip install uv==0.12.5
  du -sk /tmp/mcp1c-lock-measure
  /usr/bin/time -p /tmp/mcp1c-lock-measure/bin/uv pip compile \
    --universal --python-version 3.12 --generate-hashes \
    --output-file /tmp/mcp1c-runtime-lock.txt requirements.txt
  docker image inspect mcp1c:latest --format '{{.Size}}'  # до сборки
  docker compose build
  docker image inspect mcp1c:latest --format '{{.Size}}'  # после сборки
  ```

- **Multipart-загрузка и cookie-записи получили собственные ранние границы.**
  Общий ASGI-слой по-прежнему проверяет `Content-Length` и фактически принятые
  chunks, а потоковый multipart-parser теперь отдельно допускает ровно один
  file-part `file`, одно поле `allow_truncated` и не записывает очередной блок
  во временный spool после пересечения 500 МБ. Лишняя часть отвечает `400`,
  превышение файла — `413`, временный файл закрывается до запуска обработчика.
  Все десять изменяющих POST дашборда, включая logout, проверяют точное
  `Origin` либо `Referer`, если запрос авторизован сессионной cookie: соседний
  хост того же сайта и запрос без обоих заголовков получают `403` до изменения.
  Явный админский токен в заголовке остаётся независимым API-контрактом.

- **Все потребители реестра читают один публичный immutable snapshot.**
  `RegistrySnapshot` копирует изменяемые поля источников и прогресса под
  коротким lock, закрывает структурные карты от записи и даёт единый поздний
  CAS для тяжёлых ответов. MCP-инструменты, `/health`, dashboard, CLI, bench,
  модульный кэш и scanner больше не обходят приватные карты или `_lock`
  напрямую. Конкурентный тест воспроизводит прежний
  `RuntimeError: dictionary changed size during iteration`; публикация нового
  источника теперь ждёт завершения структурного снимка. Состояние
  `scanner.running` также синхронизировано, а слот фонового разбора занимается
  атомарно. Файловый обход `/sources` пропускает временный файл, исчезнувший
  между `rglob` и `stat` при успешной атомарной публикации, вместо
  `FileNotFoundError` всей страницы.

- **Публичные документы снова описывают один текущий контракт.** README
  показывает датированный обезличенный снимок реестра с командой
  воспроизведения и сверяет заявленное число тестов с фактической коллекцией
  pytest. Описание schema v1 фиксирует работу предопределённых на 8.3.5 и
  8.3.23, явный отказ от legacy-архива с требованием перевыгрузки и отсутствие
  запланированной общей schema v2. Статус источников данных теперь отражает
  реализованный провайдер модулей и проверенные бухгалтерские виды объектов.

- **Сетевой профиль теперь безопасен по умолчанию.** Docker Compose публикует
  backend только на `127.0.0.1:5001`, а ручной HTTP-запуск по умолчанию слушает
  `127.0.0.1`. Отдельный remote override требует одновременно `API_TOKEN` и
  `ADMIN_TOKEN`, сохраняет backend на loopback за TLS reverse proxy и явно
  включает доверие к переписанным `X-Forwarded-*`. Без этого флага Uvicorn не
  принимает proxy-заголовки; сессионная cookie получает `Secure` только для
  HTTPS-запроса. README фиксирует воспроизводимый профиль и границу доверия.

- **Schema v1 теперь проверяется fail-closed до публикации источника.**
  Обязательная версия `1.x`, формат, пути и конверты всех JSON/XML-чанков
  сверяются по `schema_version`, `type`, `chunk` и `count`; фактические числа,
  `objects_total` и уникальность `full_name` должны совпасть точно. Официальный
  экспортёр теперь пишет `count` в оба формата; пересобраны четыре BSL-варианта
  и обе EPF. XML-строки документированных числовых и булевых свойств
  нормализуются до тех же типов, что JSON, поэтому оба формата строят одну
  модель. Архивы старого конверта требуют перевыгрузки. Живая приёмка
  управляемой EPF дала полные XML и JSON по 4 596 объектов с одинаковыми
  моделями и графом; обычная XML-EPF открылась на 8.3.5.1570, а строгий loader
  принял все 1 619 объектов и 14 чанков без предупреждений.

- **Усечённая выгрузка требует явного административного подтверждения.**
  `truncated=true` по умолчанию отклоняется loader, Registry, CLI и дашбордом.
  `--allow-truncated` либо отдельная галочка разрешают диагностическую
  публикацию, но источник сохраняет признак неполноты и после рестарта все
  ответы предупреждают, что отсутствие объекта или связи ничего не
  доказывает.

- **HTTP-тело и пакет поисковых фраз ограничены до разбора содержимого.**
  Общая ASGI-граница проверяет `Content-Length` и повторно считает реальные
  chunks: для `/login` разрешено 16 КиБ, для `/queries` 1 МиБ, для загрузки
  501 МиБ с учётом multipart поверх предела файла 500 МиБ, для остальных
  маршрутов, включая MCP, — 2 МиБ. Превышение отвечает `413`. Дашборд
  принимает не больше 32 поисковых фраз и возвращает `422` до первого поиска.
  Конкурентный замер на локальном корпусе подтвердил, что прежние 5 000 фраз
  задерживали `/health` на 0,295 с; после ограничения отказ занял 0,015 с, а
  `/health` — 0,0011 с. Допустимые 32 фразы заняли 0,0056 с и не обосновали
  добавление semaphore или threadpool.

- **Поисковые фразы и кэш стемов получили общий ограниченный бюджет.** MCP,
  CLI и дашборд проходят через одну границу `SearchIndex`: принимается не
  более 4 096 символов и 32 различных токенов, а превышение отклоняется
  понятной ошибкой до расчёта покрытия. Это заменяет аварийное переполнение
  `numpy.uint32` на 33-м совпавшем токене. Общий кэш основ ограничен 4 096
  записями с детерминированным LRU-вытеснением; уникальные поисковые промахи
  больше не остаются в памяти навсегда.

- **Архивы и контейнеры разбираются с единым вычислительным бюджетом внутри
  парсеров.** Для V8, внешнего ZIP выгрузки метаданных, вложенного
  `FileStorage`, распакованного каталога и JSON-чанков ограничены число
  записей, одна запись, общий распакованный объём и коэффициент сжатия.
  Метаданные ZIP проверяются до чтения, а фактический поток — повторно на
  случай ложного заголовка; raw deflate V8 останавливается до выделения
  памяти сверх ближайшего предела. Превышение даёт контролируемый отказ без
  публикации источника, а в покрытии форм сохраняет точную категорию
  `budget_exceeded` вместо общего «контейнер не прочитан».

### Исправлено

- **Все HTTP-пути теперь сравнивают токены через один constant-time helper.**
  `/admin/reload` оставался единственным исключением: проверял
  `X-Admin-Token` обычным `!=`, тогда как чтение, admin-формы и login уже
  использовали `hmac.compare_digest` на UTF-8-байтах. Сравнение вынесено в
  общий `auth.same_token()` и подключено к обоим слоям. Один контрактный тест
  проходит реальные маршруты чтения, записи, входа и ручной перезагрузки;
  RED показал обход только на последнем пути. Целевые 16 auth-сценариев и
  полный набор из 1 710 тестов зелёные. Runtime-образ собран, helper проверен
  внутри него с ASCII и не-ASCII значениями; рабочий контейнер не
  пересоздавался. Команды проверки 2026-08-27:

  ```bash
  .venv/bin/python -m pytest -q tests/test_auth.py
  .venv/bin/python -m pytest
  docker compose build
  docker run --rm --entrypoint python mcp1c:latest -c \
    "from mcp1c.auth import same_token; assert same_token('secret', 'secret'); assert same_token('секрет', 'секрет')"
  ```

- **Параллельные административные правки словаря больше не сталкиваются на
  одном `dictionary.tmp`.** Раньше два сохранения писали один временный файл:
  первая атомарная замена уносила его, вторая получала `FileNotFoundError`.
  Кроме того, изменение объекта, запись и `reload_dictionary()` не составляли
  одной операции, поэтому одновременные формы могли потерять результат друг
  друга. Теперь `Dictionary.save()` создаёт уникальный временный файл рядом с
  `dictionary.json`, а `Registry.mutate_dictionary()` под отдельным `RLock`
  сериализует полный путь `mutation → save → reload`; ручное перечитывание и
  startup используют тот же замок. Два детерминированных конкурентных теста
  проверяют разные временные пути, отсутствие исключений, не более одного
  активного writer и сохранение обеих admin-правок. Целевой сценарий прошёл
  три раза подряд, связанный контур — 29 тестов, полный набор — 1 709 тестов.
  Runtime-образ собран, синтетическое сохранение внутри него прошло; рабочий
  контейнер не пересоздавался. Команды проверки 2026-08-27:

  ```bash
  .venv/bin/python -m pytest -q tests/test_dictionary_concurrency.py
  .venv/bin/python -m pytest
  docker compose build
  docker image inspect mcp1c:latest --format '{{.Id}} {{.Size}}'
  ```

- **Тест повторного разбора входящего расширения больше не отпускает фоновую
  задачу раньше времени.** Прежнее ожидание считало появление `Source`
  завершением разбора, хотя после публикации реестра job ещё могла выполнять
  финальную очистку и удерживать имя в `scanner.running`. Следующий POST тогда
  получал «уже идёт разбор», а ошибка зависела от скорости полного прогона.
  Теперь тест ждёт одновременно терминальное состояние последней job и
  исчезновение имени из `scanner.running`. Регрессия детерминированно удерживает
  первую задачу после появления `Source` и доказывает это окно до запуска
  разбора второй копии. Десять последовательных повторов сценария, 22
  связанных теста и полный набор из 1 707 тестов зелёные 2026-08-27:

  ```bash
  .venv/bin/python -m pytest -q tests/test_incoming_extension.py
  .venv/bin/python -m pytest -q tests/test_incoming_extension.py \
    tests/test_incoming_job.py tests/test_registry_snapshot.py
  .venv/bin/python -m pytest
  ```

- **`handler` подписки получил один публичный и обратимо совместимый
  контракт.** Каноническая форма schema v1 совпадает с exporter:
  `ИмяОбщегоМодуля.ИмяПроцедуры`. Loader снимает ошибочно документированный
  раньше префикс `ОбщийМодуль.`, а Graph принимает обе исторические формы и
  строит одну цель вместо ложной `ОбщийМодуль.ОбщийМодуль`. Существующие
  архивы обеих форм не требуют перевыгрузки.

- **MCP больше не объявляет язык запросов неверсионируемым.** Постоянные
  `initialize.instructions`, описание `search_syntax` в `tools/list` и ответ
  поиска без выбранной конфигурации теперь различают два факта: сам
  `shquery_ru.hbk` не содержит отметок версий, но подтверждённые границы
  задаёт курируемая таблица и общий фильтр применяет их по `config`. Один
  контрактный текст используется обоими MCP-входами и синхронизирован с
  README; тест на функции из 8.3.20 подтверждает отказ для 8.3.5 и доступность
  для 8.3.23.

- **Разные выгрузки с одинаковым именем архива больше не затирают сохранённый
  источник друг друга.** Копия метаданных получает детерминированное имя по
  идентификатору конфигурации и полному sha256 содержимого и публикуется
  атомарно внутри закреплённого каталога без перехода по символическим
  ссылкам. При восстановлении sha256 пересчитывается, а фактическое имя
  конфигурации сверяется с `registry.json`: повреждённая или подменённая копия
  не публикуется. Синтетический регресс-тест сохраняет две конфигурации из
  разных `same.zip` и после рестарта получает обе с исходными хешами.

### Найдено

- **Снимок трёх доступных расширений не даёт значимого торможения
  `get_object(fields/full)`.** На обезличенном корпусе основной код содержит
  15 165 элементов, крупнейшее расширение — 27, все три — 49. В двух прогонах
  по 200 вызовов p50 от 0 до 3 расширений вырос не больше чем на 0,71 мс,
  p95 — не больше чем на 1,03 мс; `tracemalloc` peak — с 3 745,4 до 3 749,0
  КиБ (+3,6 КиБ, 0,1%). Для `fields` диапазон p50 составил
  40,450–45,395 мс, p95 — 42,039–48,236; для `full` — 40,419–45,160 и
  42,326–48,432 мс соответственно. Ответы во всех сценариях совпали по
  размеру. Поэтому оптимизация не выполнялась: цена на текущем корпусе меньше
  шума полного ответа. Вывод не переносится на расширения, сопоставимые по
  объёму с основным кодом; при таком корпусе замер нужно повторить. Команда:

  ```bash
  PYTHONPATH=src .venv/bin/python tools/lab/measure_object_snapshot.py \
    --data /путь/к/копии/data --iterations 200 --allocation-runs 10
  ```

- **Строковое представление обработчика подписки не включает вид
  метаданных.** `Строка(Объект.Обработчик)` записывается exporter как
  `ИмяОбщегоМодуля.ИмяПроцедуры`, а не
  `ОбщийМодуль.ИмяОбщегоМодуля.ИмяПроцедуры`. Независимая проверка одного
  снимка трёх обезличенных выгрузок дала короткую форму у 377 из 377
  обработчиков; повторная проверка 2026-08-27 другого сохранённого снимка — у
  285 из 285 в двух XML и одном JSON. Ни одного значения с префиксом вида
  метаданных нет; прежнее описание schema опровергнуто обоими наборами.

- **У оборотного регистра в виртуальной таблице `.Обороты` нет полей
  `<Имя ресурса>Приход` и `<Имя ресурса>Расход`.** Живая проверка владельца
  2026-08-26: запрос к `<Имя ресурса>Оборот` выполнился, отдельные запросы к
  двум остальным суффиксам дали «Поле не найдено». Это подтверждает фильтр в
  `virtual_tables.py`, который для регистра вида «Обороты» оставляет только
  поле оборота, хотя общая страница справки перечисляет все три шаблона без
  оговорки о виде регистра.

## [0.7.0] — 2026-08-26

Адресный поисковый ключ для платформенного метода устраняет живой промах при
поиске способа перебрать строки результата запроса без изменений в остальных
60 992 контрольных запросах.

### Исправлено

- **Поиск понимает описание перебора строк результата запроса.** Живой запрос
  «как перебрать строки в выборке результата запроса» теперь находит
  `ВыборкаИзРезультатаЗапроса.Следующий` первым вместо промаха в top-10.
  Адресный ключ платформенного метода отделён от ключей языка запросов, не
  попадает в ответ агенту и отдельно проверяется на привязку при загрузке
  каждой версии справки.

  На сравнительном прогоне 2026-08-25 P@1 ручного набора языка запросов вырос
  с 18/22 до 19/22, P@10 — с 19/22 до 20/22. Остальные 60 992 запроса не
  сменили место: 21 по метаданным, 3 по процедурам, 50 508 точных и 10 439
  одноимённых по справке. Воспроизводится двумя вызовами одной команды: сначала
  с `--save data/bench/2026-08-25-query-row-iteration-before.json`, после
  изменения — с `--baseline` на этот файл и `--save
  data/bench/2026-08-25-query-row-iteration-after.json`:

  ```bash
  PYTHONPATH=src .venv/bin/python -m mcp1c.bench \
    --data data --config РозницаДляКазахстана \
    --auto --sets query-language,roznica-metadata,modules-procedures \
    --check-notes
  ```

## [0.6.0] — 2026-08-25

Осторожное распознавание расширенных фраз о 12 типовых событиях при поиске
процедур, воспроизводимые замеры провайдера модулей и актуальные версии GitHub
Actions.

### Добавлено

- **`search_procedures` понимает расширенные фразы о 12 типовых событиях.**
  Компактный глобальный каталог переводит однозначную формулировку в точное
  имя стандартной процедуры и разрешает его через уже готовое оглавление без
  нового индекса, дискового или сессионного кэша. Без `scope` ответ показывает
  только каноническое имя и число реализаций; при нескольких совпадениях в
  объекте просит точный модуль или форму. Нераспознанные фразы и точные имена
  сохраняют прежний поиск. Контракт и границы описаны отдельно в
  [документе типовых намерений](docs/standard-procedure-intents.md).

- **Воспроизводимый обезличенный спайк типовых намерений поиска процедур.**
  Команда `PYTHONPATH=src .venv/bin/python
  tools/lab/measure_standard_procedure_intents.py --data data` сравнивает
  нынешний top-10, компактный каталог стандартных обработчиков и наивное
  размножение подсказок по всем реализациям; отдельно меряет положительные,
  отрицательные и замороженные holdout-запросы, scoped/global разрешение,
  задержку, traced memory и размер состояния. Сам lab-скрипт не участвует в
  MCP; рабочий поиск использует вынесенный из него компактный каталог правил.

### Изменено

- **GitHub Actions переведены на актуальные major-версии.** Все задания CI
  используют `actions/checkout@v7` и `actions/setup-python@v7` вместо веток,
  работавших на снятом с поддержки Node.js 20. Удалённый в `setup-python@v7`
  вход `pip-install` в workflow не использовался, поэтому контракт установки
  зависимостей проекта не изменился.

- **Обезличенный замер приёмки кода расширен до schema v2.** Та же команда
  теперь раздельно показывает разрешение BSL-вызовов, привязок метаданных и
  их объединённого набора, классифицирует непривязанные metadata-рёбра и
  измеряет точный production-путь чтения записи `module` из плоского `.Form`.

### Найдено

- **Компактный типовой резолвер полезен, размножать подсказки по процедурам не
  нужно.** На одном доступном обезличенном корпусе 2026-08-25 из 33 555
  процедур 12 выбранных стандартных событий имеют 2 859 объявлений, одно имя
  — до 784. Нынешний поиск по экспортным процедурам нашёл ожидаемое имя первым
  в 2/60 запросов и в top-10 в 8/60; изолированный каталог из 13 шаблонов и 38
  групп маркеров распознал 59/60 положительных, все 3 исходные ручные
  формулировки и 35/36 замороженного holdout, на 30/30 отрицательных запросах
  отказался. Единственный промах — приставочная форма «проконтролировать»;
  правило после просмотра holdout намеренно не менялось.

  Точное разрешение через полное оглавление при переданном модуле дало одну
  процедуру 12/12, корректное отсутствие — 12/12. Scope объекта дал один
  адрес в 1 545/2 009 случаев (76,9%); в 464 (23,1%) осталось несколько
  совпадений, максимум 31. Значит объект сужает поиск, но не всегда снимает
  неоднозначность. Без области компактный ответ с количеством занимает
  99–117 символов и не перечисляет сотни адресов.

  В трёх прогонах распознавание заняло p50 0,0208–0,0215 мс и p95
  0,0278–0,0301 мс, точный модуль — p50 0,0844–0,0902 мс и p95
  0,6164–0,9885 мс, объект — p50 0,2987–0,3073 мс и p95
  0,6125–0,6458 мс. Компактные 12 документов дали 58,9–60,7 КиБ traced peak и
  6,4 КиБ сериализованного состояния против 4,1–4,3 МиБ и 915,5 КиБ у 2 859
  документов наивного варианта. Это `tracemalloc`, не RSS процесса. Вывод
  ограничен набором, составленным в рамках того же исследования. Поэтому
  продуктовый контракт принят только в fail-closed-виде: без области адрес не
  выбирается, а нераспознанная фраза остаётся в обычном поиске.

- **Связи из метаданных точны, но глобальную долю разрешённых вызовов почти не
  меняют.** На двух доступных обезличенных корпусах 2026-08-24 разрешено
  97 289 из 178 216 BSL-рёбер (54,59%). Из 203 обработчиков подписок и методов
  регламентных заданий 194 указывают на найденную процедуру (95,57%): восемь
  остальных ведут в скомпилированные модули без исходника, один — в модуль без
  такой процедуры. Объединённый набор даёт 97 483 из 178 419 (54,64%), то есть
  прибавка составляет лишь 0,05 процентного пункта. Гипотеза, что metadata-рёбра
  заметно поднимут общую долю, опровергнута; их ценность — отдельные точные
  точки входа, а не исправление неоднозначных BSL-вызовов.

- **Переоткрывать плоский контейнер формы по запросу достаточно дёшево.** В
  четырёх последовательных процессах все 1 079 читаемых записей `module`
  открылись без отказа. На трёх повторных проходах каждого контейнера медиана
  была 0,052–0,067 мс, p95 — 0,271–0,346 мс, p99 — 0,454–0,764 мс; один
  единичный максимум достиг 27,054 мс. Первый измеренный проход намеренно не
  назван cold: файловый кэш ОС не контролировался. Замер воспроизводится
  командой `PYTHONPATH=src .venv/bin/python
  tools/lab/measure_modules_acceptance.py --data data --timeout 120`.

## [0.5.0] — 2026-08-24

Устаревший SSE-транспорт полностью удалён из публичных вариантов запуска;
сервер поддерживает только `streamable-http` и `stdio`.

### Удалено

- **Устаревший SSE-транспорт полностью отключён.** `mcp1c-server --transport
  sse` теперь отклоняется разбором аргументов до чтения данных и запуска
  сервера; поддерживаются только `streamable-http` и `stdio`. Скрытый ручной
  вход удалён, чтобы `API_TOKEN` не обещал защиту MCP-инструментов при наличии
  пути, который запускался мимо общего слоя авторизации. В Docker SSE и раньше
  не публиковался, а живых клиентов, которым недоступен streamable-http, не
  найдено.

## [0.4.0] — 2026-08-24

Три сверяемые таблицы покрытия на `/sources` и отдельный актуальный безопасный
JSON-журнал schema v1 для основной выгрузки и каждого расширения.

### Добавлено

- **Один актуальный JSON-журнал покрытия на каждый корпус кода.** Основная
  выгрузка и каждое расширение атомарно записывают отдельный файл schema v1 в
  `data/logs/`; повторная загрузка заменяет только свой файл, удаление
  источника удаляет его, каскад конфигурации — журналы всех её корпусов.
  Identity связывает журнал с sha256 и поколением каталога локаторов; битый,
  неполный, устаревший или содержательно расходящийся с текущими индексами
  файл не публикуется и пересобирается из готовых индексов. Имя файла —
  sha256 `source_id`, каталог открывается без перехода
  по символической ссылке, запись не содержит абсолютных путей, traceback,
  токенов, тел BSL или сырой записи `form`. Отказ журнала не отменяет загрузку.

### Изменено

- **`/sources` показывает покрытие тремя сверяемыми таблицами.** Для модулей и
  процедур, структур форм и модулей форм выводятся точное количество, процент
  и явный знаменатель, включая корректное состояние при нуле. Первые 20
  адресов и «ещё N» убраны только из обзорной страницы и заменены путём к
  полному JSON-журналу; MCP, CLI и точная диагностика формы в `get_object`
  сохранены.

## [0.3.0] — 2026-08-22

Приём выгрузок модулей и расширений, провайдер кода с поиском процедур,
карточками и обратными вызовами, поддержка иерархических дескрипторов форм,
`Form.bin` и плоских `.txt`/`.Form`, строгий расходный кэш и единая точная
диагностика покрытия в MCP, CLI и дашборде.

### Изменено

- **Поиск процедур и карточка читают тело по локатору того же поколения.**
  `search_procedures` и `get_procedure` больше не восстанавливают физический
  `Module.bsl` из публичного адреса: иерархический `.bsl`, плоский `.txt` и
  запись `module` контейнеров `Form.bin`/`.Form` проходят один BSL-лексер.
  `get_callers`, scoped search, аннотации и точные границы тела работают на
  тех же данных после cold/warm старта; смена поколения даёт один полный
  повтор, не смешанный ответ.

### Исправлено

- **Warm-кэш принимает квалифицированный вызов API того же общего модуля.**
  Компактное ребро хранит только caller и target, поэтому квалифицированный
  `ИмяОбщего.Метод()` и локальный self-вызов дают одинаковый индекс цели.
  Строгая межиндексная проверка ошибочно отвергала первый вариант и заново
  строила три живых кэша при каждом рестарте. Self-цель без локального
  объявления теперь допустима только у нескомпилированного общего модуля;
  остальные виды по-прежнему fail-closed.

- **Семантически повреждённый кэш кода больше не публикует смешанный ответ.**
  При warm-старте проверяются границы и флаги параллельных массивов, ссылки на
  пул строк, счётчики покрытия, каталог локаторов и согласованность таблиц
  модулей всех четырёх индексов. Любое расхождение становится полным cache
  miss и фоновой пересборкой; read-only кэш не мешает опубликовать те же
  точные категории и ограниченный список проблем, что и исправный cold/warm
  roundtrip. Общий список ограничен 20 строками, exact aggregates и локальные
  причины формы хранятся отдельно для `get_object`. Файл ограничен 256 МиБ,
  распакованный payload — 512 МиБ: gzip bomb становится cache miss до разбора
  `marshal`. Тела, сигнатуры и сырая запись формы в кэш не сериализуются.

### Добавлено

- **Обезличенный lab-замер финальной приёмки кода.** Команда
  `PYTHONPATH=src .venv/bin/python tools/lab/measure_modules_acceptance.py
  --data data --timeout 120` печатает один JSON с видами локаторов, покрытием
  форм, точными категориями причин, временем и пиком памяти — без имён
  источников, объектов и физических путей. Живой прогон 2026-08-21 поднял
  четыре warm-источника за 3,676 с без startup-проблем; контейнерные cold/warm
  прогоны на 16 кэшах заняли 111,216 и 11,495 с, оба `healthy`, без OOM,
  рестартов и строк `error`/`exception`/`traceback`.

- **Публичные ответы показывают точное покрытие кода одного поколения.**
  `list_configurations`, `reg-list` и `/sources` одинаково различают «готов» и
  «готов с ограничениями», печатают полные разбиения структур и модулей форм,
  точное количество по каждой категории причин, первые 20 детерминированных
  проблем и точный остаток.
  `get_object` выводит все причины своих форм и не подменяет непрочитанную
  форму сообщением «форм нет»; поиск, карточка процедуры, callers и связи
  предупреждают выше частичного результата. Неадресуемый кандидат получает
  порядковый номер без физического имени, ошибки не раскрывают локальные пути
  и traceback. Reparse/remove повторяет весь снимок, включая диагностику, а
  первое появление категории в поколении записывается в журнал один раз
  агрегатом без имён и путей.

- **Контейнерная структура формы разбирается ограниченно и без догадок.**
  Один нерекурсивный ридер проверяет UTF-8, скобки и удвоенные кавычки записи
  `form` как в иерархическом `Form.bin`, так и в плоском `.Form`; размер
  ограничен 16 МиБ, глубина — 128, число лексических единиц — 2 000 000.
  Наблюдаемые корневые маркеры `19`, `20`, `23`, `25`, `26` и `27` остаются
  частично прочитанными до доказательства позиционных полей, неизвестный
  маркер получает отдельную категорию. Индекс форм объединяет дескриптор,
  `Form.xml`, контейнер и модуль, сохраняет подписанные XML-свойства
  дескриптора, точные счётчики полного/частичного/непрочитанного покрытия и
  обезличенные причины. Доступность XML-привязок хранится независимо от
  общего частичного отказа; сырая запись и позиционное дерево в кэш не попадают.
  Ошибка или превышение бюджета ограничивает одну форму и не роняет источник.

- **Четыре индекса кода строятся из одного каталога локаторов.** Единый
  детерминированный снимок объединяет иерархические `.bsl`, XML-доказательства
  форм, `Form.bin`, плоские `.txt`/`.Form` и скомпилированные `.Module`.
  Текстовые файлы и запись `module` контейнера проходят один нормализатор и
  один BSL-лексер; одинаковые тела одного адреса дедуплицируются, разные
  становятся конфликтом без выбора победителя. Каждый отобранный кандидат
  попадает ровно в одну именованную категорию покрытия, а абсолютный корень и
  тело в каталоге не сохраняются; относительный путь остаётся частью локатора.

- **CLI и страница источников отражают завершённый провайдер кода.** Команды
  `reg-search-procedures`, `reg-get-procedure` и `reg-get-callers` вызывают те
  же функции, принимают те же параметры и используют те же значения по
  умолчанию, что MCP-инструменты. Одноразовые команды ждут холодную фоновую
  сборку до 90 секунд, поэтому не завершают daemon-потоки до результата и
  записи четырёх файлов кэша; сервер продолжает отвечать без ожидания.
  `reg-list` печатает только один структурированный снимок метаданных, справок,
  основной конфигурации и расширений и различает готовность, этап сборки,
  ошибку и отсутствие кода. Страница `/sources`
  единым неизменяемым снимком отдельными строками
  показывает код основной конфигурации и каждого расширения: готовые
  счётчики, фактический прогресс сборки, обезличенную ошибку или отсутствие
  источника. Сводка и файловые обходы считаются вне цикла событий и без
  удержания замка реестра, отрисовка не перечитывает живые словари или диск, а
  единая проверка поколения выполняется после всех дисковых обходов и при
  повторной загрузке повторяет подготовку целиком.
  README разделяет оба источника кода, объясняет потери в цепочке трёх
  инструментов и явно называет границы провайдера; память финального образа
  зафиксирована воспроизводимыми холодным и тёплым замерами. Стандартный
  `tools/lab/measure_container_memory.py` с явными режимами `cold` и `warm`
  сам собирает или перезапускает контейнер и печатает только машинные
  агрегаты. Холодный режим перед удалением останавливает контейнер и снимает
  только точные рабочие имена четырёх кэшей каждого текущего источника;
  после остановки каталог закрепляется через `O_NOFOLLOW`, сверку `dev`/`ino`
  и `dir_fd`, поэтому подмена родителя на символическую ссылку не ведёт к
  удалению наружу. Готовность требует `healthy`, полного маркера с `loaded_at`,
  идентичностью файла `registry.json`, поколения самого источника кода и
  `sha256` конфигурации-владельца, статуса `ready` у каждого и точного набора
  кэшей в двух проверках. Штатный startup создаёт новое runtime-поколение
  метаданных конфигурации; измеритель разрешает только смену его `loaded_at`,
  затем закрепляет точный новый маркер. После памяти, `docker stats` и журнала
  финальная проверка
  читает реестр до и после снимка кэша; оба маркера должны совпасть с готовым
  поколением, поэтому смена источника внутри снимка отменяет результат. Тёплый
  режим повторно закрепляет каталог после `docker stop` и до `docker start`,
  пока удерживает `dir_fd`, и требует неизменных `dev`, `ino`, размера и
  `mtime_ns` каждого файла от снимка перед остановкой до финальной проверки.
  Единый конечный
  положительный timeout передаётся остатком каждой Docker-команде, а любой
  отказ CLI остаётся одним JSON без пути и stderr.

- **Стенд качества ищет процедуры по рабочему индексу кода.** Все ручные
  наборы переведены на единую schema v1 с явным доменом `syntax`, `metadata`
  или `procedures`; имя файла больше не выбирает индекс. Публичный набор
  процедур хранит только обезличенные bare-имена BSL и разворачивает
  одноимённые процедуры во все точные адреса выбранной конфигурации либо
  расширения. `mcp1c.bench --extension` измеряет расширение отдельным
  корпусом. Смена поколения кода повторяет весь набор с начала, а негодный
  набор, неоднозначный выбор и неготовый индекс отменяют весь запуск до
  печати и сохранения; файловые отказы и техническая ошибка источника не
  раскрывают локальные пути. Ожидаемые имена разрешаются через оглавление,
  без полного обхода поискового индекса. Машинные `expected_miss` и
  zero-based `expected_rank` позволяют `--check-notes` обнаружить смену
  зафиксированного места. `--save` атомарно заменяет файл до печати отчёта:
  отказ записи не оставляет частичный вывод. Отчёт schema v1 сохраняет
  `suite` и `domain` каждой строки, поэтому одинаковый запрос из разных
  доменов не склеивается при сравнении baseline.

- **Скомпилированные общие модули плоской выгрузки теперь наблюдаемы.**
  Канонические `CommonModules/<Имя>.Module` и
  `CommonModule.<Имя>.Module` сохраняются и входят в оглавление как
  `ОбщийМодуль.<Имя>` без тела и процедур. Сводки и инструменты кода прямо
  предупреждают, что исходник поставлен скомпилированным, вместо ложного нуля;
  агрегаты процедур, вызовов и перекрытий помечаются как сведения только по
  доступным исходникам. Первый этап холодной сборки считает такие модули в
  общем прогрессе. Небезопасные члены архива и мусор Finder не участвуют в
  определении формата или манифеста. Единая нормализованная карта ZIP разрешает
  дубли по правилу «последняя запись побеждает» одинаково для определения типа,
  плана и распаковки; вложенное неканоническое имя `*.Module` не переключает
  иерархическую выгрузку в плоскую. Файловые отказы предпроверки, распаковки,
  сборки и публикации возвращают обезличенную ошибку без локального пути;
  при двойном отказе отката остаётся безопасный путь восстановления
  относительно `data/`.

- **Код в существующих ответах** — `list_configurations` показывает готовность,
  этап сборки, ошибку или отсутствие индекса основной конфигурации и каждого
  расширения. Сводка расширения считает четыре вида аннотаций, затронутые
  модули и собственные процедуры без чтения тел. `get_object` на уровне
  `fields` даёт счётчики, а на `full` перечисляет модули и все формы объекта,
  включая формы только с модулем или только с `Form.xml`; метаданные остаются
  доступны и при неготовом индексе. `get_related` отмечает связанные объекты,
  которые действительно перекрыты любым загруженным расширением. Все блоки
  проверяют единое поколение конфигурации и корпусов кода перед ответом.

- **Обратный поиск вызовов процедуры** — десятый MCP-инструмент
  `get_callers`. По точному адресу он показывает три независимых раздела:
  подтверждённые места в коде с процедурой-владельцем, подписки и
  регламентные задания из метаданных, элементы и события формы. Одноимённые
  места без разрешённого модуля ограничиваются и перечисляются отдельно,
  строковые динамические вызовы называются явным ограничением. Выбранное
  расширение остаётся отдельным корпусом, тела модулей не читаются, а
  проверка поколения не позволяет смешать индексы при перезагрузке. Индекс
  форм теперь сохраняет владельца события — саму форму или ближайший элемент.

- **Карточка процедуры из выгрузки кода** — девятый MCP-инструмент
  `get_procedure`. Адрес модуля возвращает оглавление без тел; адрес
  `Модуль::Имя` — сигнатуру, контекст компиляции и окно тела до 200
  строк с готовым вызовом продолжения. Для расширений различаются
  `&ИзменениеИКонтроль`, `&После`, `&Перед` и `&Вместо`; текст чужого
  расширения не подмешивается. Тела читаются с диска и проходят единую
  проверку поколения с оглавлением и вторым корпусом, поэтому параллельная
  перезагрузка не смешивает старый и новый код.

- **Поиск процедур в загруженном коде** — восьмой MCP-инструмент
  `search_procedures`. Точное имя находит в том числе неэкспортные процедуры,
  поиск по словам работает по экспортным именам и комментариям-шапкам;
  сигнатуры читаются из выгрузки с диска только для показанных результатов.
  Необязательный `scope` явно поднимает модули объекта или один точный модуль,
  `extension` выбирает отдельный корпус расширения, а `limit` независимо
  ограничивает оба уровня выдачи. Во время фоновой сборки ответ показывает
  этап X/4 и фактически обработанные N из M элементов; отсутствие кода и
  неверный выбор конфигурации, расширения или области называются прямо.
  Оглавление, вызовы и сигнатуры проходят проверку поколения перед ответом:
  параллельная перезагрузка не смешивает старый индекс с новым файлом и не
  раскрывает локальный путь при ошибке чтения. Неразрешённые одноимённые места
  вызова считаются отдельно, а булевый флаг изменения расширением выводится
  нейтрально, без догадки о виде аннотации.

- **Постановка провайдера `modules`** — `docs/modules-provider-design.md`.
  Согласована 2026-08-20. Внутренние индексы процедур, вызовов, форм и поиска
  уже реализованы и восстанавливаются из кэша; `search_procedures`,
  `get_procedure` и `get_callers` подключены.

  Закрыт вопрос, который разведка оставила открытым: **сколько стоит индекс
  вызовов**. Замерено на «Рознице» (7 878 модулей, 136 909 процедур): 765 112
  рёбер, 77 121 цель, **52 МБ в памяти и 12,4 МБ на диске**, сборка 20 с,
  подъём 0,0–0,2 с. Тот же вопрос без индекса — **1,8 с сканом на каждый
  запрос**. Эти изолированные цифры описывают структуру индекса, но не RSS
  контейнера: холодный и тёплый режимы финального образа измерены целиком выше.

  **Форма хранения оказалась дороже отбора.** Тот же индекс списком кортежей
  вместо таблицы модулей и `array('i')` стоит 141 МБ вместо 52 — втрое. Отбор
  «только экспортные цели» даёт 27 МБ против 52, но теряет внутримодульные
  вызовы, и взят полный вариант.

  Решения владельца: поиск словами — только по экспортным (156 против 473 МБ),
  неэкспортные достаются через оглавление за 0,001 мс; индекс вызовов — по всем
  именам корпуса; разбор — свой лексер; инструментов три, формы и перекрытия —
  блоками в существующих ответах; перекрытие показывается типовым телом плюс
  `#Вставка`/`#Удаление` дословно, чужие расширения — предупреждением без
  текста; чего нет в данных — называется причиной, а не молчанием.

  Отложенный долг `INDEX_RESERVE` закрыт цифрой: 36,3 МБ индексов на 351,4 МБ
  кода — 10,3%, константа 25 МБ заменяется долей 15% с полом.

  Воспроизводится: `python3 tools/lab/measure_calls.py data/modules/<Конфигурация> известные собрать data/lab/calls.marshal`

- **Постановка приёма выгрузки конфигурации в файлы** —
  `docs/modules-intake-design.md`. Согласована 2026-08-19, прошла независимое
  ревью; реализация описана ниже в этом же разделе версии.

  Выгрузка в файлы — гигабайты: **zip 1,35 ГБ, развёрнутая 2 063 МБ, 33 188
  файлов** (замер разведки). Нынешний приём построен под метаданные в единицы
  мегабайт и ломается об это трижды: предел `MAX_UPLOAD` в 500 МБ; тройное
  копирование с пиком около **4 ГБ** — буфер `form()`, копия в `mkdtemp()` и
  третья, `_store_source`, ложащаяся **на сам том**, потому что `keep_source`
  по умолчанию `True`; и гигабайт с лишним по HTTP без возможности продолжить
  после обрыва.

  Решено три вещи. Файл кладётся в `data/incoming/` на томе, а не грузится по
  HTTP. Zip читается членом за членом — развёрнутых 2 ГБ не возникает никогда,
  на диске остаётся только код: `.bsl` дают 260 МБ, формы отдельно не мерились,
  поэтому в текстах стоит «не меньше 260 МБ» до замера. Не хватает места —
  отказ до начала с цифрами, а не полуразобранный источник.

  Место считается **до чтения тела**: центральный каталог zip хранит несжатый
  размер каждого члена. Соврать в опасную сторону это не может — `zipfile`
  обрезает вывод по объявленному размеру, а несовпадение ловит CRC. Для плоской
  выгрузки (8.3.5) цифра — оценка сверху: `file_size` контейнера `.Form`
  включает двоичную запись, которую мы не сохраняем.

  **`.cf` поставки не сохраняется, и это названо отменой.** Разведка считала его
  полезным материалом, приходящим бесплатно. Отброшен: 713 МБ — вдвое больше
  всего сохраняемого кода. Сравнение «типовая против доработанной» не
  реализовано; вернуться к нему следует только по живой задаче.

  `mem_limit` в `docker-compose.yml` намеренно не назначается: разведка
  перечисляет «сумму слоёв одним прогоном» первым пунктом среди неизмеренного,
  а лимит по сумме отдельных замеров в связке с `restart: unless-stopped` даёт
  не отказ, а цикл перезапусков. Поэтому обещание «отказ до начала» сужено до
  **места на диске**: по памяти без лимита гарантии нет.

  **Ревью нашло 14 дефектов, все подтверждены проверкой по коду.** Четыре
  меняли суть:

  - `Registry.sources` — словарь по `id`, а `id` конфигурации это её имя.
    Источник модулей с тем же ключом **вытеснил бы** учётную запись метаданных,
    и `save()` записал бы реестр уже без неё. Введена схема
    `<ИмяКонфигурации>:modules`, поиск состояния — по паре `(kind, sha256)`.
  - `_run_job` в `finally` **безусловно** сносит переданный каталог. На
    `incoming/` это стёрло бы исходник вместе с соседними файлами.
  - Новый вид источника без записи в `Registry.CACHE_KINDS` — `index_cache.sweep`
    снёс бы его кэш на первом же старте; предупреждение об этом висит прямо над
    словарём.
  - Переразбор был повешен на `_code_digest()` — отпечаток всего пакета, чья
    докстрока прямо говорит: «цена грубости — лишняя пересборка, один раз,
    полторы секунды». Ручной переразбор гигабайта после правки любого из
    тридцати файлов пакета — не полторы секунды. Разделено: «индекс устарел»
    сервер чинит сам, «отбор устарел» отмечается отдельной версией правила
    отбора.

  Ещё из найденного: состояний было четыре вместо шести (не было «разбирается»
  и «разбор не удался», из-за чего отказ терялся при рестарте вместе с `_JOBS`);
  кнопки «удалить файл» на `incoming/` не существует — `orphan_sources()` обходит
  только `data/sources`; выгрузка, положенная в `bootstrap/`, давала бы вечную
  ошибку на каждом старте; не было сказано, что «разобрать» требует
  `ADMIN_TOKEN`, что имена членов архива надо санировать и что `chown` из образа
  на bind-mount не действует.

- **Приём выгрузки конфигурации в файлы — реализация.** Постановка выше
  согласована 2026-08-19; код по ней написан тем же днём.

  `data/incoming/` подаётся на том, минуя HTTP-форму — не только ради предела
  `MAX_UPLOAD`, а потому что архив кладётся `cp`, без тройного копирования и
  без риска обрыва передачи на гигабайте с лишним. Отбор — `.bsl` и `Form.xml`
  у иерархической выгрузки, `.txt` и контейнеры `.Form` **целиком** у плоской —
  не меняется между обращениями: версия правила отбора (`SELECTION_VERSION`)
  отделена от отпечатка пакета, чтобы правка кода без правки отбора не гоняла
  архив заново.

  **Контейнер `.Form` сохраняется как есть, вместе с двоичной записью формы.**
  Код обычной формы лежит внутри него записью `module`, и вынимать его на
  приёме означало бы тащить сюда разбор контейнера 1С (`v8container`). Отсюда
  следствие для расчёта места: оговорка постановки про «оценку сверху» для
  плоской выгрузки снята — взвешенное по центральному каталогу и записанное на
  диск совпадают, цифра точная для обоих форматов.

  Место под отобранное считается по центральному каталогу zip — тело архива не
  читается вовсе, отказ на нехватке места приходит до начала разбора. На
  «Рознице» 2.3.10.5 (1 380 МБ) и её расширении (1 МБ) отбирается
  **351,4 МБ из 11 072 файлов** (17,0% развёрнутого) и **6,9 МБ из 155
  файлов** у расширения — замер по одним лишь именам и размерам из
  центрального каталога, воспроизводится `python3 tools/lab/measure_intake.py
  <архив>...`, прогон 2026-08-19.

  Живой прогон самого приёма — не оценка, а реальный вызов
  `intake.planned_size`, `intake.enough_space` и `intake.extract` на тех же
  файлах: оценка требуемого места — **0,15 с**, распаковка членом за членом —
  **3,1 с**, пик RSS процесса — **111 МБ**; файлы и байты, которые вернул
  `extract()`, совпали с тем, что фактически легло на диск. Воспроизводится
  `python3 tools/lab/measure_intake_run.py <каталог для распаковки> <архив>...`
  — новый разведочный скрипт рядом с `measure_intake.py`, прогон 2026-08-19.

  Источник заводится ключом `<ИмяКонфигурации>:modules` — отдельным от
  метаданных той же конфигурации, как и предусматривала постановка. Исходник
  в `data/incoming/` сервер не удаляет ни при успехе, ни при отказе: удалять
  этот каталог принадлежит человеку и сервер его не удаляет.

  Тестов на приём — 62 (`test_intake_extract.py`, `test_intake_select.py`,
  `test_intake_space.py`, `test_incoming_state.py`, `test_incoming_job.py`,
  `test_incoming_page.py`, `test_registry_modules.py`,
  `test_bootstrap_modules.py`); всего в пакете стало 500.

- **Полоса передачи файла на странице «Источники».** Проценты, объём,
  скорость и оценка остатка, дальше — «Файл передан, сервер принимает и ставит
  в разбор». Раньше человек жал «Загрузить» и получал пустой экран на всё время
  заливки: локально это незаметно, на удалённом сервере — минуты.

  Считает браузер (`xhr.upload.onprogress`), и иначе нельзя — см. «Найдено».
  Форма осталась обычной: **с выключенным JS загрузка работает как раньше**,
  просто без полосы. Заодно предел в 500 МБ проверяется до отправки — раньше
  файл-переросток заливался целиком и лишь потом получал отказ.

- **Выбор конфигурации, к которой привязывается выгрузка в файлы.** На
  рабочем сервере владельца загружено четыре конфигурации — приём отказывал
  вживую словами «привязка... пока не реализована». Постановка
  (`docs/modules-intake-design.md`) откладывала привязку провайдеру `modules`,
  но там имелась в виду **автоматическая** привязка по манифесту выгрузки;
  явный выбор человеком проще и снимает блокировку сейчас.

  При нескольких загруженных конфигурациях рядом с кнопкой «разобрать» (блок
  «Входящие выгрузки», страница «Источники») появляется
  `<select name=configuration>` со списком загруженных имён; при одной поле
  не показывается — лишний выбор из одного варианта только мешает,
  `_configuration_for` берёт её сама, как и раньше. Обработчик
  `parse_incoming` проверяет присланное имя до начала разбора: форму
  присылает человек, а имя уходит в путь на диске (`_modules_root`) — имя, не
  найденное среди загруженных, отклоняется с объяснением, `note_failure`,
  редирект 303, как и остальные отказы этого обработчика.

  Тестов добавлено 5 (`test_incoming_job.py`, `test_incoming_page.py`);
  всего в пакете стало 505 (`.venv/bin/python -m pytest`, прогон 2026-08-19).

- **Расширение конфигурации — отдельный источник, а не затирание модулей.**
  Владелец проверил приём на живой паре «типовая конфигурация + расширение к
  ней» и упёрся: обе выгрузки ложились по одному ключу `<Конфигурация>:modules`
  и в один каталог `data/modules/<Конфигурация>/` — расширение (155 файлов,
  6,9 МБ) стирало модули конфигурации (11 072 файла, 351,4 МБ) целиком,
  потому что каталог чистится перед распаковкой.

  Выгрузка расширения распознаётся сама, по `Configuration.xml` в архиве —
  человек по-прежнему выбирает только конфигурацию, кнопки «это расширение»
  нет. Правило — из «Найдено» выше: у расширения есть `ObjectBelonging` и
  `ConfigurationExtensionPurpose`, непустой `NamePrefix`, и нет
  `CompatibilityMode`; у конфигурации наоборот.

  Заведён вид источника `KIND_EXTENSION = "extension"`. Ключ —
  `<Конфигурация>:ext:<Имя>`, имя расширения берётся из тега `Name` его
  собственной выгрузки, а не придумывается человеком: два файла с одинаковым
  тегом `Name` под разными именами файла (человек переименовал архив)
  переразбирают один и тот же источник, а не заводят второй. Код ложится в
  `data/extensions/<Конфигурация>/<Расширение>/` — два уровня, а не склейка
  имён в одно: `index_cache.safe_name` схлопывает необычные символы в
  подчёркивание, и склейка вида `Розница@Доп` дала бы `Розница_Доп`, путь,
  который мог бы совпасть с конфигурацией, названной так же. Каждый уровень
  чистится и проверяется на принадлежность корню тем же способом, что уже
  был у `data/modules/` (`_modules_root`, `_drop_modules_root` →
  `_extension_root`, `_drop_extension_root`). Расширений у конфигурации может
  быть сколько угодно, модулей — как и раньше, ровно одна выгрузка. Платформа
  наследуется от привязанной конфигурации — так же, как у модулей: выгрузка
  расширения точной сборки платформы не содержит.

  `add_modules(path, *, configuration)` остаётся публичной точкой входа для
  обоих случаев — распознавание и ветвление на `_add_extension` спрятаны
  внутри, страница и `restore()` его не касаются. `KIND_EXTENSION` добавлен в
  `Registry.CACHE_KINDS`, иначе `index_cache.sweep` снёс бы кэш нового вида на
  первом же старте. Таблица «Загружено» на странице «Источники» стала
  называть вид источника по-русски и для модулей («Модули»), и для расширений
  («Расширение») — раньше `modules` печаталось латиницей.

  **Независимое ревью показало, что затирание сужено, а не устранено — две
  критичные дыры, обе воспроизведены и обе закрыты в том же коммите.**

  Первая: распознавание читало `Configuration.xml` строго из корня архива.
  Расширение, упакованное обычной командой `zip -r архив.zip папка` (приём —
  `intake.extract`/`safe_target` — вложенность и так поддерживает), корневого
  `Configuration.xml` не имеет; `KeyError` тихо читался как «это
  конфигурация», и `add_modules` шёл в разрушительную ветку модулей.
  Воспроизведено: конфигурация с модулями плюс такой архив расширения — на
  диске остался один файл расширения, источник `<Конфигурация>:modules`, код
  типовой стёрт. Починено поиском `Configuration.xml` также в единственном
  каталоге верхнего уровня. Заодно выяснено на реальном файле: настоящая
  **плоская** выгрузка отраслевой конфигурации на 8.3.5 упакована ровно так
  же — весь архив под одним каталогом верхнего уровня, `Configuration.xml`
  не в корне. Это не гипотетический случай сборки архива вручную, а
  раскладка, в которой реально приходят настоящие выгрузки; без починки эта
  конфигурация отказом из следующего абзаца отклонялась бы как расширение,
  у которого нельзя определить вид.

  Вторая: цена ошибок распознавания несимметрична — ложное «конфигурация»
  стоит весь разобранный код, ложное «расширение» стоит лишний источник.
  Раньше при частичном совпадении признаков (виден один из двух сильных —
  `ObjectBelonging` или `ConfigurationExtensionPurpose` — но не все условия
  расширения сошлись) или при полном отсутствии `Configuration.xml` функция
  молча возвращала «не расширение», и `add_modules` шёл в ветку модулей.
  Теперь оба случая — явный отказ до какого-либо изменения на диске.
  Добавлена и проверка объявленного размера `Configuration.xml` из
  центрального каталога перед чтением в память (боевой файл — 847 КБ, порог —
  8 МБ) — раньше `zf.read` тянул в память что дадут.

  Третья находка, отдельная от распознавания: `safe_name(".")` возвращает
  `.`, а `pathlib` схлопывает такой сегмент при `resolve()`. Для одного
  уровня (`_modules_root`) это ловилось побочно — `корень == база` верно и
  когда единственный сегмент "." Для двух уровней (расширение) — нет:
  `extensions_dir/<Конфигурация>/.` резолвится в САМ каталог конфигурации
  (чужой, уже занятый другими расширениями), а не в `extensions_dir`, и
  проверка на принадлежность корню это пропускала. Воспроизведено: были два
  расширения, разбор архива с `Name = .` оставил на диске только своё, оба
  прежних источника остались в реестре и указывали на пустоту. Починено
  явным отказом сегментов `""`, `.` и `..` до построения пути, а не только
  проверкой после `resolve()`.

  Четвёртая, важная: `Name = a/b` и `Name = a:b` очищаются в один и тот же
  каталог (`index_cache.safe_name` схлопывает и `/`, и `:` в `_`), но ключ
  источника строился из СЫРОГО имени — второй разбор тихо переписывал файлы
  первого поверх, а оба источника оставались в реестре и врали счётчиком.
  Починено: ключ теперь строится из того же очищенного имени, что и каталог
  — у обычных имён чистка ничего не меняет, а два разных расширения,
  чьи имена совпадают после чистки, теперь честно схлопываются в один
  источник (задокументировано в README как обратная сторона решения), а не
  тихо теряют друг друга.

  Заодно устранено дублирование ~20 строк между веткой модулей и
  `_add_extension` — общий приватный `_extract_code` (хеш, снос старого
  каталога, распаковка, отказ на пустом результате) вынесен один раз,
  разница между двумя ветками — только функция сноса и как назвать источник.

  Обезличенный прогон на живой паре конфигурации и расширения 2026-08-20:
  после разбора на
  диске одновременно 11 072 файла модулей (351,4 МБ) и 155 файлов расширения
  (6,9 МБ) — в разных каталогах, ничего не стёрто. Повторено с той же парой,
  но расширение упаковано в единственный каталог верхнего уровня (как в
  критическом сценарии ложного распознавания, собран вручную): распознано верно
  (155 файлов), модули конфигурации не задеты. Второй разбор того же
  расширения под скопированным именем файла не завёл второго источника: в
  реестре остался один `<Конфигурация>:ext:<Имя>`, `origin` обновился на имя
  последнего разобранного файла, модули конфигурации не задеты.

  **Проверка повреждённого манифеста показала, что найденный, но нечитаемый файл
  всё ещё уходил в разрушительную ветку.** `ET.ParseError` и отсутствие
  `Properties` по-прежнему давали мягкое `(False, "")` — «это конфигурация»,
  и `add_modules` сносил уже разобранный код. Причина была в форме
  фикстур: синтетические архивы тестов несли `Configuration.xml = "<x/>"`,
  и мягкий возврат существовал ради них — форма фикстур диктовала поведение
  боевого кода в разрушительной ветке. Воспроизведено на трёх входах,
  каждый раз с потерей кода конфигурации: обрезанный `Configuration.xml`
  (архив валиден, CRC сходится — так выглядит выгрузка, прерванная на
  записи), `Configuration.xml` в чужом пространстве имён (8.2 вместо 8.3) и
  `Configuration.xml` нулевой длины.

  Починено положительным правилом вместо отрицательного: **в ветку модулей
  уходим только при непустом `CompatibilityMode`; во всех прочих случаях —
  отказ с объяснением.** Замер на обеих реальных конфигурациях:
  `CompatibilityMode` непустой что у иерархической выгрузки (`Version8_3_21`),
  что у плоской 8.3.5 (`DontUse`) — а у расширения тега нет вовсе.
  `NamePrefix` для этого решения ненадёжен: у иерархической конфигурации тег
  пустой, у плоской 8.3.5 его нет вовсе. Восемь мест с фикстурой `<x/>`
  (`test_registry_modules.py`, `test_bootstrap_modules.py`,
  `test_incoming_job.py`, `test_intake_space.py`) заменены на общий помощник
  `conftest.modules_configuration_xml()` — с непустым `CompatibilityMode`, —
  чтобы девятая копия не завелась снова где-то ещё. `bootstrap()` и его
  распознавание выгрузки в файлы фикстуру не используют вовсе (решают по
  `_похоже_на_выгрузку_в_файлы`, без `Configuration.xml`) — прогон
  `test_bootstrap_modules.py` подтвердил: не задеты.

  Заодно вернули перехват `OSError`/`BadZipFile` при чтении
  `Configuration.xml` — без него архив с валидным центральным каталогом, но
  битым CRC именно у этой записи, ронял голое исключение вместо объяснения.
  И починили `__MACOSX/`: архив, собранный на macOS через «Сжать объекты»
  (обычный способ владельца), даёт два каталога верхнего уровня — свой и
  служебный `__MACOSX/` с копиями ресурсных вилок — и без исключения этого
  каталога (вместе с `.DS_Store` в корне) поиск единственной обёртки не
  срабатывал.

  Тестов добавлено 3 (нечитаемый манифест не сносит конфигурацию — три
  входа одним тестом; битый CRC даёт понятную ошибку; архив, собранный
  Finder, распознаётся) — было 525, стало 528 (`.venv/bin/python -m pytest`,
  прогон 2026-08-20). Прежняя запись здесь называла 6 — правило проекта
  запрещает цифры на глаз, поправлено.

- **Починка положительного правила сама внесла регрессию
  того же класса, ради которого затевался весь этот круг.** Порядок
  проверок ставил `if значение("CompatibilityMode"): return False, ""`
  раньше отказа и вообще не смотрел на признаки расширения. Воспроизведено:
  манифест со всеми четырьмя признаками расширения (`ObjectBelonging`,
  `ConfigurationExtensionPurpose`, непустой `NamePrefix`) плюс
  `CompatibilityMode=Version8_3_12` давал `(False, '')` — ветка модулей —
  снесённый `data/modules/<Конфигурация>/`. На предыдущем коммите тот же
  архив получал явный отказ; guard, который это ловил, потерялся при
  переходе на позитивную формулировку правила.

  Починено порядком, а не новым условием: сильные признаки расширения
  (`ObjectBelonging`, `ConfigurationExtensionPurpose`) проверяются первыми и
  безусловно закрывают дорогу в ветку модулей — дальше выбор только между
  «расширение» (полный набор из четырёх условий) и отказом, независимо от
  `CompatibilityMode`. Только когда ни одного сильного признака нет вовсе,
  решает `CompatibilityMode`. README поправлен: прежняя формулировка
  обещала отказ там, где по факту был бы тихий переход в ветку модулей.

  **Заодно — снос каталога кода шёл до распаковки, а не после.**
  `_extract_code` чистил `корень` и только затем звал `intake.extract`: любая
  ошибка распаковки (битый CRC у МОДУЛЯ, а не у манифеста — воспроизведено
  тестом; тот же исход дали бы кончившееся место или права) или пустой
  результат (все члены отсеклись санитизацией `safe_target`) оставляли
  каталог пустым или полупустым, а источник в реестре продолжал висеть со
  `status=ready` и старым `items_total` — реестр и диск расходились.

  Починено перестановкой: распаковка идёт в соседний временный каталог
  внутри того же родителя, старый каталог сносится и временный встаёт на
  его место (`rename`, атомарно в пределах одной файловой системы) только
  после того, как `intake.extract` уже гарантированно отработала и вернула
  непустой результат. Любое исключение или пустой результат до замены не
  доходят вовсе — прежний разбор остаётся как был.

  **Мелочь того же прогона: `intake.is_wanted` не знал про `__MACOSX/`.**
  Архив, собранный Finder («Сжать объекты» на macOS), несёт в `__MACOSX/`
  копии ресурсных вилок каждого файла под именем `._Имя` — с тем же
  суффиксом, что у оригинала. `__MACOSX/.../._ObjectModule.bsl` проходил
  отбор как настоящий `.bsl`, и на диск рядом с настоящим модулем ложился
  двоичный AppleDouble-файл (воспроизведено: `items_total=2` на архиве с
  одним настоящим модулем). Починено: члены из `__MACOSX/` и имена,
  начинающиеся на `._`, отбор пропускает.

  Тестов добавлено 3 (регресс `CompatibilityMode` поверх признаков
  расширения; ошибка распаковки не уносит прежний разбор; мусор Finder не
  попадает в отобранный код) плюс один существующий тест переписан на
  верное поведение (пустой результат распаковки теперь сохраняет прежний
  разбор, а не сносит его). Было 528, стало 531
  (`.venv/bin/python -m pytest`, прогон 2026-08-20).

- **Снос каталога на месте и молчаливая смена прав
  каталога кода.** `drop(корень)` и `временный.rename(корень)` стояли вне
  `try` — отката для них не было. Прерывание процесса в этом окне (секунда
  `rmtree` на 11 072 файлах, не три секунды распаковки, но не ноль) или,
  хуже, отказ `rmtree(..., ignore_errors=True)` по правам на части файлов
  (реальный триггер: контейнер под uid 10001, том владельца — bind-mount, о
  чём предупреждает сама форма приёма) оставляли каталог непустым, роняли
  `rename` с `ENOTEMPTY`, утекал временный каталог, а старый разбор
  оказывался снесён наполовину при живой записи «разобрано» в реестре.

  Починено рокировкой, а не заплаткой: старое содержимое не сносится на
  месте, а сначала отставляется в сторону (`корень → отставленный`), затем
  распакованное встаёт на место корня (`временный → корень`), и только
  потом отставленное сносится. Точка невозврата — одно переименование, а не
  многосекундный снос: если второе не удалось, отставленное возвращается на
  место корня, временный каталог убирается, и исключение летит дальше —
  прежний разбор снова цел. Окно между двумя `rename` осталось (доли
  секунды, не три и не одна) — это ограничение модели «переименование, а не
  журналируемая транзакция», названо в докстроке отдельно от того, что уже
  гарантируется.

  Заодно: `tempfile.mkdtemp` создаёт каталог с правами 0700, и после
  рокировки их получал бы `data/modules/<Конфигурация>/` — молча, каждый
  разбор. Раньше каталог создавал `mkdir` внутри `extract()` и получал
  обычные 0755. Человек на хосте (bind-mount) после переразбора вдруг
  перестал бы заходить в каталог, который до этого читался свободно.
  Временному каталогу теперь выставляются права прежнего разбора, а при его
  отсутствии — обычные 0755, до переименования.

  Правило отбора `__MACOSX/`/`._` (закрыто в прошлом коммите) получило
  раздельное покрытие в `test_intake_select.py`: единственный прежний тест
  бил сразу по обоим условиям одним архивом «как Finder», и снятие любого
  одного правила тесты бы не поймали.

  Докстрока `_extract_code` и README раньше обещали больше, чем делал код:
  «любое исключение оставляет прежний разбор нетронутым» было неверно для
  исключений из сноса и переименования (правится этим же коммитом) — и всё
  равно осталось бы неточным: гарантия про ошибку РАСПАКОВКИ и про отказ
  финального переименования, а не про любую мыслимую ошибку. Уточнено в
  обоих местах. README, раздел «Что остаётся на диске», заодно назвал то, о
  чём молчал: мусор Finder в код не попадает, а на время рокировки на диске
  лежат обе копии — подушка «место мерили, когда старое уже освободилось»
  на время распаковки больше не работает.

  Тестов добавлено 2 (второе переименование не удалось — прежний разбор
  восстановлен; режим каталога не сужается после переразбора) и 2 в
  `test_intake_select.py` (правило `__MACOSX`/`._` — раздельно и обратная
  сторона: законный каталог метаданных с именем `__MACOSX`, но не на первом
  уровне пути, берётся). Было 531, стало 535 (`.venv/bin/python -m pytest`,
  прогон 2026-08-20).

- **Требование «вся рокировка под try» было выполнено для
  одной ветки из четырёх.** Прогон с подменой `Path.rename` показал: сам
  откат (`отставленный.rename(корень)`) ничем не защищён. Провал второго
  переименования и следом провал отката (природа отказа у обоих одна —
  права, bind-mount, «два подряд» не выдумка) оставляли каталог `корень`
  не существующим вовсе: прежний разбор физически цел под
  `.Имя.old-…`, а реестр продолжает показывать `status=ready` по
  пропавшему пути — после рестарта `restore()` тихо выкидывает источник,
  которого как будто никогда не было, хотя данные на диске никуда не
  делись. Первое переименование (`корень → отставленный`) и переименование
  в ветке самого первого разбора тоже были вне `try` — их провал протекал
  полностью распакованным временным каталогом (сотни мегабайт на боевой
  конфигурации), который не подчищал ни рестарт, ни следующий разбор.

  Починено тремя вещами, без новых механизмов. Вся рокировка — под одним
  `try`/`finally`: `finally` убирает временный каталог при любом исходе
  (успех — каталог уже переехал, `rmtree` на несуществующем пути тихий
  no-op; любой провал — каталог ещё на месте, убирается). Провал самого
  отката больше не молчит: наверх летит `RegistryError`, называющий в
  тексте оба пути — где лежит прежний разбор, где пустой или частично
  распакованный новый — чтобы вернуть каталог руками. И подметание: перед
  началом разбора убираются осиротевшие `.<Имя>.tmp-*` от прежних неудач
  (`_sweep_stale_extract_tmp`) — а вот `.<Имя>.old-*` не трогается никогда:
  в нём может лежать единственная уцелевшая копия прежнего разбора, и
  решение о ней — за человеком, не за сервером при следующем нажатии
  кнопки.

  Докстрока `_extract_code` и README раньше обещали безусловно: «не
  удалось — отставленное возвращается на место». Верно только когда сам
  откат удался — уточнено в обоих местах, с явным названием нового
  поведения на случай, когда не удался.

  Тестов добавлено 4: провал первого переименования (прежний разбор цел);
  провал переименования при первом разборе (мусора не осталось); провал
  отката (`RegistryError` с обоими путями в тексте); подметание
  осиротевшего `.tmp-` при живом `.old-` рядом. Все четыре падали на
  предыдущем коммите — проверено `git stash` с полным выводом, а не на
  слово. Было 535, стало 539 (`.venv/bin/python -m pytest`, прогон
  2026-08-20).

### Изменено

- **Иерархический приём больше не выбрасывает доказательство существования
  контейнерной формы.** Канонические XML-дескрипторы и `Ext/Form.bin`
  сохраняются рядом с прежними `.bsl` и `Form.xml`; посторонние XML/bin по
  похожему суффиксу не проходят. `planned_size` и распаковка используют один
  набор, исходный контейнер не меняется и производный `.bsl` не создаётся.
  `SELECTION_VERSION` поднята до 4: источник версии 3 показывается как «отбор
  устарел», но startup его не переразбирает; только явный административный
  reparse атомарно заменяет корень, кэш и `registry.json`. Версия отбора
  входит в штамп кэша, а журнал рокировки позволяет startup завершить или
  откатить замену после аварийной остановки между `rename` и записью реестра.

- **Приём кода проверяет место внутри жизненного цикла реестра.** После выбора
  конфигурации и вида источника требуется объём безопасно отобранных файлов
  плюс `max(ceil(15%), 25 МБ)` под индекс. Расчёт, предпроверка и распаковка
  используют один набор: опасные пути и предыдущие дубли не завышают план,
  `*.Template.txt` исключён, правило отбора поднято до версии 3. Ошибка места
  сохраняет прежний разбор и не раскрывает локальные пути.

- **Холодная пересборка индексов кода больше не задерживает запуск сервера.**
  Валидный кэш по-прежнему поднимается сразу, а при промахе четыре индекса
  строятся фоном и публикуются только готовым комплектом. Пока идёт сборка,
  существующие MCP-инструменты и `/health` продолжают отвечать;
  `POST /admin/reload` также выполняет восстановление вне event loop.
  `HEALTHCHECK --start-period` увеличен с 40 до 90 секунд как запас на
  восстановление остальных источников до открытия порта. Параллельное
  снятие источника или перезагрузка реестра больше не могут опубликовать
  устаревший индекс или оставить новый код со старой записью реестра.
  При `remove` старый каталог сначала отставляется от canonical path, поэтому его
  долгое удаление не может снести код, загруженный заново под тем же именем.
  Невозможность удалить расходный файл кэша больше не прерывает
  снятие источника: оставшийся кэш ничем не заявлен и не восстанавливает
  Source после рестарта.
  Модули и расширения теперь поднимаются вторым проходом и привязываются
  к identity своей конфигурации: обратный порядок строк `registry.json` работает,
  а старый код не публикуется, если владелец исчез или был заменён во время
  того же `restore`. Снимок generation не даёт старой строке откатить уже
  завершённый параллельный reparse даже после исчезновения его active token.
  Живые разборы одного источника теперь сериализованы на весь цикл
  `sweep → extract → build → publish`: новая заявка заранее инвалидирует
  старую, но не удаляет её активный временный каталог. Ожидающая устаревшая
  заявка отменяется до распаковки, из нескольких поколений публикуется только
  последнее; разные источники остаются независимыми. Реальная ошибка файловой
  системы актуального разбора сохраняется без подмены сообщением об отмене.
  Финальный `save()` фоновой сборки ждёт завершения startup и сверяет его
  поколение с последним успешно завершённым: быстрый первый источник больше
  не может записать в `registry.json` частичный список до восстановления
  следующих строк. Если startup оборвался, ожидавший фон не заменяет прежний
  полный снимок частично восстановленным состоянием.
  Живой контейнерный замер 2026-08-21 на холодном кэше трёх источников кода:
  73 из 73 вызовов `/health` и `search_objects` успешны; медиана/худшее время —
  5,76/171,01 мс и 8,11/172,49 мс соответственно. Воспроизводится скриптом
  `tools/lab/measure_background_responsiveness.py` по команде из README.

- **Колонка журнала «Принято» переименована в «Размер».** Она врала: задание
  заводится, когда файл уже пришёл целиком, и хода приёма журнал не показывает.
  Размер мелких файлов показывается в килобайтах — «0.0 МБ» для выгрузки в
  1588 байт не сообщало ничего.

- **Обёртка архива не воспроизводится в пути на диске.** Архив, упакованный
  `zip -r архив.zip папка` или через Finder («Сжать объекты»), кладёт всю
  выгрузку внутрь одного каталога верхнего уровня — раньше этот уровень
  ложился и на диск: `data/extensions/<Конфигурация>/<Расширение>/<Расширение>/Catalogs/…`
  вместо `.../Catalogs/…`. Сегодня это было безвредно — путями разобранного
  кода никто не пользовался, — но провайдеру `modules` они понадобятся, чтобы
  привязывать процедуру к объекту метаданных, и тогда один и тот же объект
  лежал бы по двум разным путям в зависимости от того, чем человек упаковал
  архив. `intake.extract`/`planned_size` теперь снимают единственный каталог
  верхнего уровня, если он один.

  Признак обёртки — то же самое правило, что уже применяла
  `registry._единственный_верхний_каталог` при распознавании вида выгрузки
  (мусор Finder — `__MACOSX/`, корневой `.DS_Store` — в счёт не идёт).
  Правило вынесено в `intake._единственный_верхний_каталог`, `registry`
  теперь зовёт его оттуда: одно правило, два вызывающих — тем же приёмом, что
  и `index_cache.safe_name` для очистки имён. Санитизация путей
  (`safe_target`, отказ членам, ведущим наружу корня) применяется уже после
  снятия обёртки, а не вместо неё; добавлен отдельный отказ признавать
  обёрткой каталог `..`/`.` — реиспользование правила для нового вызывающего
  открыло путь, которого не было у прежнего: архив, целиком упакованный под
  `../` как единственный «каталог верхнего уровня», без этого отказа снял бы
  стриппингом именно ту часть пути, которую `safe_target` обязан отвергнуть.

  `SELECTION_VERSION` поднята до 2 — меняется именно то, что достаётся из
  архива и куда кладётся: тот же файл, разобранный старым правилом, лежит на
  диске на один уровень глубже, чем разобранный новым. Уже разобранный код
  задним числом не переписывается — человек увидит «отбор устарел» и
  переразберёт кнопкой.

  Проверено на трёх реальных обезличенных выгрузках: плоская выгрузка
  8.3.5 упакована ровно так же — весь архив под
  одним каталогом верхнего уровня; после снятия обёртки 2877 файлов
  (125,8 МБ) легли без лишнего уровня. Иерархические конфигурация и
  расширение (без обёртки) дали прежние числа — 11 072 и 155 файлов
  соответственно; `planned_size` и `extract` сошлись по байтам на всех трёх
  архивах.

  Тестов добавлено 4 (`test_intake_extract.py`: обёртка снимается, несколько
  каталогов верхнего уровня обёрткой не считаются, мусор Finder не ломает
  распознавание и не попадает в отбор, `planned_size`/`extract` сходятся по
  байтам), один существующий тест на дублирующиеся имена перестроен — без
  второго каталога верхнего уровня он сам оказался бы обёрнутым и попал под
  новое правило случайно.

  **Проверка нормализации пути нашла две дыры и одну неточность — все три из
  одной причины: правило переиспользовано для нового назначения (запись на
  диск), а разбор пути остался старым (`str.split("/")`), рассчитанным только
  на то, чтобы найти `Configuration.xml` в namelist.**

  Первая — критическая: пустой первый компонент пути (`части[0] == ""` у
  `"/Catalogs/…".split("/")`) не отбраковывался как имя обёртки. Архив
  целиком из абсолютных путей давал «обёртку» `""`, стриппинг снимал ведущий
  `/`, и абсолютный член, которого `safe_target` обязан отвергать, тихо
  становился относительным и писался на диск. Воспроизведено архивом
  `/Configuration.xml` + `/Catalogs/…` + `/../снаружи.bsl`: до починки
  `extract` возвращал `(1, 10)` вместо ожидаемого `(0, 0)`, и `add_modules`
  перетирал бы уже разобранный код архивом, который раньше честно отклонялся
  бы `_нет_модулей`.

  Вторая: срез строкового префикса (`name[len(префикс):]`) неверно обрабатывал
  двойной слэш — `"Обёртка//Catalogs/…"` превращался в `"/Catalogs/…"` (снят
  только один слэш из двух), результат читался как абсолютный путь и
  отвергался `safe_target`, хотя `planned_size` эти байты уже посчитал —
  расхождение планового и фактического, в безопасную для целостности, но не
  для доверия к цифре сторону (файл терялся молча, наружу корня не выходил).

  Обе чинятся одним приёмом: `_единственный_верхний_каталог` и `_без_обёртки`
  разбирают путь через `PurePosixPath.parts`, а не `str.split("/")` — двойной
  слэш и одиночная точка схлопываются сами, ведущий слэш даёт явный компонент
  `"/"` (а не пустую строку), и `путь.is_absolute()` ловит абсолютность
  напрямую. Guard на `..`/`.`/пустую строку первым компонентом оставлен как
  явная защита сверху, даже там, где `PurePosixPath` теперь и сам не должен
  их порождать — дешевле, чем доказывать заново на каждой следующей правке.

  Заодно guard действует и на распознавание вида выгрузки, не только на
  раскладку по диску: архив, у которого «единственный каталог верхнего
  уровня» — это `..`, `.` или пустая строка (абсолютные пути), теперь
  читается как «`Configuration.xml` не нашёлся», а не пытается угадывать —
  `registry._найти_configuration_xml` зовёт то же правило `intake`.

  Третья (неточность, не дыра): `registry._отбираемых_членов` — предпроверка
  «есть ли что взять» до сноса прежнего разбора — была третьим вызывающим
  правила отбора и не снимала обёртку, в отличие от `intake.extract`/
  `planned_size`. На обычных архивах числа совпадали случайно (`is_wanted`
  решает по суффиксу и последнему компоненту пути, глубина ему не важна), но
  на члене вроде `Обёртка/__MACOSX/x.bsl` разошлись бы: предпроверка сочла
  бы его отобранным по сырому имени (первый компонент — `Обёртка`, не
  `__MACOSX`), а `extract` после снятия обёртки — мусором Finder. Вреда не
  было (`_extract_code` ловит пустой результат отдельной проверкой уже после
  попытки распаковки), но заявка «одно правило, три вызывающих» была неточна.
  Выровнено: `_отбираемых_членов` тоже снимает обёртку тем же вызовом
  `intake._единственный_верхний_каталог`/`_без_обёртки`.

  Добавлен тест на инвариант, которым держится всё правило целиком:
  настоящий единственный каталог верхнего уровня без обёртки (например,
  расширение, где все объекты лежат в `Catalogs/`) раздетым не бывает —
  архив без `Configuration.xml` (ни в корне, ни в этом самом каталоге)
  отклоняется распознаванием раньше, чем дело доходит до `extract`.

  Тестов добавлено ещё 4 (архив из абсолютных путей не трогает прежний
  разбор, двойной слэш под обёрткой не теряет файл, единственный каталог без
  манифеста отклоняется до `extract`, `_отбираемых_членов` и `extract`
  сходятся на архиве с вложенным `__MACOSX`). Было 541, стало 549
  (`.venv/bin/python -m pytest`, прогон 2026-08-20).

### Исправлено

- **Состояние «отбор устарел» было недостижимо ни при каких данных.** У
  `Source` поля `selection_version` не было вовсе, а `_состояние` в
  `incoming.py` читало его через `getattr(источник, "selection_version",
  SELECTION_VERSION)` — отсутствующий атрибут всегда подставлял ТЕКУЩУЮ
  версию, и сравнение `устарел < SELECTION_VERSION` не срабатывало никогда.
  Кнопка «переразобрать» у этого состояния была подписана верно, добраться до
  него было нельзя. Осознанное решение до тех пор, пока правило отбора не
  менялось, — но выше правило поменялось, и без работающего механизма человек
  никогда не увидел бы приглашение переразобрать код под новым правилом.

  `Source` получил поле `selection_version: int = 0` (0 — «неизвестно»),
  `to_dict`/`from_dict` его переносят, причём `from_dict` для отсутствующего
  ключа подставляет 0, а не текущую версию: соврать «отбор свежий» про
  запись, о которой ничего не известно, — это ровно та ошибка, из-за которой
  человек не переразберёт устаревший код. `add_modules` и `_add_extension`
  пишут в источник `intake.SELECTION_VERSION`, `_состояние` сравнивает поле
  источника с текущей версией напрямую.

  Тестов добавлено 2 (свежий разбор → «разобрано», версия ниже текущей →
  «отбор устарел», запись без поля в `registry.json` после `restore()` →
  «отбор устарел»), один существующий тест уточнён явной версией отбора.
  Было 539, стало 541 (`.venv/bin/python -m pytest`, прогон 2026-08-20).

- **Разбор выгрузки из `incoming/` не переживал перезапуск.** `add_modules`
  пишет только в `self.sources`, в память процесса, а обработчик разбора —
  в отличие от `_index_source` — не звал `registry.save()`. В `registry.json`
  источника модулей не появлялось: после рестарта страница показывала «не
  разобрано» при 351 МБ кода на диске, ветка `KIND_MODULES` в `restore()` была
  недостижима, и человек гонял гигабайтный архив заново. Существующий тест был
  зелёным потому, что смотрел в `registry.sources`, а не в файл.

- **«Разбор не удался» был тупиком.** Кнопки у этого состояния не рисовалось
  (постановка, §2, назначает ему действие «разобрать»), а записанная неудача
  привязывалась к имени файла и проверялась раньше проверки по имени — то есть
  исправленный архив, положенный под тем же именем, состояния не снимал. Выйти
  можно было только переименованием файла или правкой `incoming-state.json`.
  Теперь неудача хранится вместе с sha256 файла, на котором случилась, и
  считается устаревшей, как только содержимое изменилось; старый формат
  (причина строкой) читается по-прежнему — файл расходный, но ронять показ он
  не вправе.

- **Переразбор смешивал две выгрузки в одном каталоге.** `extract` писал
  поверх, и файлы, которых в новой выгрузке нет (удалённый объект,
  переименованный модуль), оставались навсегда. `data/modules/<Имя>/` теперь
  очищается перед распаковкой. Вместе с этим имя конфигурации из манифеста
  санируется тем же правилом, что и имена файлов кэша (`index_cache.safe_name`,
  вынесено из `path_for`), а построенный путь проверяется на принадлежность
  `data/modules/`: чистка сохраняет точку, поэтому «..» проходит через неё
  неизменным, а рядом лежит `incoming/` с исходником человека.

- **Архив без единого отобранного файла давал «разобрано».** Выгрузка
  метаданных schema v1 — тоже `.zip`; отбор не находил в ней ничего, а источник
  заводился со `status=ready` и `items_total=0`. Теперь это отказ с объяснением,
  куда такую выгрузку подавать.

- **Каталог `data/incoming/` сервер не создавал.** `mkdir` из `Dockerfile` на
  bind-mount не действует, а `scan()` на отсутствующем каталоге возвращает
  пустой список — блок «Входящие выгрузки» не рисовался вовсе, и человек не
  видел даже подсказки, куда класть файл. Каталог создаётся при старте, пустой
  показывается с подсказкой, а в README сказано, что сканируется только он сам,
  без вложенных подкаталогов (`glob` нерекурсивный).

- **Заголовок «не похоже на zip-архив» покрывал и падение проверки места.**
  Один `try` накрывал `planned_size` и `enough_space`; вторая падает по другим
  причинам — нет прав на каталог данных, том отвалился, — и получала
  заголовок-ложь. Разнесено на два блока; в тексте про место названы права и
  uid 10001, как требует постановка (§3).

- **sha256 гигабайтного файла считался в цикле событий.** `GET /sources` —
  `async def`, и сканирование `incoming/` шло прямо в нём: первый показ
  страницы после появления архива на 1,4 ГБ останавливал весь процесс —
  остальные страницы, `/health`, инструменты MCP. Сканирование уходит в поток
  тем же `run_in_threadpool`, которым пользуется разбор, и передаётся в
  страницу готовым списком.

- **Признак «файл ещё копируется» был объявлен и не использован.**
  `SETTLE_SECONDS` не участвовала нигде: файл виден с первой секунды `cp`, клик
  по нему давал битый архив и вечную неудачу, а растущий `mtime` обесценивал
  кэш хеша — sha256 полутора гигабайт пересчитывался на каждом показе страницы,
  которая сама себя обновляет раз в две секунды. Теперь у файла, менявшегося
  меньше `SETTLE_SECONDS` назад, хеш не считается вовсе, строка показывается с
  подробностью «файл ещё копируется», кнопки нет, а обработчик такой файл не
  берёт и говорит почему. Новых состояний не заведено — их по-прежнему шесть.

- **Снятие источника модулей оставляло каталог на диске навсегда.**
  `Registry.remove` для `KIND_MODULES` уходил в ветку «ничего не знаю», а
  `orphan_sources` обходит только `data/sources` и этот каталог не показывает:
  351 МБ занимались невидимо и никаким действием через интерфейс не
  возвращались. Теперь снятие уносит `data/modules/<Имя>/` — с той же проверкой
  принадлежности каталогу, что и очистка перед распаковкой.

- **Мелочи страницы «Источники».** Кнопка у обновлённой выгрузки и устаревшего
  отбора подписана «переразобрать», а не «разобрать» — человек должен понимать,
  что перетирает уже лежащий код. Отказ по занятости («разбор уже идёт») был
  молчаливым редиректом и теперь объясняется в журнале заданий. Отказ привязки
  при нуле загруженных конфигураций назывался неверную причину («привязка не
  реализована» вместо «привязывать не к чему»). Комментарий про условный
  `rmtree` в `_run_job` описывал механизм, которого нет. Общий словарь
  `IncomingScanner._state` (в него пишет поток разбора, из него читает цикл
  событий) закрыт замком: `_save` ловил только `OSError`, и «dictionary changed
  size» уронил бы показ страницы.

- **Очистка каталога кода стояла до проверки годности архива.** `add_modules` сносил
  `data/modules/<Имя>/`, затем распаковывал, и только потом смотрел, отобралось
  ли хоть что-нибудь. Выгрузка структуры метаданных, поданная в `incoming/` по
  ошибке (ровно тот случай, на который рассчитан текст отказа), сносила 351 МБ
  уже разобранного кода: проверка места не спасала — отбираемого ноль, нужен
  один `INDEX_RESERVE`. Годность архива теперь выясняется по центральному
  каталогу zip **до** того, как что-то удалено; сигнатуры `intake` не менялись.
  Падение `extract` на середине (кончилось место, битый член, CRC) прежний
  разбор по-прежнему губит — это лечится только распаковкой во временный
  каталог с подменой, и в эту волну не входило.

- **Метка времени в будущем давала вечное «файл ещё копируется».** Проверка
  возраста была односторонней, а `cp -p`, `rsync -t`, `mv` с другого тома и
  перекос часов контейнера ставят `mtime` вперёд. Файл получал «копируется»
  навсегда: ни кнопки, ни разбора. Возраст сверяется с обеих сторон.

- **`note_failure` и снятие источника работали в цикле событий.** Первая после
  привязки отказа к содержимому считает sha256 — на архиве в 1,4 ГБ это
  секунды, а три её вызова из пяти сидят в `async def parse_incoming`. Вторая
  перестала быть словарной операцией: у источника модулей снятие уносит 11 072
  файла, причём `Registry.remove` держал на всём этом общий замок реестра —
  тот же, что берут `resolve()` и инструменты MCP. Удаление каталога вынесено
  из-под замка, оба вызова уходят в `run_in_threadpool`.

- **Проверка «ноль записанных файлов» после распаковки вернулась.** Она была
  заменена предпроверкой по центральному каталогу, а не дополнена ею — и
  `status=ready` при пустом каталоге снова стал достижим: предпроверка считает
  имена, а `extract` прогоняет каждый член ещё и через `intake.safe_target`,
  который отвергает абсолютные пути и `..`. Архив, у которого все отбираемые
  члены такие, предпроверку проходит и не кладёт на диск ничего. Обычным
  архиватором и выгрузкой 1С такое не собирается, но проверка стоит пять
  строк, а счётчик уже на руках. Обе проверки теперь на месте и отвечают одним
  текстом: предпроверка бережёт прежний разбор от удаления, вторая ловит то,
  что отсеяла санитизация имён.

  Заодно sha256 архива считается **после** проверки годности: негодный архив
  оплачивался полным проходом по файлу.

- **`IncomingScanner._save` писал файл вне замка.** Под замком собирался
  снимок, а `write_text` шёл уже без него — и после ухода `note_failure` в
  потоки пула два сохранения успевали разойтись: на диск ложился более старый
  снимок. Кэш хеша потерю переживает, записанный отказ — нет, он и существует
  ради того, чтобы пережить рестарт. Запись под тем же замком, `try/except
  OSError` на месте: том только на чтение по-прежнему не роняет показ.

### Найдено

- **Двенадцать форм плоского имени не образуют декартов продукт с видами
  метаданных.** Обезличенный read-only проход подтвердил 18 стандартных видов
  и 49 сочетаний `kind/pattern`, тогда как разрешение каждого pattern каждому
  виду приняло бы 234 сочетания. Например, `CommonForm.<Имя>.Module.txt` и
  `CommonCommand.<Имя>.Module.txt` синтаксически похожи на модуль, но не входят
  в формат и создают тот же публичный адрес, что каноническая форма или
  команда. Плоский адресатор поэтому использует точную allow-list матрицу;
  остальные 185 сочетаний дают обезличенный отказ, а не угадываемый адрес.
  Контракт воспроизводится командой
  `.venv/bin/python -m pytest tests/test_flat_module_address.py -q`.

- **Холодный пик и тёплая память провайдера различаются больше чем вдвое.**
  Финальный образ измерен 2026-08-21 на рабочем корпусе из трёх источников
  кода и 12 файлов кэша. После удаления только расходного модульного кэша все
  12 файлов построились за 125 с: `memory.peak` cgroup — 1 142,9 МиБ,
  `memory.current` после готовности — 1 130,6 МиБ, RSS PID 1 — 651,4 МиБ,
  `docker stats` — 699,1 МиБ. Тёплый перезапуск того же контейнера занял 19 с:
  517,3 МиБ пика cgroup, 504,2 МиБ текущего cgroup, 492,2 МиБ RSS PID 1 и
  469,4 МиБ по `docker stats`. Оба запуска завершились `healthy` при
  `restart_count = 0` и `oom_killed = false`; в 146 строках журнала по нулю
  вхождений `traceback`, `exception`, `critical` и `error`. Cgroup включает
  кэш страниц, поэтому не равен RSS процесса или `docker stats`; это корпусный
  ориентир, а не готовое значение `mem_limit`. Полные команды
  воспроизведения приведены в README.

- **Гипотеза «переразбор требует удвоить новый код поверх свободного места»
  опровергнута.** Старый канонический корень действительно остаётся до
  рокировки, но его байты уже вычтены из `disk_usage.free`. Поэтому
  дополнительная потребность одинакова для новой загрузки и переразбора:
  выбранные новые файлы плюс резерв индекса; формула `2 × выбранное + резерв`
  повторно учитывала старый объём.

- **Плоская выгрузка 8.3.5 именует скомпилированный общий модуль
  `CommonModule.<Имя>.Module`.** Команда
  `python3 tools/lab/measure_flat.py <каталог плоской выгрузки>` нашла семь
  таких файлов; каталожная форма
  `CommonModules/<Имя>.Module` остаётся вторым поддержанным каноническим
  вариантом. Иные `*.Module` и варианты регистра не считаются кодом.

- **Директивы правки расширения закрывают текст запроса дважды.** Внутри
  многострочного литерала `#Удаление` и `#Вставка` несут каждая свою закрывающую
  кавычку. Автомат, просто пропускающий строки на `#`, считает первую
  закрывающей, вторую — открывающей и уходит «внутрь литерала» до конца файла:
  на живом расширении это дало две процедуры без `КонецПроцедуры` и потерянное
  объявление в общем модуле с 56 директивами правки. Правило: внутри `#Удаление`
  текст мёртв, состояние ведётся по ветке `#Вставка`.

- **Многострочная сигнатура прячет `Экспорт`.** Разбор, читающий только строку
  объявления, теряет признак у **2 074 процедур из 49 182** — 4,2%, и именно у
  самых длинных сигнатур. Такая процедура выпадает из поиска по экспортным и
  показывается агенту как непубличная.

- **Имя цели перекрытия — строковый литерал**, поэтому разбор аннотации по
  очищенной от литералов строке даёт пустое имя у **185 перекрытий из 185**.
  Отказ молчаливый: счётчик перекрытий выглядит правдоподобно.

- **Имя процедуры не является ключом — цифры сильнее, чем считалось.**
  `ПриСозданииНаСервере` объявлена в **3 003 модулях** конфигурации розничных
  продаж, `ПриОткрытии` — в 1 464, `ПередЗаписью` — в 644. Ответ «кто вызывает»,
  собранный по одному имени, врёт и выглядит правдоподобно.

- **Четверть процедур не вызывается по имени нигде** — 33 743 из 137 115
  (24,6%), из них 25 606 в модулях форм. Плюс 6 765 `Выполнить(` и 150
  `Вычислить(` — вызовы строкой, которых индекс не видит принципиально. Пустой
  ответ «кто вызывает» без объяснения читается как «мёртвый код».

- **Привязка обработчиков формы лежит в `Form.xml`** в виде
  `<Event name="OnOpen">ПриОткрытии</Event>` — **23 210 привязок в 3 047 формах**,
  покрывают 23 198 процедур модулей форм. Это и есть ответ на «кто вызывает» для
  тех, кого не вызывает никто.

- **Хранение сигнатуры в памяти стоит вдвое дороже самого оглавления.**
  Оглавление 137 115 процедур: 128 МБ кортежами с текстом сигнатуры, 62 МБ
  параллельными массивами без него. Сигнатура читается с диска по номеру строки
  за 0,1 мс на модуль.

- **1С разрешает комментарий внутри незакрытого строкового литерала.** Пока
  строка не закрыта, строка файла, начинающаяся с `//`, — комментарий, а не
  содержимое и не конец. Живой случай: разработчик закомментировал кусок текста
  запроса **вместе с закрывающей кавычкой** (`//|  … ";`), и лексер, считающий
  эту кавычку закрывающей, теряет состояние до конца файла — одна такая строка
  съела 184 объявления. Случаев: 2 049 строк в обезличенном крупном
  корпусе (100 файлов), 282 в типовой конфигурации розничных продаж
  (47 файлов).

- **Регулярка на BSL режет тексты запросов.** В «Рознице» 239 574 строки —
  продолжения многострочного литерала через `|`, и **5 563 из них содержат `//`
  внутри литерала**. Снятие комментария простым `find("//")` портит текст
  запроса в каждом сороковом случае. Ещё 38 341 строка содержит удвоенную
  кавычку `""`, 38 строк несут слово `Процедура`/`Функция` внутри литерала,
  две — `КонецПроцедуры`.

- **`Асинх Процедура` — вид объявления стоит вторым словом** (8.3.18+).
  Проверка «первое слово строки» теряет такие объявления: в конфигурации
  розничных продаж их **207**. Первая цифра — 17 — была снята на смещённой
  выборке (только файлы, где не ошибался tree-sitter) и **опровергнута**
  сплошным прогоном. Это же закрыло расхождение счётчиков: 136 909 у регулярки
  без `Асинх` плюс 207 даёт 137 116 против 137 115 у лексера — разница в одну
  запись, а не в 206.

- **`str.splitlines()` нельзя применять к модулям 1С.** Он режет по `\v`, `\f`,
  `\x1c–\x1e`, `\x85`, `U+2028`, которых для платформы не существует, а
  одиночный `CR` без `LF` в корпусе встречается: 50 штук в 6 файлах «Розницы».
  Номер строки разъезжается на несколько единиц, и ссылка ведёт не туда.
  Нумерация считается по `\n` после нормализации переводов строк.

- **`tree-sitter-bsl` 0.1.6 не разбирает базовые конструкции BSL.** Замер на
  «Рознице»: 31 261 узел-ошибка в 2 596 файлах из 7 878 — треть корпуса.
  Ошибку дают `А = (Б + 1) * 2` (скобки в выражении), `Если (А) Тогда`,
  `Пока … КонецЦикла;` (у `Для` — нормально) и `Новый("Массив")`. Гипотеза
  «взять готовую грамматику вместо своего разбора» на этой версии
  **не подтвердилась**; собственный лексер совпал с ней на 5 279 файлах из
  5 282, где она не ошибается, — 99,94%. Версия 0.1.7 (GitHub, май 2026) не
  проверялась: на PyPI её нет, сборка требует Node.

- **Эталонная грамматика BSL публична, но под GPL-3.0** — `.g4` у
  `1c-syntax/bsl-parser`. В Apache-2.0 репозиторий её производные брать нельзя.
  MIT-вариант — `grammar.js` у `alkoleft/tree-sitter-bsl`, годится как
  справочник.

- **Тела процедур короткие.** «Розница», 137 115 процедур с границами: медиана
  **12 строк**, 75-й процентиль 25, 90-й — 51, 99-й — 185, максимум 1 458.
  Длиннее 200 строк — 1 180 штук, 0,9%. Отсюда предел вывода тела.

- **`NamePrefix` не различает конфигурацию и расширение — поправка к разведке.**
  Разведка (раздел 5) перечисляет его признаком расширения: «нет у
  конфигурации, есть у расширения». Проверено 2026-08-19 на живой паре
  «типовая конфигурация розничных продаж 2.3.10.5 + расширение к ней» —
  тег есть **у обоих**:

      конфигурация:  <NamePrefix/>                 ← пустой
      расширение:    <NamePrefix>ПРЕФИКС_</NamePrefix>   ← непустое значение

  Различает не наличие тега, а непустое значение. Проверка по наличию —
  а именно так правило и записано — классифицировала бы конфигурацию как
  расширение.

  Ложных признаков, таким образом, два. Про
  `ConfigurationExtensionCompatibilityMode` разведка знала и предупреждала;
  про `NamePrefix` — нет. Надёжны только три: `ObjectBelonging` и
  `ConfigurationExtensionPurpose` есть лишь у расширения, `CompatibilityMode`
  лишь у конфигурации.

- **Отбираемое из выгрузки в файлы — 351,4 МБ, а не 260.** Замерено
  2026-08-19 на обезличенной выгрузке: развёрнутая
  2 063 МБ, отбираем 11 072 файла на 351,4 МБ — 17,0% развёрнутого. Оценка
  «~315 МБ» из первой редакции постановки не выводилась из разведки вовсе;
  замер дал больше, потому что `Form.xml` крупнее среднего XML метаданных:
  `.bsl` дают 260,3 МБ, формы — 91,1 МБ.

  Воспроизводится: `python3 tools/lab/measure_intake.py <выгрузка.zip>`.

  Выгрузка расширения на этом фоне бесплатна: архив 1 МБ, развёрнутая 8 МБ,
  отбираем 6,9 МБ из 155 файлов.

- **План утверждал, что источник модулей переживёт перезапуск сам — не
  переживал.** `restore()` знал только два ветвления: конфигурация — своим
  путём, всё остальное — путём `add_syntax`. Источник модулей уходил во
  второе: `add_syntax` падал `IsADirectoryError` на каталоге
  `data/modules/<Имя>` (это каталог, а не файл справки), общий `except`
  ошибку глотал, и запись молча выпадала из `self.sources`. Код при этом
  оставался на диске — рестарт контейнера превращал загруженный источник в
  ничей. Понадобилась отдельная ветка восстановления: код уже на месте,
  перечитывать архив (который человек мог и убрать) незачем — запись просто
  переносится из `registry.json` обратно в память.

- **Признак «выгрузка в файлы» по `.bsl` и `.Form` был неполон.** У плоской
  выгрузки (8.3.5) модули лежат не в `.bsl`, а в `.txt`, а контейнеров `.Form`
  в ней тоже нет — код форм лежит внутри них самих. Конфигурация без единой
  пользовательской формы под старым признаком не опознавалась вовсе:
  `bootstrap()` принимал её архив за обычную выгрузку метаданных и пытался
  прочитать `Configuration.xml` как schema v1. Манифеста (`manifest.json` или
  `manifest.xml`) в такой выгрузке нет, поэтому загрузчик отказывал одной и
  той же ошибкой «нет ни manifest.xml, ни manifest.json — это не выгрузка
  структуры конфигурации» на каждом старте (проверено прогоном `loader.load()`
  на синтетическом архиве с `.txt`-модулями и без манифеста — том же, что
  использует `test_плоская_выгрузка_в_файлы_узнаётся`). Признак распознавания
  дополнен `.txt`; заодно проверка манифеста стала смотреть на имя
  `manifest.xml` наравне с `manifest.json` — схема v1 умеет отдавать оба.

- **Нормировка длины в ранжировании — гипотеза опровергнута замером.**
  Предварительный диагноз считал, что длинное описание
  выигрывает объёмом, лечится нормировкой по длине поля (BM25) или насыщением
  повторов. Спайк 2026-08-19 прогнал оба варианта на живых данных (пять
  конфигураций, три справки) — **оба в минус**, брать нечего.

  | Режим | язык запросов P@1 | MRR | чужой домен 1-м | отрыв |
  |---|---|---|---|---|
  | как сейчас | **85,7%** (18/21) | 0,869 | 1 | 35,0% |
  | насыщение повторов | 81,0% (17/21) | 0,833 | 2 | 45,1% |
  | нормировка по длине | **57,1%** (12/21) | 0,678 | **6** | 43,5% |
  | оба сразу | 66,7% (14/21) | 0,732 | **6** | 42,7% |

  Автоматические наборы не шелохнулись ни в одном режиме: 97,1% и 98,7%
  (61 470 запросов). Ломается ровно то, ради чего затевалось.

  **Причина провала: длина коррелирует с доменом.** Замерено на том же
  индексе — описание у языка запросов имеет медиану 59 токенов, у справки
  платформы 9 (максимумы 633 и 1668). Нормировка по длине систематически
  топит весь домен языка запросов в пользу коротких платформенных свойств,
  отсюда «чужой домен первым» 1 → 6.

  **Насыщение магнит сбивает, но не лечит.** На формулировке «как задать
  параметр в запросе» цель поднимается с 4-го места на 2-е,
  `SelectionFieldsList` теряет первое — но первым встаёт другой неверный
  ответ (`LitDate`). Обмен одного неверного ответа на другой ценой одного
  правильного в наборе.

  Перебора параметров (`k1`, `b`) не делали: мерили значения по умолчанию
  1,2 и 0,75. Вывод поэтому — «в таком виде не работает», а не «бесполезно
  в принципе».

- **`EXACT_BOOST` — множитель, а не прибавка, и на нуле даёт ноль.** Найдено
  при разборе живого промаха «стрнайти в запросе». Одни и те же токены дают
  счёт, различающийся ровно в 12 раз:

      «стр найти»    -> ['стр','найти']   2484,9
      «СтрНайти»     -> ['стр','найти']   2484,9
      «стр+найти»    -> ['стр','найти']    207,1     (207,1 x 12 = 2485)

  Точное совпадение целой строки ищется по `normalize(text).replace(" ", "")`,
  поэтому пробел и CamelCase дают ключ `стрнайти` и множитель, а плюс — нет.
  Множитель применяется к базе, набранной токенами; если токенов не совпало,
  умножать нечего.

  Отсюда единственная форма, которая ломается: **слитное строчное имя плюс
  уточнение**. `СтрНайти` индексируется как `стр` + `найти`
  (`split_identifier` режет по регистру), строчное «стрнайти» резать не по
  чему, целой строкой «стрнайти в запросе» ключ тоже не является — база
  нулевая, и выдачу забирают длинные статьи. Обе `СтрНайти` выпадают из
  двадцатки.

  Проверено, что остальные формы работают и **выбор между двумя `СтрНайти`
  ранжирование делает верно**: на «СтрНайти в запросе» (272,0 против 143,9) и
  на «как найти подстроку в запросе» (177,0 против 54,3) функция языка
  запросов идёт первой, метод платформы ниже.

- **Набор языка запросов не измеряет магнит.** В
  `tests/queries/query-language.json` лежит рабочая формулировка «как задать
  параметр **в тексте** запроса» (первое место, 134,6), а ломающаяся «как
  задать параметр в запросе» отсутствует. То есть правку магнита нечем было
  принимать до того, как её начали делать.

- **Эталонный прогон стенда, на который ссылалась доска, не существует.**
  Доска обещала: «прогон сохранён в `data/bench/2026-08-18.json` — следующий
  сравнивать с ним». Каталога `data/bench/` не было вовсе; создан 2026-08-19.

  Из-за этого расхождение с записанными цифрами **недоказуемо в обе стороны**.
  Так и вышло: набор по метаданным дал 81,0% первым местом и MRR 0,862 против
  записанных 90,5% и 0,934. Регрессия это или другое состояние кода —
  установить нечем: история до 2026-08-19 сведена в один коммит `git
  filter-repo`, набор запросов с 18 августа не менялся, версия и размер
  конфигурации те же.

  Правило «цифра без команды устаревает молча» знает про команду, но не про
  прогон: команда записана, а сравнивать не с чем. Эталоном назначен
  `data/bench/2026-08-19-base.json`.

- **Служебные виды метаданных перехватывают запрос — не новость, а известный
  дефект.** Разбор четырёх запросов набора, не занявших первое место, показал
  один почерк в трёх из них: первым идёт `ОпределяемыйТип` или
  `Перечисление`, цель — вторая.

      виды оплат             Перечисление.ВидыОплатСерверЛояльности   117,1
                             Справочник.ВидыОплатЧекаККМ              105,4  <- цель
      подарочный сертификат  ОпределяемыйТип.Подарочный...            113,5
                             РегистрНакопления.ПодарочныеСертификаты   82,4  <- цель
      вид оплаты             ОпределяемыйТип.ВидОплатыСерверЛояльности 714,4

  Причина записана в самом коде, в комментарии к `EXACT_BOOST`:
  `ОпределяемыйТип` именуется в единственном числе, справочники во
  множественном, и запрос в единственном достаётся ему. Заход по этому
  дефекту уже был и провалился — ослабление множителя вышло в минус.

  «вид оплаты» — тот же множитель ×12, что разобран выше на `СтрНайти`:
  714,4 против 76,4 у следующего.

- **Сервер хода передачи файла не видит и видеть не может.** `await
  request.form()` у starlette — это `MultiPartParser.parse()`, который
  вычитывает тело **целиком**; управление возвращается, когда файл уже на
  сервере, и только после этого вызывается `_start_job`. Значит «принимается»
  и растущие мегабайты в журнале — не приём по сети, а копирование из буфера
  на диск, то есть секунда на 500 МБ.

  Проверено на живом сокете, а не по чтению исходников: дашборд под uvicorn,
  40 МБ отправлены медленно (как по узкому каналу), параллельно со **второго
  соединения** опрашивался `/sources`.

      передача заняла            : 8.1 с
      ответ пришёл через         : 8.2 с (HTTP/1.1 303 See Other)
      опросов /sources за это    : 32
      из них видели «принимается»: 0

  Отсюда следствие: совет «откройте вторую вкладку» не работает — показывать
  там нечего. Прогресс передачи может дать только браузер.

- **Выключенное поле формы не попадает в `FormData`.** Обработчик выключал
  `input[type=file]` (чтобы не жали дважды) **до** сборки `new FormData(форма)`
  — и на сервер уходил запрос без файла. Сервер отвечал «файл не выбран»
  страницей, задание не заводилось вовсе, а браузер бодро показывал «100%
  передано» и уходил на `/sources`.

  Тестами это не ловится: pytest видит разметку, но не выполняет обработчик.
  Нашлось только в живом браузере — перехватом `XMLHttpRequest.send`, где
  `FormData.get("file")` оказался `null`. Данные теперь собираются до
  выключения поля.

- **Выгрузка конфигурации в файлы не содержит версии платформы — только
  режим совместимости, и это не одно и то же.** Владелец завёл источник
  модулей и увидел в таблице «Загружено» прочерк вместо платформы; искать в
  `Configuration.xml` оказалось нечего — точной сборки там нет вовсе. Есть
  `CompatibilityMode`, и число другое: на конфигурации розничных продаж
  2.3.10.5 в выгрузке в файлы стоит `CompatibilityMode = Version8_3_21`,
  тогда как фактическая платформа по выгрузке метаданных той же конфигурации
  — **8.3.23.1997**. Рядом в том же `Configuration.xml` лежит и
  `ConfigurationExtensionCompatibilityMode = Version8_3_23` — третье число,
  снова не платформа.

  Путать `CompatibilityMode` с платформой нельзя: `get_syntax` фильтрует
  выдачу справки по версии платформы, и подмена спрятала бы методы, которые
  на самом деле доступны (или наоборот, показала бы недоступные). Поэтому
  источник модулей платформу у собственной выгрузки не берёт: привязка
  модулей уже даёт знать, к какой конфигурации относится код, а та
  знает свою точную сборку: платформа наследуется от неё (`Registry.add_modules`).
  `CompatibilityMode` при этом не бесполезен — это отдельная величина,
  «чем разрешено пользоваться в этом коде», а не «что вообще существует», и
  пригодится провайдеру `modules`, когда тот появится.

  Три числа сняты чтением тегов `Version`, `CompatibilityMode`,
  `ConfigurationExtensionCompatibilityMode` из `Configuration.xml` внутри
  архива выгрузки в файлы — корпус типовая конфигурация розничных продаж
  2.3.10.5, прогон 2026-08-19, воспроизводится однострочником:

      python3 -c "import zipfile,re; xml=zipfile.ZipFile('<выгрузка.zip>').read('Configuration.xml').decode('utf-8-sig'); [print(t, re.search(fr'<{t}>(.*?)</{t}>', xml).group(1)) for t in ('Version','CompatibilityMode','ConfigurationExtensionCompatibilityMode')]"

## [0.2.0] — 2026-08-19

Язык запросов отдельным источником, дашборд, авторизация, слияние справок
нескольких версий, поисковые ключи, стенд замеров, кэш индексов. Репозиторий
стал публичным: лицензия, границы «что наружу», CI.

> **Сессия 2026-08-16** — 25 коммитов, тестов 69 → 155. Приём справки перестал
> принимать что попало и научился читать старые платформы; поиск получил
> Snowball и псевдонимы независимо от формы слова; дашборд — правку словаря,
> фоновую загрузку и разбор markdown; появился `API_TOKEN` на чтение. Два
> захода закончились отрицательным результатом и записаны как есть: ослабление
> множителя точного совпадения и работа с памятью справки. Отдельной веткой —
> слитый индекс справок нескольких версий, 9 коммитов, тестов 155 → 212.

> **Сессия 2026-08-17 (ранжирование языка запросов)** — ветка
> `feat/query-ranking`, 3 коммита, тестов 284 → 295. Живой промах: на вопрос
> «чем отличается объединить от объединить все» статья языка запросов была
> второй, впереди — платформенное свойство с похожим именем. Три меры,
> каждая с числом: индекс, подъём по обороту, видимость в дашборде.

> **Сессия 2026-08-18** — 17 коммитов, пять веток, тестов 317 → 376. Закрыты
> четыре пункта очереди: поисковые ключи языка запросов (57,9% → 94,7% первым
> местом), описания инструментов с порядком вызовов, мелочи после ранжирования,
> стенд замеров. README приведён в соответствие и получил правило обновления.
> Снят раздел «в радиусе N» у `get_related` — он врал и стоил 3 000 токенов на
> вызов. Инструмент путей по графу для агента разобран и **отложен**, вместо
> него сделан граф на дашборде. Три дефекта найдены не тестами: `.pyc` подменял
> измеряемый код, фикстура теряла типы реквизитов, страница связей была тупиком
> по ссылке из меню — последний нашёл владелец на живом дашборде.

> **Сессия 2026-08-17 (поисковые ключи языка запросов)** — ветка
> `feat/query-search-keys`, тестов 317 → 328. Восемь промахов из 19 были
> лексическим разрывом в чистом виде: «уникальные записи без повторов» против
> `РАЗЛИЧНЫЕ`, ноль общих слов с целью. Лечится слоем сочинённых формулировок
> поверх справки — метод doc2query, без модели и без новых зависимостей.

> **Сессия 2026-08-19 (публикация репозитория)** — репозиторий стал публичным,
> и это потребовало ровно того, чего приватный не требовал: лицензии, границы
> «что наружу», обезличивания и машинной проверки правил. README ссылался на
> 14 файлов, которых в репозитории нет, — все ссылки вели в 404 с первой
> строки. Тестов 371 → 432 (цифра в README разошлась с фактом и была
> исправлена по тому же правилу, которое требует держать её актуальной).

### Добавлено

- **Лицензия Apache-2.0 и `NOTICE`.** Без файла лицензии публичный репозиторий
  юридически закрыт: «No license» означает «все права защищены», форкать нельзя
  никому. Выбран Apache-2.0, а не MIT, по двум причинам, обе про этот проект:
  раздел 6 прямо оговаривает, что прав на **товарные знаки** лицензия не даёт
  (репозиторий насквозь про «1С»), и раздел 3 даёт явную **патентную**
  лицензию — существенно, потому что разобраны закрытые форматы (контейнер v8,
  `.hbk`, схема выгрузки).

  `NOTICE` говорит то, чего лицензия не говорит: проект не аффилирован с
  ООО «1С»; справки и выгрузок в репозитории нет и быть не может; лицензия
  покрывает код, а не данные, которые пользователь кладёт в `data/`.

- **`SECURITY.md`.** Отдельно перечислено, что уязвимостью **не** является:
  открытое чтение без `API_TOKEN`, 404 на запись без `ADMIN_TOKEN`, проход
  `/health` и `/login` мимо проверки, неработающий кириллический токен в
  заголовке. Всё это поведение по умолчанию, описанное в README, и без такого
  списка каждый пункт прилетел бы отчётом.

- **`CONTRIBUTING.md`.** Публичные правила вклада: язык, граница
  `data/`, требование документировать функциональность в README,
  обоснование зависимостей цифрами и чек-лист перед PR.

- **`pyproject.toml`.** Сервер ставится пакетом (`pip install .`, `uvx`), а не
  только запускается из копии репозитория через `PYTHONPATH=src`. Появились три
  точки входа: `mcp1c`, `mcp1c-server`, `mcp1c-bench` — те же модули под другим
  именем, модульный запуск остался рабочим. Колесо проверено: 26 модулей из 26,
  данных внутрь не попало.

- **CI на GitHub Actions — три job'а.** `pytest` на матрице 3.12 и 3.13
  (432 теста зелёных на обеих), сборка пакета с `twine check`, и проверка
  обработки выгрузки: `build_modules.py` пересобирает `dist/` и
  `git diff --exit-code` падает, если результат разошёлся. Последнее — машинная
  проверка правила «поменял `src/` — пересобери `dist/` тем же коммитом»,
  которое до сих пор держалось на памяти. Тесты не зависят от `data/`, поэтому
  прогон идёт на голом раннере, без секретов и внешних данных.

- **Версии функций языка запросов — курируемая таблица.** До неё сервер
  показывал конфигурации на 8.3.5 функции, которых в её платформе нет:
  `СтрНайти`, `Лев`, `Прав`, `ВРег`, `НРег`, `СтрЗаменить`, `Окр`, `Цел`,
  тригонометрия целиком. Та же ошибка, ради которой затевалось слияние справок
  платформы, только в другом источнике.

  Взять версию из данных нельзя, и это проверено на файлах:

  | Где искали | Что нашли |
  |---|---|
  | `shquery_ru.hbk`, 129 страниц | **ноль** упоминаний «8.3.x», «начиная с версии», «доступно с версии». Три упоминания слова «версия» — про режим совместимости 8.1 |
  | `shcntx_ru.hbk` 8.3.27, 25 511 элементов | функций языка запросов **нет вовсе**: `ПОДСТРОКА` — ноль совпадений, `СтрНайти` — четыре, и все методы глобального контекста |

  Это две разные книги: синтакс-помощник (39 МБ) и справка конструктора
  запроса (170 КБ), и они не пересекаются. Единственное про запросы в
  синтакс-помощнике — виртуальные таблицы, потому что они привязаны к объектам
  метаданных.

  Поэтому таблица курируемая, как таблица замен и таблица переименований
  владельцев. Источник — список 1С «Функции, добавленные в язык запросов
  начиная с релиза 8.3.20»; сверен с содержимым справки: **все 25 имён
  нашлись**, 25 страниц из 127 получили `since=8.3.20`. Остальным 27 функциям
  версия не приписывается намеренно: `ПОДСТРОКА`, `ГОД`, `СУММА` и прочие были
  всегда, и пометить их значило бы спрятать от старых конфигураций — соврать в
  другую сторону.

  Ключ таблицы — английское имя страницы: оно однозначно (имя файла внутри
  контейнера), а русское у части функций записано вместе с английским.

  Пополняется по мере подтверждённых сведений. Сейчас в таблице четыре релиза:

  | Релиз | Что добавилось |
  |---|---|
  | 8.3.13 | `АВТОНОМЕРЗАПИСИ` |
  | 8.3.16 | `СГРУППИРОВАНОПО` |
  | 8.3.20 | 25 штук: строковые, математические, округление, тригонометрия, `РазмерХранимыхДанных` |
  | 8.3.22 | `УНИКАЛЬНЫЙИДЕНТИФИКАТОР` |
  | 8.3.25 | `ДОБАВИТЬ <Имя временной таблицы>` — конструкция, а не функция |

  Каждая запись — из списка «что нового» своего релиза, а не из догадки по виду
  элемента: ошибка спрячет рабочую функцию от конфигурации, где она есть, и это
  хуже, чем показать лишнее.

  **Проверена данными в тот же день.** Владелец принёс `shquery_ru.hbk` от
  8.3.5.1570 — 98 страниц против 127 у текущей. Сравнение даёт точный список
  того, что появилось после 8.3.5, и по нему таблица проверяется целиком:

      появилось после 8.3.5 : 29
      покрыто таблицей      : 29
      ложных срабатываний   : 0
      исчезло               : 0

  Ни одному элементу, существовавшему в 8.3.5, версия не приписана — то есть
  спрятать рабочую функцию мы нигде не могли. Способ перепроверки записан в
  модуле: справка другой версии — единственное, что покажет пропуск. Сама она
  релиза не назовёт, только вилку, поэтому номера остаются за списками
  «что нового».

  **Рецепты замены в язык запросов не переезжают.** Таблица замен написана про
  платформу: вместо `СтрНайти` — `Найти`. У функции запроса имя то же, а
  `Найти` — метод глобального контекста, в тексте запроса его не написать.
  Подставить этот рецепт значило бы выдать невыполнимый совет за проверенный.

- **Строки неограниченной длины помечены и снабжены оговоркой.** Живой промах
  2026-08-18, первый пойманный на реальной работе агента через наш MCP. Агент
  сгруппировал запрос по реквизиту-строке без ограничения длины — платформа
  такой запрос не выполняет. Отдали мы верное, но разница читалась только по
  отсутствию числа в скобках, а чем это грозит, не говорилось нигде.

  Теперь тип печатается как `Строка (неогр.)` — с пробелом перед скобкой,
  чтобы пометка не читалась как длина. На карточках, где такие поля есть,
  один раз печатается оговорка с рецептом `ПОДСТРОКА(поле, 1, N)`.

  Цена измерена на всех пяти конфигурациях: оговорка попала на **2 474 карточки
  из 20 522** (12,1%), медиана роста 501 символ, корпус карточек +10,0%.
  Ранжирование не затронуто — типы полей в поисковый индекс не идут, все четыре
  набора без изменений.

  **Первая редакция не сработала, и это выяснилось на живом агенте.** Оговорка
  стояла предпоследним абзацем карточки. Агент вызвал `get_object` с
  `detail=fields`, получил её целиком и всё равно сгруппировал по такому полю:
  она приходила на **721 токен позже строки поля**, а решение принимается там,
  где имя копируют. Исправлено переносом — рецепт встал в сам тип
  (`Строка (неогр. — только через ПОДСТРОКА)`), а оговорка поднялась выше
  списков полей. Корпус карточек при этом не вырос, а стал легче: +9,51%
  против +10,01% у первой редакции, потому что оговорка ужалась до фактов.

  **Вторая редакция проверена на живом агенте — сработала, один прогон.**
  Агент поставил `ПОДСТРОКА(…, 1, 100)` и в выборку, и в `СГРУППИРОВАТЬ ПО`,
  одинаково. Сверх ожидаемого — применил её и внутри `В (&СписокКодов)`, то
  есть вывел из слова «сравнивать» правило, а не скопировал шаблон. Один
  прогон подтверждением не считается, тестирование продолжается.

  **Доуточнено в тот же день прогонами на живой базе.** Все запреты
  подтверждены, осторожных формулировок в оговорке не осталось, и она стала
  короче на 14%. Тексты ошибок платформы записаны в `docs/data-sources.md`.

  Перевыгрузка конфигураций не потребовалась: признак уже был в данных.
  `exporter-1c/src/core.bsl` пишет `string_length` только при длине больше нуля,
  поэтому отсутствие ключа у строкового поля и означает «неограниченная» —
  `КвалификаторыСтроки.Длина` там ноль, а сами квалификаторы платформа отдаёт
  всегда. Ветка `string_length == 0` в `type_spec` была мёртвой: выгрузка ноль
  никогда не присылала.

- **Собранные обработки выгрузки лежат в репозитории обе.** Обработка под
  обычную форму (XML, 8.3.5 и выше) не отслеживалась git вовсе и валялась под
  именем `ВыгрузкаСтруктурыКонфигурацииОФ (2).epf`. Обе переименованы по виду
  формы и формату, как одноимённые модули в `dist/`:
  `ВыгрузкаСтруктурыКонфигурации_ОбычнаяФорма_XML.epf` и
  `ВыгрузкаСтруктурыКонфигурации_УправляемаяФорма_XML_JSON.epf`.

  Содержимое сверено по составу процедур, а не по имени файла (`v8container`
  читает `.epf` тем же кодом, что `.hbk`): управляемая совпадает с
  `УправляемаяФорма_XML_JSON.bsl` полностью (42 из 42), обычная — с
  `ОбычнаяФорма_XML.bsl` плюс обработчик формы `ПриОткрытии`, обращений к
  `ЗаписьJSON`/`ЗаписатьJSON` в ней нет ни одного.

### Изменено

- **Имя конфигурации в публичных документах обезличено.** Репозиторий стал
  публичным вместе с названием конкретного внедрения — 31 упоминание в README
  (5), CHANGELOG (15), тестах (9), `docs/data-sources.md` и скрипте замеров,
  включая аббревиатуру и имя отраслевого общего модуля. Заменено
  на «Отраслевая конфигурация»; типовые продукты фирмы «1С»
  («Бухгалтерия для Казахстана», «Розница для Казахстана», «Документооборот
  КОРП», «Зарплата и управление персоналом») оставлены как есть — они никого не
  выдают.

  Информативно не название, а **версия платформы**: от неё зависит, что
  `get_syntax` покажет доступным. README получил абзац об этом: ограничений на
  конфигурацию нет, проверено на 8.3.5 и 8.3.23, справки слиты из трёх версий.

- **Документация разделена на публичную и локальную часть.** README
  ссылался на отсутствующие в репозитории рабочие файлы — 14 ссылок
  вели в 404, включая первую строку README и целиком таблицу
  «Документы». В репозиторий
  вошли четыре документа-контракта (`schema-v1.md`, `data-sources.md`,
  `query-language-design.md`, `dashboard-design.md`). Все публичные ссылки
  теперь ведут только на файлы, которые входят в репозиторий.

  Каталог гасится по `/docs/*`, а не `/docs/`: git не может вернуть файл, если
  исключён сам каталог, и строки-исключения просто не сработали бы.

- **`.gitignore` покрывает всю локальную обвязку разработки.** Локальные инструкции,
  каталоги рантаймов, рабочие деревья git и настройки IDE не попадают в публичный
  репозиторий.
  Каталог `.idea/` до этого лежал неотслеженным и просился в коммит.

### Исправлено

- **Тесты дашборда падали на CI и проходили локально.** Первый же прогон на
  ubuntu-latest дал `4 failed, 428 passed` на py3.12 и `3 failed, 429 passed`
  на py3.13 — при 432 зелёных на macOS. Наборы упавших тестов **разные между
  прогонами**, прогон 23 с против 1,9 с локально. Дефект был всё это время,
  показать его было нечему.

  Две причины, обе в тестах, не в дашборде.

  **Ожидание ждало не того.** `дождаться_разбора(client, "Плохая.zip")` ждал
  появления имени файла — а `_start_job` кладёт его в журнал **синхронно, до
  редиректа**. Условие выполнялось на первом же опросе, пока задание было в
  состоянии «принимается», и следующая строка проверяла «ошибка» у ещё не
  упавшего задания. Запас времени — нулевой, вопрос только в том, кто успеет
  первым. Теперь ожидание принимает несколько признаков и ждёт терминального
  состояния; после этой правки оба теста **упали локально** — гонка стала
  честной.

  **`TestClient` без `with` поднимал портал на каждый запрос.** У
  `starlette` 1.6.0 `_portal_factory` при `self.portal is None` создаёт
  `start_blocking_portal()` и закрывает его сразу после ответа — то есть поток
  с event loop на каждый HTTP-вызов. На 432 тестах это сотни созданий и
  уничтожений потоков; на двухъядерном раннере пятисекундного ожидания
  переставало хватать. Клиенты создаются через `живой_клиент()` из
  `conftest.py`, портал живёт весь тест и закрывается autouse-фикстурой.

  Заодно проверено и **опровергнуто**: фоновая задача действительно
  отменяется вместе с умирающим loop'ом, но работу это не срывает — `_run_job`
  выполняется в сыром потоке через `run_in_threadpool` и к отмене asyncio
  нечувствителен. Замерено на замедленном разборе: результат доходит до
  реестра в обоих случаях, 3,1 с против 3,0 с. Отмена реальна и безвредна.

  Ожидания подняты с 5 до 20 с и при истечении **падают с причиной**, а не
  возвращают последнюю страницу молча: раньше «ожидание истекло» выглядело
  как «в реестре нет конфигурации».

- **Фоновые задачи дашборда держались только слабой ссылкой.**
  `asyncio.create_task()` не удерживает задачу — сборщик мусора вправе забрать
  её в любой момент до завершения, и снаружи это выглядит как «разбор молча не
  случился». Ссылки складываются в `_ФОНОВЫЕ`, `add_done_callback` их убирает.
  На нынешних падениях это не сказывалось, но условие возникновения ничем не
  ограничено.

- **Отсечённый фильтром версии одноимённый пропадал бесследно.** Фильтр молчит,
  и это правильно, пока он убирает всё: тогда срабатывает ветка «недоступен в
  этой версии» с причиной и заменой. Но если рядом остался одноимённый —
  например, функция языка запросов, у которой версий не бывает, — отсечённое
  исчезало без следа.

  Живой промах: на отраслевой (8.3.5) запрос `СтрНайти` отдавал карточку
  функции языка запросов и **ни слова** о том, что платформенная `СтрНайти`
  появилась только в 8.3.6 и заменяется на `Найти`. Агент пишет её в модуль —
  код не компилируется. По полному адресу
  (`Глобальный контекст.СтрНайти`) ответ при этом был правильным: расходились
  два пути к одному элементу.

  Замер по живому реестру, сколько имён отвечали так же:

  | Конфигурация | Карточка вместо предупреждения | Отсеянное выпало из списка одноимённых |
  |---|---|---|
  | Отраслевая (8.3.5) | **419** | 697 |
  | Розница (8.3.23) | 124 | 283 |
  | Бухгалтерия (8.3.27) | 101 | 92 |

  Теперь отсечённые называются в обеих ветках: при одной карточке — с причиной
  и рецептом замены, в списке одноимённых — короткой сводкой с отсылкой к
  полному адресу (у имени вроде «Количество» отсечённых бывает полсотни).

  Дефект вскрылся вчерашней расклейкой имён языка запросов: пока функция
  называлась `СтрНайти(StrFind)`, столкновения не было. Правка его не создала,
  а привела к нему кратчайший путь.

- **Элемент языка запросов нельзя было назвать по имени, когда одноимённый
  есть и в платформе.** `get_syntax("СтрНайти")` отвечал «Одноимённых
  элементов: 2» и советовал «уточните запрос в виде `Объект.Член`». Для
  платформенного это работает (`Глобальный контекст.СтрНайти`), а у элемента
  языка запросов владельца нет вовсе: `Функция запроса.СтрНайти` не находится,
  `СтрНайти` возвращает тот же список. Тупик на полном наборе источников,
  найден живой проверкой.

  Появился квалификатор **`Запрос.<Имя>`** (принимается также `ЯзыкЗапросов.`),
  и список одноимённых печатает готовый адрес каждого — повторить вызов можно
  прямо строкой из выдачи.

- **`/health` выдавал язык запросов за справку платформы.** При загруженном
  только `shquery_ru.hbk` ответ гласил `"syntax_loaded": true, "syntax": ""` —
  «справка есть, но сломана» вместо «справки платформы нет, есть язык
  запросов». Тело ответа переехало в `tools.health`: `syntax_loaded` относится
  только к платформе, добавлен флаг `query_language_loaded`, `syntax` стал
  списком версий. `/admin/reload` считает состав тем же вызовом — иначе два
  ответа про одно и то же расходятся.

- **Английское имя оставалось внутри русского у восьми функций языка
  запросов.** Страница озаглавлена «Функция СтрНайти(StrFind)», и всё это
  попадало в `name_ru`, хотя английское имя и так лежит в `name_en` (берётся
  из имени файла). Точное совпадение из-за этого промахивалось:
  `get_syntax("СтрНайти")` отвечал «Точного совпадения нет» и предлагал в
  подсказках сам `СтрНайти(StrFind)`.

  На реальном `shquery_ru.hbk` так названы 8 функций из 127, все ходовые:
  `Лев`, `Прав`, `ВРег`, `НРег`, `СтрНайти`, `СтрЗаменить`,
  `РазмерХранимыхДанных`, `УНИКАЛЬНЫЙИДЕНТИФИКАТОР`. Скобка снимается только
  когда внутри действительно английское имя этой же страницы — содержательные
  скобки в заголовках остаются.

- **«Ответы собраны из соседних версий» — при нуле загруженных справок.**
  Раздел «Справки платформы» в `list_configurations` обещал приблизительный
  ответ там, где ответа нет вовсе: соседних версий не существует, если не
  загружено ни одной. Теперь при пустом наборе говорится прямо — методы и
  свойства платформы недоступны, загрузите `shcntx_ru.hbk` этой версии.

- **Выдача поиска называлась справкой платформы, когда платформы нет.**
  Заголовок `# Справка платформы: «…»` печатался и при одном загруженном
  языке запросов, тут же противореча собственному подзаголовку «Только язык
  запросов». Отказ «в справке платформы ничего не найдено» — то же самое.
  Найдено живой проверкой на чистом сервере.

- **Пятое место: сервер называл справку, по которой не отвечал.** Найдено
  2026-08-18 при живом вызове `list_configurations`. Конфигурация «Отраслевая»
  на платформе 8.3.5.1570 получала строку «справка **8.3.27** — версия
  совпадает с конфигурацией»: фраза противоречит сама себе, и по ней читающий
  делает вывод, что фильтр по версии не работает. То же у «Розницы» (8.3.23).

  Соотношение считалось верно — `_compute_relation` видит через `has_help_for`,
  что справка нужного релиза в слитом индексе есть, и ставит `exact`. Неверен
  был **номер**: он брался у объединённого источника
  (`syntax.source.platform`), а тот всегда называется самой свежей из
  загруженных справок. Пока справка была одна, это совпадало; со слитым видом
  разошлось и осталось незамеченным, потому что на 8.3.27 строка выглядит
  правильной.

  Цена дефекта в том, что он бьёт по единственной возможности, которой нет ни
  у кого на рынке: слитый индекс справок как раз и нужен, чтобы отвечать по
  версии конфигурации, а строка сообщала обратное.

  Появился `SyntaxIndex.help_for(platform)` — «какая из загруженных справок
  отвечает за эту версию», и `has_help_for` стал его частным случаем.
  `ResolvedContext.syntax_platform` называет её вместо источника; `reg-list` и
  `list_configurations` читают одно и то же поле и починены оба.

  Тем же именем подписывался отказ `get_syntax` («в справке платформы 8.3.27
  нет элемента X, доступного в версии 8.3.5») — исправлено там же. Ещё два
  места того же вида оставлены как есть намеренно: они печатаются только
  когда не загружено ни одной конфигурации, а на живом сервере их пять.

  Проверено на реальном реестре из пяти конфигураций и трёх справок:
  «Отраслевая» — справка 8.3.5.1570, «Розница» — 8.3.23.1997, три остальные —
  8.3.27. Тестов 413 → 415.

- **Четыре места, где сервер врал человеку.** Ни одно не в пути агента:
  платит за такое человек у первого запуска или у отладочного CLI, и молча.

  `reg-list` при загруженных только справках печатал «Ничего не загружено» и
  возвращал **код 1** — то есть скрипт вокруг счёл бы рабочий сервер
  отказавшим. `reg-list` при конфигурации без справки платформы, но с языком
  запросов печатал `синтаксис : справка , не подключён` — пустое имя платформы
  рядом со словом «справка».

  Оба — **третий и четвёртый случай одного баг-паттерна**: `LoadedSyntax`
  держит справку платформы и язык запросов в одном объекте, поэтому
  `providers['syntax']` истинно и когда платформы нет вовсе. В `tools.py` это
  чинили дважды, в `cli.py` оба раза забывали.

  Пустой реестр называл только выгрузку структуры, хотя справок в нём тоже
  нет; человек у первого запуска узнавал об этом со второго захода. Теперь
  отказ перечисляет всё недостающее и говорит, чем каждое берётся.

  `render_markdown` не разбирал ограждённые блоки кода — сигнатуры и примеры
  печатались абзацами, теряя моноширинный вид и отступы, а карточка справки из
  них и состоит. Дефект ровесник самого `render_markdown`; всплыл на лесенке
  грамматики, когда стало видно, что отступы теряются. Внутри блока разметка
  не разбирается: решётка и дефис там часть кода. Незакрытый блок доходит до
  конца текста, но остаток страницы остаётся видимым внутри `<pre>`.

- **Таблицы страниц языка запросов печатались столбцом значений.** Карточка
  «Итоги по иерархии» выводила «Товар / Количество / Номер / Сантехника /
  104 / …» два десятка строк подряд. Причина в `query_parser.parse_page`:
  описание собиралось как все `<P>` страницы, а ячейки таблиц в этой справке
  размечены абзацами внутри `<TD>` — каждая ячейка становилась отдельным
  абзацем, структура строки и столбца терялась.

  Показ разведён с поиском: таблицы уходят в отдельное поле `SyntaxItem.tables`
  и печатаются markdown-таблицей **на своём месте в тексте** (метка в описании,
  подстановка при отрисовке) — на странице с двумя примерами каждый результат
  стоит под своим примером. В поисковый индекс содержимое таблиц не попадает.
  Разобрана 51 таблица на 31 странице из 127; описания стали на 10% короче
  (99 345 → 89 441 символ).

  Замер после правки, все четыре набора против прогона 2026-08-18: без
  изменений (94,7% / 90,5% / 97,1% / 98,8% первым местом), одно движение вверх
  — «в чем разница между внутренним и левым соединением» с 5-го места на 4-е.

- **`render_markdown` дашборда не разбирал таблицы, потому что их не бывало.**
  В докстроке так и было записано. С разделением показа и поиска они появились,
  и страница печатала бы разметку сырыми абзацами. Разбирается пара «строка с
  чертами плюс строка-разделитель»; одиночная черта таблицей не считается — в
  лесенке грамматики она стоит в каждой второй строке.

- **Предупреждения загрузки были видны только в строке вывода CLI.** Она
  прокручивается и теряется, а человек смотрит на «Источники» дашборда — там
  их не было. Теперь замечания источника печатаются отдельной строкой под ним.
  Заодно стало видно и существующее предупреждение о непривязанных поисковых
  ключах.

### Найдено

- **Опровергнуто: «язык запросов не меняется от версии к версии».** Так было
  записано в README, когда язык запросов заводился отдельным источником, и на
  этом основании фильтр по версии его не касался. Неверно: релиз 8.3.20 добавил
  в язык запросов **25 функций** — строковые (`СтрНайти`, `Лев`, `Прав`, `ВРег`,
  `НРег`, `СтрЗаменить`, `Строка`, `ДлинаСтроки`, `СокрЛ`/`СокрП`/`СокрЛП`),
  математические (`Exp`, `Log`, `Log10`, `Pow`, `Sqrt`), округление (`Окр`,
  `Цел`), тригонометрию целиком и `РазмерХранимыхДанных`.

  Верна оказалась только вторая половина утверждения: **версий нет в файле**,
  а не в языке. Найдено владельцем на живой работе: сервер предлагал `СтрНайти`
  конфигурации 8.3.5.

- **Форматов выгрузки конфигурации в файлы два, и они устроены по-разному.**
  Найдено на выгрузке отраслевой конфигурации 10.5.1.3 (8.3.5) рядом с уже
  разобранной «Розницей» 2.3.10.5 (8.3.23):

  | | Розница | Отраслевая |
  |---|---|---|
  | раскладка | иерархическая, папки | плоская, `Catalog.Имя.Что.txt` |
  | модули | `.bsl` | `.txt` плюс двоичные контейнеры |
  | формы | `Form.xml`, разбираемый | `.Form` — контейнер 1С |
  | процедур | 136 909 | 33 555 |

  **Модуль обычной формы лежит внутри контейнера `.Form` записью `module`** — и
  достаётся уже написанным `v8container.py`: прочитались все 1 332 формы, 1 080
  с кодом, 341 746 строк, 0,2 с. Структуры формы в этом формате нет: запись
  `form` двоичная, XML не пишется.

  **Часть общих модулей поставляется скомпилированной.** Внутри контейнера
  запись `image` с образом: кириллица есть (имена), объявлений процедур ноль.
  В отраслевой таких семь, крупнейший — 356 КБ образа. Отличить «нет исходника»
  от «не нашлось»
  можно только по этому признаку.

  Правило «конфигурация или расширение» на плоском формате не сломалось:
  `Configuration.xml` есть, признаков расширения нет, имя и версия совпали с
  загруженной в реестр.

- **Выгрузка конфигурации в файлы: кодом является 12,6% объёма.** Разведка перед
  провайдером `modules`, на «Рознице для Казахстана» 2.3.10.5 — той же версии,
  что загружена в реестр. Развёрнутая выгрузка 2 063 МБ и 33 188 файлов:
  713 МБ конфигурация поставки (`Ext/ParentConfigurations/*.cf`), 665 МБ
  двоичные макеты (драйверы ККТ и весов), 378 МБ XML метаданных и форм и
  **260 МБ собственно кода** — 7 878 модулей, 3 938 811 строк, 136 909 процедур
  и функций, из них экспортных 49 068.

  Попутно: `.cf` поставки приходит вместе с выгрузкой, и `v8container.py` такие
  контейнеры читать умеет — материал для сравнения «типовая против
  доработанной» просить отдельно не нужно.

- **Цену индекса модулей платит не хранение, а поиск.** Замеры на той же
  Рознице, `ps -o rss=` живого процесса:

  | Слой | На диске | В памяти |
  |---|---|---|
  | сигнатуры и адреса 136 909 процедур | 18,1 МБ | 65 МБ |
  | поиск по всем процедурам | — | **473 МБ** |
  | поиск только по экспортным (49 068) | — | 156 МБ |
  | формы: 3 194 файла, 69 769 элементов, 25 651 реквизит | 5,8 МБ | 44 МБ |

  Хранить оглавление — 65 МБ, искать по нему — в семь раз больше. Причина в
  устройстве `SearchIndex`: объект `Doc` на документ нормален для 20 тысяч
  карточек справки и становится главной статьёй на 136 909 процедурах. Отбор
  только экспортных срезает поиск на 67%. Латентность: медиана 1,2 мс на полном
  наборе, 0,4 мс на экспортных.

  **Опровергнутая гипотеза:** «провайдер `modules` слишком тяжёл для общего
  контейнера, ему нужен свой процесс». Отдельный контейнер память не уменьшает,
  а переносит — 265 МБ остаются 265 МБ. Рычаг другой: провайдер включается на
  конфигурацию, а не на сервер. Выгрузка в файлы есть по одной Рознице, значит
  цена сегодня — один раз, контейнер 628 → около 890 МБ.

- **Расширение конфигурации несёт свою дельту прямо в тексте.** Разобрано
  отраслевое расширение (назначение `AddOn`, совместимость 8.3.12, 92 модуля,
  35 611 строк, 629 процедур):

  - **186 перекрытий**: `&ИзменениеИКонтроль` 139, `&После` 29, `&Вместо` 16,
    `&Перед` 2. Из них **96 — в модулях форм**, то есть больше половины
    доработки лежит там, где у нас модели нет вовсе (формы отложены до
    `schema_version 2`).
  - **700 директив `#Вставка` / `#Удаление`** — что именно изменено в теле
    типовой процедуры, размечено самим расширением. Вычислять сравнением с
    поставкой не нужно.
  - **Имя метода не является ключом:** 186 перекрытий на 155 уникальных целей,
    двенадцать имён перекрыты в разных модулях. Ключ — пара «модуль + метод».
  - Все 1 088 отметок `<ObjectBelonging>` — `Adopted`, ни одного `Own`:
    расширение не добавляет ни одного объекта метаданных.

  **Опровергнутая гипотеза:** «выгрузка молча содержит объекты расширения и не
  помечает их». Помечать нечего — проверено на живом сервере: объекты с
  локальным префиксом (синтетические примеры `Документ.лок_ОбъектА` и
  `Справочник.лок_ОбъектБ`) уже лежат в основной выгрузке, а расширение их только
  заимствует.
  Открытым остаётся вопрос про реквизиты, добавленные расширением заимствованному
  объекту: в этом расширении таких нет.

  Публичные выводы, раскладки и команды воспроизведения собраны в
  `docs/modules-provider-design.md`.

- **Справка запрещает неограниченные строки через раз.** Проверены все статьи
  `shquery_ru.hbk`, где запрет должен был бы стоять:

  | Статья | Что говорит |
  |---|---|
  | `КОЛИЧЕСТВО`, `МИНИМУМ`, `МАКСИМУМ` | «Не может применяться к … Строкам неограниченной длины» |
  | `СУММА`, `СРЕДНЕЕ` | молчит |
  | `СГРУППИРОВАТЬ ПО` | молчит — **а падает именно она** |
  | `УПОРЯДОЧИТЬ ПО`, `РАЗЛИЧНЫЕ`, соединения, `ОБЪЕДИНИТЬ`, `ИНДЕКСИРОВАТЬ ПО`, `ИТОГИ` | молчат |
  | `Приведение типа` (`ВЫРАЗИТЬ`) | молчит |

  То есть агент, честно прочитавший `СГРУППИРОВАТЬ ПО`, узнать об ограничении
  не мог. Отсюда и оговорка в карточке объекта: справку мы не правим, но и
  промолчать нельзя.

- **Запрет на неограниченные строки шире, чем описан где-либо.** Прогоны на
  живой базе 2026-08-18, тексты ошибок платформы дословно:

  | Конструкция | Что сказала платформа |
  |---|---|
  | выборка поля | работает |
  | `УПОРЯДОЧИТЬ ПО` | «недопустимое поле для упорядочивания КодСкидки» |
  | `РАЗЛИЧНЫЕ` | «в запросе с предложениями РАЗЛИЧНЫЕ или **ОБЪЕДИНИТЬ** нельзя использовать поля неограниченной длины» |
  | условие соединения | «неверные параметры в операции **сравнения**. Нельзя сравнивать поля неограниченной длины и поля несовместимых типов» |

  Два вывода, которых не было в первой редакции оговорки. **`ОБЪЕДИНИТЬ` тоже
  под запретом** — платформа называет его сама, мы о нём не догадывались.
  **Сравнение запрещено шире соединения**: платформа говорит «нельзя
  сравнивать», а не «нельзя соединять», значит под запрет попадают и равенство
  в `ГДЕ`, и `В (…)`. Упорядочивание при этом — отдельное ограничение со своим
  текстом ошибки, а не следствие сравнения.

- **`ВЫРАЗИТЬ(поле КАК СТРОКА(N))` ограничен 1024 символами и предназначен для
  составного типа.** Прямо в статье «Приведение типа»: «Для <Тип значения>
  СТРОКА с указанием длины максимальный размер строки составляет 1024», и там
  же — «если выражение не содержит в составном типе требуемый тип, выполнение
  запроса завершится ошибкой». Поэтому в рецепт пошла `ПОДСТРОКА`: у неё
  потолка нет, она описана как обычная строковая функция и проверена владельцем
  на живой базе. Заодно это первая польза от разбора диаграмм — до него
  синтаксис `ВЫРАЗИТЬ` печатался столбцом и не читался.

- **Таблицы не были причиной «словесного магнита» — им оказалась длина
  описания.** Постановка на доске утверждала, что развёрнутая таблица на
  странице `SelectionFieldsList` («Список полей выборки / Поле выборки /
  Описание поля / Выражение / Псевдоним поля») делает её магнитом, выигрывающим
  5 живых вопросов из 27. Проверено опытом 2026-08-18: таблицы выброшены
  полностью — магнит на месте, 4 запроса из 8 контрольных.

  Настоящая причина: слова «параметр» на этой странице **нет ни разу**, а она
  выигрывает «как задать параметр в запросе» со счётом 100,3 против 88,2 у
  правильного ответа. Поле `description` суммирует вес по каждому вхождению
  (`search.py`, `SearchIndex.add`), нормировки по длине нет, а
  `SelectionFieldsList` — самое длинное описание справки: **4 905 символов при
  медиане 467**, вдесятеро. Она выигрывает объёмом. Таблицы дают там 423
  символа из 4 905, то есть 8,6%.

  Хрупкость видна на одном слове: «как задать параметр **в тексте** запроса» —
  правильный ответ первый, «как задать параметр в запросе» — магнит первый.
  В наборе `tests/queries/query-language.json` лежит длинный вариант, поэтому
  стенд показывает 94,7% и магнита не видит. Лечение — нормировка длины в
  ранжировании; это отдельное направление улучшения поиска.

- **Таблиц в справке языка запросов две разных природы, и класс CSS их не
  различает.** `class=SimplyTable` — настоящая таблица данных (результат
  запроса-примера), но стоит не на всех: 7 настоящих таблиц идут без него
  (`pseudonim_fields.html`, `GROUPING`). Таблица без класса, с `border=0`,
  вложенная, из ячеек `&nbsp;` и `|` — **нарисованная синтаксическая
  диаграмма**: колонки держат уровень ветвления грамматики, а не смысл.
  Печатать её markdown-таблицей — тот же столбец значений. Различает геометрия:
  у таблицы данных все строки одной ширины и нет ячеек из одной черты.
  Итог по справке: 51 таблица данных на 31 странице, 21 диаграмма на 17.

- **Таблицы в справке вложены — и вложена таблица данных в таблицу данных.**
  Разбор регуляркой `<table>.*?</table>` останавливается на закрывающем теге
  внутренней, и остаток внешней утекает в описание обычными абзацами. Хуже
  того, ячейки внутренней попадают в строку внешней: на `nestedtables_fetch.html`
  строки выходят рваной ширины (2 и 3), и обе таблицы разом опознаются как
  диаграмма. Границы приходится искать счётчиком глубины.

- **Текст, возвращённый в HTML-поток, проходит `_text()` дважды — и теряется.**
  Диаграмма, вставленная обратно абзацем `<P>лесенка</P>`, теряла угловые
  скобки (`<Перечень соединений>` принимается за тег) и отступы (`_text`
  обрезает каждую строку). Обходится меткой: в поток идёт `\x00…\x00`, текст
  подставляется после сборки описания.

### Добавлено

- **Граф связей: страница без выбранной конфигурации была тупиком —
  исправлено в тот же день.** Нашёл владелец, открыв «Связи» по ссылке из
  навигации: при нескольких загруженных конфигурациях реестр отказывается
  угадывать, страница печатала «Загружено несколько конфигураций, укажите
  нужную явно» — и не давала ни одного способа её указать. Поле `config` было
  скрытым, форма на странице отказа не рисовалась вовсе.

  Исправлено тремя правками: конфигурация выбирается списком, как на
  «Запросах» и «Словаре»; при пустом `config` подставляется первая по
  алфавиту; форма остаётся на странице при любом отказе — иначе он не даёт
  исправить то, на что жалуется. Пустой реестр объясняется словами со ссылкой
  на «Источники».

  Заодно опечатка в имени объекта теперь предлагает похожие имена, как это
  делает `get_object`: имена в 1С длинные, и промах по ним — самый частый
  способ попасть на отказ.

  **Тесты дефект не ловили и не могли:** все они ходили с уже заданным именем
  конфигурации, то есть проверяли путь, на который живой человек по ссылке из
  меню не попадает. Добавлены четыре на путь без `config` и один на подсказку.

- **Граф связей на дашборде — страница `/graph`** (`graph_view.py`, тестов
  348 → 371). Окрестность объекта картинкой: узлы по видам цветом, стрелки по
  направлению ссылки, подпись ребра по наведению, клик по узлу строит граф
  вокруг него. Предел соседей выбирается на странице (15…400).

  **Почему человеку, а не агенту.** Инструмент `find_dependency_paths` не
  реализован: живого случая нет, а на нынешних данных он давал бы неверные
  ответы. Картинка снимает оба
  возражения: token-цена нулевая, а мусорную связь человек видит глазом там,
  где агент принял бы её за ответ.

  **Отсечения по степени нет, и это замер, а не лень.** Первая мысль была
  резать узлы с большой степенью как мусорные. Верхушка по степени в Рознице:
  `Справочник.Пользователи` 323 соседа, `Организации` 311, `Номенклатура` 264,
  `Магазины` 243 — самые осмысленные объекты конфигурации. А узел, который
  портил пути (`ДополнительныеРеквизитыИСведения`), имеет скромные 34: он
  мешает не размером, а тем, что в него **входят** 33 объекта. Порога, который
  отличит одно от другого, не существует — глаз отличает мгновенно.

  **Ни одной зависимости и ни одного файла.** SVG собирается сервером, на
  клиенте сорок строк обычного JS для перетаскивания и зума. d3 (250 КБ) или
  React завели бы второй язык и шаг сборки в дашборде, который сейчас —
  1 155 строк на голом HTML. Решение по замеру, а не заранее: не хватит
  интерактивности — вернёмся, уже зная, чего именно.

  **Раскладку правили по картинке, а не по коду.** Первая версия задавала
  радиус кольца константой, и при тридцати узлах подписи наехали друг на
  друга — увидели на первом же скриншоте. Радиус теперь считается из числа
  узлов на кольце, кольца два при двадцати и более соседях, а предел по
  умолчанию снижен с 60 до 30: при шестидесяти схема ужимается в окно так, что
  подписи превращаются в три пикселя.

- **Стенд замеров стал инструментом, а не набором функций** (`bench.py`,
  тестов 332 → 348). Раньше `bench.py` давал `run()` и загрузку наборов, но не
  давал прогона: каждый замер собирался руками. За две сессии так написалось
  три разовых скрипта — замер до/после, разбор смены мест, подбор веса, — и
  все три выброшены.

  Теперь одна команда на все четыре набора, 43 секунды:

      PYTHONPATH=src .venv/bin/python -m mcp1c.bench \
          --data data --config РозницаДляКазахстана \
          --auto --sets query-language,roznica-metadata --check-notes

  **Метрики сверх прежних трёх.** P@1/P@3/P@5/P@10 раздельно; «чужой домен
  первым» — ждали язык запросов, получили платформу (та самая мера, которой
  руками считали «магнит выигрывает 5 вопросов из 27»); отрыв первого от
  второго медианой — «уверенно попали или чудом».

  Замер на 2026-08-18, четыре набора:

  | Набор | Запросов | P@1 | P@3 | P@5 | MRR | Чужой домен | Отрыв |
  |---|---|---|---|---|---|---|---|
  | Язык запросов | 19 | 94,7% | 94,7% | 100% | 0,958 | 1 | **35,0%** |
  | Метаданные Розницы | 21 | 90,5% | 95,2% | 95,2% | 0,934 | 0 | 94,0% |
  | Точные имена справки | 50 926 | 97,1% | 98,3% | 98,7% | 0,978 | 21 | 91,7% |
  | Одноимённые | 10 544 | 98,8% | 99,8% | 99,9% | 0,993 | 29 | 93,8% |

  **Новое, чего прежний стенд показать не мог:** победы по языку запросов
  держатся на отрыве в 35% против 91,7–94,0% у остальных наборов. То есть они
  втрое шатче, и правка ранжирования способна их перевернуть, не сдвинув ни
  одного процента P@1. Прежде это было невидимо.

  **Сравнение прогонов поимённо.** `--save` пишет прогон, `--baseline`
  сравнивает со следующим и называет, кто сменил место — ухудшения первыми.
  Запросы сверяются по тексту, а не по порядку: порядок в наборе меняется от
  любой правки файла, и привязка по индексу молча сравнивала бы разные
  запросы.

  Выдача сохраняется только у промахов: у взявшего первое место она известна
  из `expected`. На автоматических наборах это разница между 74 МБ и 21.

  **Сверка пометок окупилась в первом же прогоне.** `--check-notes` сверяет
  `note` в наборе с тем, какое место запрос занял, и сразу нашёл вравшую
  запись в `roznica-metadata.json` — наборе, которого правка ключей не
  касалась вовсе: «товары» помечен «ИЗВЕСТНЫЙ ПРОМАХ», а он давно первый.
  Разошлось само — выправили стемминг Snowball и нормирование по длине имени,
  прямой правки под этот запрос не делалось ни разу.

  Порогов в assert нет намеренно: наборы запросов — не тесты, проценты
  ломались бы от каждой правки словаря. Ненулевой код возврата — только на
  расхождении пометок: это не качество поиска, а враньё в файле.

  Записано и то, обо что уже спотыкались: наборы в образ не входят
  (`tests/` в `.dockerignore`), мерить надо из рабочей копии.

### Изменено

- **`get_related` отдаёт только прямые связи: раздел «в радиусе N» врал.**
  Замер на живых данных: для `Справочник.Номенклатура` раздел дал 199 строк,
  все до одной с расстоянием 1. Ни одного объекта на расстоянии 2 в нём не
  было — обход упирается в собственный лимит в 200 объектов раньше, чем делает
  второй шаг, а у Номенклатуры одних прямых соседей 264.

  Цена вранья — 14 085 символов против 4 958, то есть 3 000 лишних токенов на
  вызов за продолжение списка прямых соседей под заголовком, обещающим соседей
  соседей.

  Поднимать лимит смысла нет: на двух шагах достаётся 1 000 объектов, на
  трёх — 1 700 из 5 637. Список имён без объяснения, через что идёт связь,
  бесполезен и показанный целиком. Параметр `depth` снят, отсутствие закреплено
  тестом, описание инструмента теперь называет причину.

- **Три уточнения ранжирования, найденные проверкой 2026-08-17, закрыты
  2026-08-18** (тестов 331 → 332).

  *Набор видов языка запросов объявлялся дважды* — `QUERY_LANGUAGE_KINDS` в
  `search.py` и `_QUERY_LANGUAGE_KINDS` в `tools.py`, слово в слово. Переехал
  в `syntax_model.py`, рядом с самими видами: четвёртый вид иначе добавили бы
  в одном месте, забыв про второе.

  *Два теста проверяли одно и то же.* `«ОбъединитьВсе»` и `«объединить все»`
  после `split_identifier` дают одинаковые токены — это был один запрос
  дважды. Один снят, второй перепрофилирован на границу подъёма: отрыв 1,19
  подъём отыгрывает, 1,21 — уже нет. Прежние тесты множитель не стерегли
  вовсе (замер показывал, что 1,2, 1,5, 2,0 и 3,0 дают одинаковый результат
  на живом наборе); новый краснеет при подмене 1,2 на 1,5.

  Ровно на 1,2 счёт сходится вничью до последнего бита, и победителя выбирает
  порядок сортировки. Проверяются обе стороны границы, а не она сама: тест на
  ничью закреплял бы за правило то, что правилом не является.

  *Пометки промахов в `tests/queries/query-language.json` врали.* После слоя
  ключей семь запросов сменили место, а файл описывал состояние до него: восемь
  записей с «ИЗВЕСТНЫЙ ПРОМАХ» на запросах, которые уже первые. Переписано 10
  пометок; каждая теперь называет место и чем промах вылечен — иначе через
  месяц непонятно, что запрос стережёт. «Известным промахом» остался один:
  «в чем разница между внутренним и левым соединением», 5 место.

- **Описания инструментов называют порядок вызовов и лимиты.** Порядок
  `search_objects` → `get_object` → `search_syntax` был записан только в
  `instructions` сервера, которые часть клиентов сокращает или не показывает
  вовсе. Теперь переход назван в описании самого поискового инструмента — там,
  где агент читает его в момент вызова.

  Проверено на живых данных, что дыра настоящая: `search_objects` по
  «остатки товаров» отдаёт имена и счётчики, и ничего больше, а
  `get_object` по `РегистрНакопления.ТоварыНаСкладах` — вид регистра
  `Остатки` и раздел «Таблицы запроса» с `КоличествоОстаток`,
  `КоличествоОборот`, `КоличествоПриход`. Запрос, написанный сразу после
  поиска, выглядит правильным и падает на «поле не найдено».

  Что дописано: `search_objects` отправляет к `get_object` и называет причину;
  `get_object` перечисляет, что есть только в нём (вид и периодичность
  регистра, корреспонденция, предел субконто, готовые имена полей);
  `search_syntax` отправляет к `get_syntax` и называет свой предел в 400
  символов описания; `get_syntax` говорит про замены версий и «Доступность»
  как про ошибки компиляции; `get_related` предупреждает про цену `depth=2`;
  `limit` объясняет, что выше 10 поднимать обычно незачем.

  **Цена измерена, польза — нет.** Контракт `tools/list` вырос с 7 239 до
  10 208 символов (≈ 2 413 → 3 402 токена), это разово на сессию. Проверить
  выигрыш нечем: он проявляется в поведении агента, а стенда на это у нас
  нет — заявлять улучшение без замера нельзя, поэтому здесь только цена.
  Правка вступает в силу после пересборки образа.

- **Тесты на контракт `tools/list` (`test_tool_descriptions.py`).** Проверяется
  не текст описаний — он живой, — а что контракт заполнен: у каждого
  инструмента есть описание, у каждого параметра тоже, и поисковые инструменты
  называют, куда идти за подробностями. Пустое описание агент не заметит:
  просто угадает и ошибётся молча, как уже было с `config`, куда подставлялся
  путь к файлу вместо имени конфигурации.

### Добавлено

- **Слой поисковых ключей языка запросов (`search_keys.py`).** К 116 страницам
  из 127 приписаны формулировки, которыми их спрашивают: «сколько дней между
  двумя датами» → `РАЗНОСТЬДАТ`, «убрать повторы» → `РАЗЛИЧНЫЕ`. Синоним такое
  не лечит — заменять нечего, у запроса с целью ноль общих слов. Ключи
  попадают в обычный лексический индекс отдельным полем с весом 2.0; в
  рантайме не весят ничего.

  Замер на слитом индексе (справка 8.3.27 плюс язык запросов), одни и те же
  данные, разница ровно в одном поле:

  | Набор | Первым | В пятёрке | MRR |
  |---|---|---|---|
  | Язык запросов (19 живых) | 11 → **18** (57,9% → 94,7%) | 15 → **19** (78,9% → 100%) | 0,639 → 0,958 |
  | Точные имена справки (50 814) | 49 312 → 49 323 (97,0% → 97,1%) | 50 127 → 50 132 | 0,977 |
  | Одноимённые (10 593) | 10 471 → 10 471 (98,8%) | 10 581 → 10 586 | 0,993 |

  Регресса нет ни на одном наборе. Цена: токенов индекса 14 450 → 14 508
  (+0,4%), постингов 536 639 → 537 111 (+0,09%), массивы 13 046 → 13 057 КБ
  (+0,08%), латентность 0,21 → 0,22 мс.

  **Вес 2.0 — не компромисс, а строгий максимум.** Прогон по трём наборам:
  1.0 даёт 14 из 19 на живом наборе, 2.0 — 18, а 3.0 и выше начинают сдвигать
  одноимённые (10 471 → 10 468 первым местом: статья «ГОД» обходит
  платформенный «Год») и при этом на живом наборе не выигрывают ничего —
  4.0 и 6.0 дают 17, то есть хуже.

  **Оговорка о честности замера.** Ключи писались по текстам статей, без
  сверки с `tests/queries/query-language.json`. Но две формулировки из
  выигравших семи — «уникальные записи без повторов» и «как задать параметр» —
  были известны автору до генерации проверочного набора. Стало
  быть, консервативная оценка выигрыша — 11 → 16, а не 11 → 18. Остальные
  пять (приведение типа, первые 10 строк, разыменование через точку,
  сортировка результата, прибавить месяц к дате) чистые.

- **Отчёт о привязке ключей при загрузке справки языка запросов.** Ключи
  привязаны к страницам по идентификатору, а он приходит из имени файла внутри
  `.hbk`. Справка другой сборки может дать другой набор страниц — тогда часть
  ключей повиснет и поиск молча просядет до состояния «как без ключей».
  Расхождение теперь попадает в `Source.warnings` с числами и примерами
  потерянных. Отчёт пересчитывается и при `restore()`, то есть переживает
  перезапуск.

  Механизм окупился немедленно: первый же прогон поймал 27 неверных
  идентификаторов в свежей таблице — страницы статей называются
  `query/query_totals.html`, а не `query/query_totals`.

- **Снятие повторов токена в поле `keys` (`DEDUPED_FIELDS`).** Поле — набор
  перефразировок одного и того же, и общие слова в них неизбежны: «дата»
  стоит в четырёх формулировках из пяти. Без дедупликации вес поля умножался
  бы на то, сколько раз слово написано, то есть на случайность авторского
  стиля. В остальных полях повтор по-прежнему копится намеренно.

### Исправлено

- **Описание статьи языка запросов индексировалось на 2–5% — теперь целиком.**
  Индекс справки резал описание любого элемента до 400 символов; для метода
  платформы это аннотация, и потолок ни разу не бился, а для статьи языка
  запросов описание — единственное содержимое страницы (8–24,8 КБ). Потолок
  снят для трёх видов (`query_function`, `query_keyword`, `query_article`,
  127 страниц), для остальных остался как был — замер показал, что целиком и
  4000 символов дают тот же результат, промежуточный потолок не нужен. На
  живом наборе из 19 формулировок: первым 47,4% → 52,6%, в пятёрке
  57,9% → 73,7%, MRR 0,532 → 0,593; автоматические наборы не сдвинулись.

- **Дашборд не показывал, из какого домена пришла строка выдачи.** Подпись
  переключателя `/queries` «по справке платформы» была неточной с момента
  подключения языка запросов: индекс этого режима общий для обоих доменов. В таблицу
  выдачи добавлена колонка «Вид» (те же подписи, что печатает MCP,
  `KIND_TITLES`/`FIELD_KIND_TITLES`) — без неё страница не отвечала на свой
  главный вопрос «почему выдача такая».

### Добавлено

- **Мягкий подъём элементов языка запросов по обороту в вопросе.** Оборот
  «в запросе», «в тексте запроса», «в выборке» или «языка запросов» — признак,
  которого не бывает в именах элементов платформы или конфигурации. При его
  наличии `query_function`/`query_keyword`/`query_article` получают множитель
  1,2 к оценке. Место правки — `SearchIndex.search()`, общее для MCP и
  дашборда: подниматься должно одинаково независимо от потребителя.

  Множитель подобран измерением: 1,2, 1,5, 2,0 и 3,0 дают одинаковый
  результат — разрывы между платформой и языком запросов невелики, большего
  толчка не требуется, а он начал бы переставлять случаи уверенного отрыва
  платформенного элемента («как задать параметр в запросе» может быть и про
  `Запрос.УстановитьПараметр`). Безопасность проверена на обоих
  автоматических наборах: ни один из четырёх оборотов не встретился ни в
  одном из 61 051 запроса (0 из 50 572, 0 из 10 479). На живом наборе из 19:
  первым 52,6% → 57,9%, в пятёрке 73,7% → 78,9%, MRR 0,593 → 0,639.

### Добавлено

- **Таблицы запроса в карточке регистра — имена полей, которых нет в
  метаданных.** Ресурс регистра называется в запросе не так, как в
  конфигураторе: `Количество` в основной таблице, `КоличествоОстаток` в
  `.Остатки`, `КоличествоПриход`/`КоличествоРасход`/`КоличествоОборот` в
  `.Обороты`. Из-за этого агент писал запрос, который выглядел правильным и
  падал на «поле не найдено»: структуру объекта он получал верную, а
  спрашивал другую таблицу.

  Знание было разложено на две половины, и порознь ни одна не помогала:
  справка платформы знает шаблоны (`<Имя ресурса>Остаток`), выгрузка знает
  настоящие имена ресурсов. `virtual_tables.py` соединяет их и печатает
  готовые имена в `get_object` на уровне `fields` — том самом, которым
  пользуются для написания кода.

  Наизусть суффиксы не сочиняются: нет справки — нет и блока.

### Исправлено

- **Обработка выгрузки читала несуществующее свойство и молча теряла все
  остальные.** `ДополнитьСвойствамиВида` оборачивала весь список одним
  `Попытка`. Для регистра сведений первой строкой шло `Объект.Периодичность`
  — такого свойства у объекта метаданных нет (оно называется
  `ПериодичностьРегистраСведений`), исключение прерывало процедуру, и вместе
  с периодичностью терялся режим записи. Выгрузка при этом считалась
  успешной: **2 615 регистров сведений в пяти конфигурациях без единого
  свойства**. Теперь каждое свойство берётся своей попыткой — потеря одного
  не уносит соседние.

  Имя свойства сверено справкой платформы через собственный MCP-сервер:
  `ОбъектМетаданных: РегистрСведений` → `ПериодичностьРегистраСведений`,
  тип `ПериодичностьРегистраСведений`, значения `Год`, `День`, `Квартал`,
  `Месяц`, `Непериодический`, `ПозицияРегистратора`, `Секунда`.

  **Чтобы признак появился, конфигурации нужно перевыгрузить** новой
  обработкой — в существующих выгрузках его нет.

- **Срез предлагался непериодическому регистру сведений.** Найдено владельцем
  на живом агенте: тот собрал запрос
  `РегистрСведений.КомплектацияТоваров_kaspi.СрезПоследних(&МоментВремени, …)`
  для регистра, у которого срезов нет вовсе. Причина — первая версия
  `virtual_tables.py` отдавала `СрезПервых`/`СрезПоследних` любому регистру
  сведений, не глядя на периодичность, потому что признака в выгрузке не
  было. Теперь срезы показываются только при подтверждённой периодичности;
  нет признака — нет и срезов.

### Найдено

- **Фикстура тестов писала `types`, а загрузчик читает `type` — типы
  реквизитов молча терялись.** Найдено 2026-08-18 при тестах графа: на
  синтетической выгрузке из `conftest.py` граф выходил пустым, ноль рёбер.
  Причина в двух местах сразу: `write_export` сериализовала поле под ключом
  `types` (схема v1 требует `type`), а сам тип был записан как
  `СправочникСсылка.Контрагенты`, тогда как выгрузка пишет полное имя объекта
  (`Справочник.Контрагенты`) — граф разрешает ссылку только по нему.

  Следствие: **рёбра графа через загрузчик не проверял ни один из 348 тестов.**
  Дефект не в коде, а в фикстуре, и потому был невидим — тесты проходили, но
  проверяли граф без единого ребра. Исправлено, теперь синтетическая выгрузка
  даёт связь `Документ.РеализацияТоваровУслуг -> Справочник.Контрагенты`.

- **Стенд может померить не тот код: `.pyc` переживает правку, если mtime
  совпал до секунды.** Найдено 2026-08-18 при подборе граничного отрыва.
  Правка константы, замер, откат файла через `cp` — и следующий прогон
  показал поведение отката, хотя в файле уже стояло прежнее значение:

      grep  QUERY_LANGUAGE_BOOST = 1.2      # в исходнике
      python import mcp1c.search -> 1.0     # в памяти

  Python проверяет свежесть байт-кода по паре (mtime, size) исходника.
  `cp` ставит mtime «сейчас», и если `.pyc` был записан в ту же секунду
  предыдущим прогоном, а размер файла не изменился (`1.0` и `1.2` — одна
  длина), кэш считается актуальным и читается вместо исходника.

  Час ушёл на «расхождение»: константа объявлена 1,2, а отношение счётов
  выходило ровно 1,3. Никакого расхождения не было — мерился откаченный код.
  После `rm -f src/mcp1c/__pycache__/*.pyc` граница встала ровно на 1,2, и на
  ней счёт сходится вничью до последнего бита (41,107076 против 41,107076).

  **Правило: перед замером, который откатывал файлы, сносить `__pycache__`.**
  Практика «замер перед утверждением» этого не покрывала — она требует
  показать вывод, но вывод может быть снят не с того кода. `pytest` тем же
  не страдает: он импортирует один раз за прогон, а правки между прогонами
  меняют размер файла чаще, чем нет.

- **Qwen Code подключается по `httpUrl`, а не `url`; расследовано и закрыто
  2026-08-17.** Прошлый заход кончился выводом «не подключается ни по
  `httpUrl`, ни по `url`, дело в клиенте» — вывод был верным, но причину мы
  тогда не назвали, потому что не смотрели коды ответов.

  Владелец заметил в логе контейнера обращение `GET /mcp` вместо `POST` и
  предположил, что дело в этом. Предположение подтвердилось:

      Created new transport with session ID: bd9d5e1b…
      172.20.0.1 - "GET /mcp HTTP/1.1" 400 Bad Request

  Транспорты начинают рукопожатие с разных концов. Старый HTTP+SSE: клиент
  делает `GET`, сервер отдаёт поток и в первом событии сообщает адрес для
  POST. Наш streamable-http: сначала `POST initialize`, в ответе приходит
  `mcp-session-id`, и только потом `GET` открывает поток. Голый `GET` без
  сессии наш сервер законно отбивает — `400 Bad Request: Missing session ID`.

  В конфигах семейства Gemini CLI (Qwen Code наследует формат) транспорт
  выбирается ключом: `url` — старый SSE, `httpUrl` — streamable-http. Стоял
  `url`, поэтому клиент всегда упирался в 400 на первом же запросе, до
  `tools/list` дело не доходило. С `httpUrl` подключение прошло.

  Поведение сервера снято прогоном сборки из текущего кода на свободном порту:

      GET  /mcp  без mcp-session-id           -> 400  Missing session ID
      GET  /mcp  с mcp-session-id             -> 200  text/event-stream
      POST /mcp  Accept: application/json     -> 406  нужен ещё text/event-stream
      POST /mcp  Accept: json + event-stream  -> 200
      GET  /sse                               -> 404  старого транспорта наружу нет

  Отсюда таблица разбора для будущих клиентов: 406 — клиент не шлёт оба типа
  в `Accept`; 400 — не возвращает `mcp-session-id`; 404 на `/sse` — говорит
  старым транспортом; 401 — нет `X-Api-Token`. Решение не выводить SSE наружу
  остаётся в силе: второй транспорт означает второй процесс и ещё 644 МБ.

- **`ЗаписатьJSON` не принимает перечисления платформы, а XML-ветка их
  терпит.** Найдено владельцем 2026-08-17 на первой перевыгрузке: выгрузка в
  формате JSON падала с «Значение содержит данные недопустимых типов», в
  формате XML та же конфигурация выгружалась. Причина расхождения — в самой
  выгрузке: XML пишется через `ЗаписатьСтруктуруВXML`, которая гонит каждое
  значение через `СкалярВСтроку`, а `ЗаписатьJSON` получал структуру как есть.
  Виновником оказалось `ЗаписьДвиженийПриПроведении` — это перечисление
  платформы, а не Булево, и без `Строка()` оно роняло выгрузку целиком.

  Сообщение платформы не называет ни свойство, ни объект — искать пришлось
  сверкой типов всех добавленных свойств по справке. Поэтому свойство
  исправлено, но рядом поставлена и общая защита: перед записью структура
  приводится к типам, которые формат понимает. Свойств у метаданных сотни,
  тип каждого наизусть не проверить, а падать целой выгрузкой из-за одного
  поля нельзя.

- **`ДатаГрафика` и `ЗначениеГрафика` регистра расчёта — не дата и не число.**
  По справке это `ОбъектМетаданных: Измерение` и `ОбъектМетаданных: Ресурс`,
  то есть измерение и ресурс графика. `Строка()` от них даёт представление
  вида «Измерение», по которому ничего не найти; нужны имена.

- **Срезы существуют только у периодического регистра сведений.** У
  непериодического нет самого поля `Период`, по которому берётся срез.
  Признак — `ПериодичностьРегистраСведений` объекта метаданных; значение
  `Непериодический` означает, что `.СрезПервых` и `.СрезПоследних` у регистра
  отсутствуют и запрос к ним не скомпилируется.

- **`РазрешитьИтогиСрезПоследних` — не про доступность среза.** Свойство
  управляет обновлением таблицы итогов, а не наличием виртуальной таблицы;
  опираться на него вместо периодичности нельзя.

- **Виртуальная таблица переименовывает ресурсы регистра, и это описано в
  справке.** Раздел `tables/` содержит 736 элементов и 591 поле, из них 31
  шаблон вида `<Имя ресурса>…`: `Остаток`, `Приход`, `Расход`, `Оборот`,
  `НачальныйОстаток`, `КонечныйОстаток`, а для регистра бухгалтерии ещё
  `ОстатокДт`, `РазвернутыйОстатокКт`, `КорОборот`. Основная таблица регистра
  отдаёт ресурс без суффикса — отсюда и расхождение с конфигуратором.

- **Раздел `tables/` живёт в справке платформы, а не языка запросов.**
  `shquery_ru.hbk` (127 страниц) таблиц не содержит вовсе: 0 из 127. Загрузка
  языка запросов состав виртуальных таблиц не даёт и дать не может.

- **Поля таблиц запроса не привязаны к своей таблице.** У `query_field` пустой
  `parent_ru`; единственная связь — путь идентификатора
  (`tables/catalog8/table13/fields/field293`). Поэтому агент, даже найдя
  шаблон `<Имя ресурса>Остаток`, не мог понять, к какой таблице тот относится.

- **Справка 8.3.5 размечает те же таблицы без полей.** Для отраслевой видны
  четыре дубля вида `РегистрНакопления.<Имя регистра>.Остатки` — имя есть,
  полей ноль. Пустые шаблоны отбрасываются; на конфигурации 8.3.5 блок
  собирается по справке более новой платформы, и это работает, потому что у
  полей таблиц нет границ версий.

- **Справка не различает оборотный и остаточный регистр в составе
  `.Обороты`** — перечисляет `Приход`, `Расход` и `Оборот` без оговорки, хотя
  приход и расход следуют из вида движения, которого у оборотного регистра
  нет. Единственное место модуля, где правило взято не из справки, и оно
  сознательно строже её. **Требует подтверждения на живой базе.**

- **Регистр бухгалтерии описан в справке дважды** — с поддержкой
  корреспонденции (`ОстатокДт`/`ОстатокКт`) и без неё (`Остаток`). Признака
  корреспонденции в выгрузке нет, выбрать нечем — блок для него не печатается
  вовсе, кроме однозначной `ОборотыДтКт`. Показать наугад один из двух
  вариантов значило бы вернуться к той же ошибке, ради которой писался модуль.

- **Перебор справки на каждый вызов стоит 14,8 мс.** Первая реализация искала
  таблицы по всем 25 691 элементу: `get_object` по регистру стоил 14,8 мс
  против 0,04 мс по справочнику. Индекс шаблонов считается один раз при
  загрузке справки (`LoadedSyntax.tables`, тот же приём и по той же причине,
  что `by_name`) — **0,022 мс, в 672 раза быстрее**, старт не изменился.

- **Вид регистра есть в выгрузке, но только у регистра накопления.**
  `register_kind` (`Остатки`/`Обороты`) проставлен ему одному; у регистра
  сведений и регистра бухгалтерии `props` пуст. Первая версия требовала это
  поле и молча пропускала регистр сведений — то есть не показывала
  `СрезПоследних`, самую частую таблицу в отчётах.

- **Версии платформы и доступность не делает никто на рынке.** Разобрано десять
  публичных MCP-проектов экосистемы 1С; вывод ниже самодостаточен.
  У самого популярного справочника, `alkoleft/mcp-bsl-platform-context` (187
  звёзд, MIT, тесты и CI), разбор этих секций отключён явно: в
  `MethodPageParser.createHandler()` строки `"Доступность:"`,
  `"Использование в версии:"` и `"Использование в интерфейсе:"` сопоставлены с
  `null`, полей `since`/`until` в модели нет, одна версия платформы на инстанс
  заложена архитектурно. То есть 730 неверных фактов, которые мы намерили на
  8.3.5 одной свежей справкой, там выдаются агенту как есть.

- **Тот же `.hbk` читают тем же способом — побайтово, без деархивации.**
  `HbkContainerReader` на `MappedByteBuffer` устроен как наш `v8container`:
  заголовок 16 байт, таблица файлов, блоки «заголовок с UTF-16LE именем →
  тело». Независимое подтверждение, что формат разобран правильно.

- **Лексика — не наша особенность, а норма для этого домена.** Из десяти
  проектов эмбеддинги используют три, и все три — не для справки платформы и не
  для структуры метаданных, а для текстов модулей и корпусов шаблонов кода.
  Прямой конкурент по справке ищет чисто лексически: цепочка стратегий с
  приоритетами плюс расстояние Левенштейна, без морфологии русского.

- **Выигрыш эмбеддингов измерен ровно один раз и относится к текстам модулей.**
  `ROCTUP/1c-mcp-metacode`, бенчмарк 17 моделей на 500 вопросах и 25 000
  процедур: coverage@5 0,7204 без эмбеддингов против 0,9376 с вектором и
  переранжированием. На метаданных и справке ни один проект прироста от вектора
  не показал, latency семантического поиска не публикует никто. Гипотеза
  «вектор ничего не даёт нигде» этим опровергнута частично: она верна для наших
  сегодняшних доменов и неверна для провайдера `modules`.

- **Векторная база не нужна и в том случае, если вектор когда-нибудь возьмём.**
  25 701 документ на 384 измерения — 37,65 МБ матрицы `float32`; brute-force
  одним `matmul` на numpy даёт точный результат за доли миллисекунды. Flat-индекс
  рекомендуют до ~100 тыс. векторов, а FAISS FlatL2 — тот же перебор с SIMD.
  Цена вектора целиком в модели-энкодере на рантайме, а не в хранилище.

- **Компактные русские модели для поиска не годятся.** ruMTEB Retrieval
  nDCG@10: `multilingual-e5-base` 67,1, `ru-en-RoSBERTa` 66,5,
  `multilingual-e5-small` 65,8, `rubert-tiny2` — **8,9**. Маленькие rubert
  оптимизированы под семантическое сходство и классификацию, а не под retrieval;
  «лёгкая модель для русского» задачу поиска не решает.

- **Тяжёлый стек стоит часы, а не проценты.** У `1c-mcp-metacode` построение
  графа типовой Бухгалтерии КОРП — ~30 минут при 7 ГБ RAM приложения и 5 ГБ
  Neo4j, фаза эмбеддинга кода 4–6 часов, ~$3,5 + ~$16 за прогон через
  OpenRouter. У «облегчённого» `1c-litecode-mcp` (Memgraph + ONNX, образ 1,4 ГБ)
  индексация 638 тыс. процедур на CPU — 10–18 часов. Наши 0,25–6 с загрузки и
  350 МБ образа — не мелкая экономия, а другой порядок.

- **Статический анализ BSL не требует MCP.** У `bsl-language-server` есть
  batch-режим `analyze -s <src> -o <out> -r json` — MCP-мост добавляет не сам
  анализ, а интерактивный доступ к нему (hover, rename, живая сессия). Если
  анализ понадобится, начинать надо с CLI, а не с моста на JVM с 8 ГБ.

- **Стенд замеров должен генерировать автоматические кейсы отдельно от
  языка запросов.** `bench.cases_from_syntax`/`cases_from_syntax_ambiguous`,
  запущенные по слитому индексу (платформа + язык запросов), добавляют
  самоссылочные кейсы вида «Функции языка запросов» → сама же статья — три
  статьи называются оборотом «языка запросов» дословно. Кейсы нужно строить
  только по справке платформы (`syntax`, без `query`), а искать — по слитому
  индексу: так счётчики (50 572 / 10 479) и метрики совпадают с ручным
  замером. Иначе безопасность подъёма (часть выше) выглядела бы нарушенной
  тремя случаями — не поиском, а способом счёта.

### Добавлено

- **Язык запросов как источник справки.** `shquery_ru.hbk` — 127 элементов
  (52 функции, 67 ключевых слов, 8 статей) разбираются отдельным парсером
  (`query_parser.py`, обычная разметка `<H1>`/`<P>`/`<PRE>`, не `V8SH_*` как
  у справки платформы) и живут рядом с ней: один поисковый индекс и одна
  таблица имён на оба набора, но источник — отдельный вид (`KIND_QUERY`) и
  не участвует в слиянии версий платформы, потому что версий у него нет.
  Инструменты `search_syntax`/`get_syntax` отвечают по языку запросов теми
  же вызовами, что и по платформе; дашборд и CLI видят источник в общем
  списке. Замеры — в `tests/queries/query-language.json` и разделе
  «Найдено» ниже: качество на новом наборе из 19 живых формулировок 47,4%
  первым местом (73,7% в первой десятке), качество на существующих
  автоматических наборах не просело на уровне точных имён (`cases_from_syntax`,
  50 944 запроса — 97,1% и до, и после) и просело на 0,2 п.п. на уровне
  голых одноимённых (`cases_from_syntax_ambiguous`, 10 549 запросов, 99,1%
  → 98,9%) — по измеримой причине, см. «Найдено».

- **Таблица замен для старых платформ.** Слияние справок закрыло вопрос «есть
  ли элемент в моей версии», но на вопрос «чем его заменить» сервер отвечал
  «нужен способ, доступный в 8.3.5» и на этом заканчивал. Теперь карточка
  недоступного элемента несёт рецепт: чем заменить, фрагмент кода там, где
  одной функцией не обойтись, и чем замена отличается от оригинала.

  Шесть строковых функций 8.3.6 — `СтрНайти`, `СтрРазделить`, `СтрСоединить`,
  `СтрШаблон`, `СтрНачинаетсяС`, `СтрЗаканчиваетсяНа`. Текст рецептов свой:
  переносить описания из `.hbk` в репозиторий нельзя, это производное от файла
  «1С».

  Оговорка обязательна у каждого рецепта — замена почти никогда не
  эквивалентна. `Найти` не умеет ни направления поиска, ни номера вхождения;
  `Лев` вместо `СтрНачинаетсяС` на пустом префиксе вернёт Истину там, где
  оригинал выбрасывает исключение. Молча подсунуть замену значит поменять
  невыполнимый код на неверный.

  В выдаче поиска рецепта нет — только отметка `· есть замена` у скрытых по
  версии элементов: десять фрагментов кода в списке из десяти строк съели бы
  контекст агента. Рецепт отдаётся тому, кто спросил `get_syntax`.

  Удалённым элементам рецепт не подставляется: там нужен путь вперёд, а не
  обход назад. У одноимённых причины могут различаться, и рецепт получает
  только тот, кто «ещё не появился».

- **Справки нескольких версий платформы в одном индексе.** Прежде справка была
  одна на процесс, и на конфигурации 8.3.5 свежая справка ошибалась в 730
  местах: 199 элементов объявляла несуществующими, 117 отдавала с чужой
  сигнатурой, 410 — с чужой доступностью. Всё это ошибки компиляции.

  Теперь справки версий стоят рядом, а слитый вид собирается из всего набора:
  у элемента появилась вторая граница `until`, а расхождения старых справок
  хранятся версионными фактами — сигнатура, доступность, прежнее имя.

  На трёх рабочих конфигурациях со справками 8.3.5.1570 и 8.3.23.1997:

      Отраслевая (8.3.5)  ЗаписьXML.ОткрытьФайл(<ИмяФайлаXML>, <ТипКодировки>)
                          СтрНайти и СтрРазделить скрыты как более поздние
                          ПреобразованиеXSL.ЗагрузитьИзФайла доступен
      Розница (8.3.23)    ЗаписьXML.ОткрытьФайл(…, <ДобавлятьBOM>)
                          ЗагрузитьИзФайла скрыт: описан по 8.3.5 включительно

  Правила сопоставления выведены замерами: ключ — русское имя (английское
  меняется в 3–10 раз чаще), английское — запасной ключ, путь страницы —
  третий признак для полей таблиц запросов, где имя одно на десятки страниц.
  Переносы разделов и переименования владельцев — курируемой таблицей:
  автоматика по составу членов путает семью «Расширение управляемой формы
  для…», у соседей состав совпадает полностью.

- **Версия справки обязательна при загрузке.** Вывести её из данных удаётся не
  всегда: в справке 8.3.5 отметок «начиная с версии» нет ни на одной из 18 936
  страниц. Берётся из имени файла (`syntax-8.3.5.1570.hbk`) или указывается
  явно; расхождение с выведенной из данных — отказ, а не молчаливая замена.

- **Покрытие: каких справок не хватает и какие лишние.** Сервер один видит и
  платформы конфигураций, и версии справок, поэтому называет это сам — в
  `list_configurations` и на странице обзора. Разрыв в один релиз стоит
  примерно 10–15 сигнатур и 35–45 контекстов доступности.

### Изменено

- **Ответы собираются под версию конфигурации, а не по самой свежей справке.**
  Карточка отдаёт сигнатуру и доступность нужной версии; когда сигнатура
  отличается, разбор параметров не показывается — он описывает другой набор.
  Версия между известными справками помечается прямо, с обоими состояниями и
  прямым указанием не додумывать. Прежние имена элементов стали ключами
  поиска: агент на старой платформе спрашивает `Жирный`, а не `Полужирный`.

- **Фильтр по версии работает всегда, когда известна платформа конфигурации.**
  Прежде он включался только для справки новее — у одной справки других поводов
  не было. Со второй границей их стало два: элемент, которого ещё нет, и
  элемент, которого уже нет, и путать причины нельзя.

- **Память и старт удержаны в прежних пределах.** Справки версий не лежат в
  памяти: реестр держит учётные записи, содержимое поднимается с диска на
  время сборки, слияние идёт по одной справке. Пять справок стоят +5,5 МБ
  (104,0 → 109,5) вместо +204 МБ при хранении разобранных. Слитый вид
  кэшируется на диске штампом по набору справок и отпечатку кода — тем же
  правилом, что остальные производные. Старт 1,5 с на одной справке, 2,2 с на
  трёх; поиск 0,632 → 0,643 мс, качество 97,1% первым не сдвинулось.

### Исправлено

- **Поиск выдавал скрытое по версии за ненайденное.** Когда фильтр версии
  убирал из выдачи всё, `search_syntax` отвечал «ничего не найдено» — и
  `filtered_out`, собранный строкой выше, выбрасывался. Вывод получался
  обратный правильному: агент идёт искать опечатку в имени, которое
  существует.

  Живой случай: запрос «ПолучитьБуферДвоичныхДанных» под отраслевой (8.3.5)
  отвечал «ничего не найдено», хотя
  `РезультатЧтенияДанных.ПолучитьБуферДвоичныхДанных` есть в платформе с 8.3.9.
  Теперь ответ называет 20 скрытых элементов с причиной.

  Тот же дефект чинили в дашборде 2026-08-16 — в инструменте он остался.
  Блок скрытого вынесен в `_hidden_block` и печатается на всех путях, чтобы
  ещё одного места для той же ошибки не появилось. «Ничего не найдено» остаётся
  ответом только тогда, когда скрывать было нечего.

  **После починки проверены все четыре места, где выдача фильтруется по
  версии** — и молчали ещё два:

  - `get_syntax`, подсказки при опечатке (`tools.py:595`). Кандидаты отсеивались
    фильтром до проверки на пустоту, поэтому «БуферДвоичныхДанных.ЗаписатьВФайлик»
    под 8.3.5 отвечало «нет элемента», хотя `БуферДвоичныхДанных.Записать`
    существует с 8.3.9. Для старого элемента та же опечатка подсказки давала —
    то есть терялись они ровно там, где ошибиться вероятнее: имена нового API
    агент помнит лучше, чем 8.3.5.
  - CLI `reg-search --syntax` (`cli.py:202`) печатал пустоту. Этим CLI
    разбирают, почему поиск ведёт себя так, и разбор начинался с ложного вывода.

  Дашборд `/queries` оказался чист — там это починили 2026-08-16.

- **Сборщик обработки запрещал `СтрСравнить` напрасно.** Список
  `PLATFORM_836_TOKENS` писался по памяти в первом же коммите обработки
  (`c498cee`, 2026-08-15), когда справку ещё не умели разбирать. `СтрСравнить`
  появилась в 8.3.5 и на целевой платформе работает. Запрет был мёртвым —
  после правки `dist/` не изменился ни на строку, — но список врал про
  платформу и упал бы, понадобься она. Тест держит список сверенным: каждой
  запрещённой функции должен соответствовать рецепт замены.

- **Отметка «есть замена» в выдаче поиска не срабатывала.** Владелец спросил
  агента, есть ли `СтрРазделить` в отраслевой; тот вызвал `search_syntax`,
  увидел в блоке скрытых пометку `· есть замена` — и ответил «метода нет».
  Рецепт он достал только после прямого второго вопроса «чем заменить», а на
  разбор ответил сам: «ответил на вопрос буквально и по подсказке не пошёл».

  Теперь под списком скрытых стоит указание — вызвать `get_syntax` по
  перечисленным именам и показать рецепт вместе с ответом. Цена 110 символов
  на ответ (142 при трёх именах), и только когда скрытое с рецептом есть.

  Рецепт в выдачу поиска по-прежнему не кладётся, и уже не ради экономии: у
  каждой замены есть оговорка, чем она отличается от оригинала. `Найти` вместо
  `СтрНайти` без оговорки — это подмена невыполнимого кода неверным.

- **Хранение справки теряло границы версий и версионные факты.** Поймано на
  живом контейнере сразу после перезапуска: конфигурация на 8.3.5 снова
  получала сигнатуру свежей справки. `asdict` писал в файл и `until`, и
  `older`, а чтение их не восстанавливало. Пока слитый вид собран в том же
  процессе, всё было верно; после перезапуска он поднимался из кэша уже без
  фактов — то есть ровно то, ради чего слияние делалось, отваливалось при
  первом же рестарте.

  Тесты этого не видели: слитый вид в них строился и проверялся в одном
  процессе. Добавлен сквозной — сохранить, поднять заново, спросить факты под
  старую версию.

- **Дашборд отбрасывал скрытое по версии молча.** Запрос «СтрРазделить» под
  конфигурацией 8.3.5 давал «ничего не найдено», хотя метод существует и
  просто появился в 8.3.6 — через MCP сервер это говорит. Вывод из молчания
  получается обратный правильному: «метод не нашёлся» вместо «метод есть, но
  не в этой версии». Теперь под таблицей результатов список скрытого с
  причиной для каждого элемента, и причины разделены: одно ещё не появилось,
  другого уже нет.

- **`ОпределяемыйТип` перехватывал запросы в единственном числе — вылечено.**
  «контрагент» отдавал `ОпределяемыйТип.Контрагент`, а `Справочник.Контрагенты`
  стоял на **16-м месте**. То же с «склад», «физическое лицо», «организация».

  Разбор показал, что дело не в весе вида, как записывалось раньше. Встроенные
  псевдонимы записаны во множественном числе — «контрагенты», «физические
  лица», «товары», — потому что так называются справочники. Спрашивают в
  единственном, а псевдоним сопоставлялся по точной фразе и не срабатывал.
  Дальше выдачу забирал `ОпределяемыйТип`: он именуется как раз в единственном
  числе и получал точное совпадение всей строки, то есть множитель ×12,
  против которого не помогают ни вес вида, ни вес основы.

  Теперь фраза псевдонима и запрос сравниваются **по основам**. Перечислять
  формы в `synonyms.py` не нужно — одна запись работает для всех.

  Ручной набор **66,7 → 90,5%** (14/21 → 19/21), MRR 0,741 → 0,934.
  Автоматические: 97,4 → 97,3, 96,7 → 96,6, 96,7 без изменений. Просадка
  разобрана поимённо — сломались **ровно два запроса из 5 586**:
  «Присоединенный файл» и «Физическое лицо». Оба перестали отдавать
  `ОпределяемыйТип` и стали отдавать справочник, то есть это и есть желаемое
  поведение: автоматический набор механический, он берёт синоним
  `ОпределяемыйТип` как запрос и ждёт его же в ответе.

  Отвергнутые по дороге варианты (замерены, не приняты): поднятие веса основы
  0,55 → 0,90 и понижение веса `ОпределяемыйТип` 0,45 → 0,12 двигают позиции,
  но первых мест не дают; ослабление множителя точного совпадения даёт
  71,4% на ручном ценой **−5,3 п.п.** на Рознице.

- **Справка от старых платформ не принималась: разделы размечены `div`.**
  Владелец взял `shcntx_ru.hbk` от 8.3.5 — ровно то, что нужно конфигурации на
  8.3.5, — и получил отказ. Классы разметки во всех версиях одни
  (`V8SH_chapter`, `V8SH_title`, `V8SH_heading`), а теги разные: до какой-то
  версии разделы на `div`, позже на `p`. Выражения требовали строго `<p>`,
  поэтому из старой справки доставались имена и больше ничего: 18 936
  элементов и **ноль описаний**, а такую справку отвергает проверка, введённая
  днём раньше ради `shcntx_root.hbk`.

  Тег в выражениях больше не фиксируется. Стало 17 901 описание из 18 936
  (94%) и 14 296 элементов с доступностью. Разбор новой справки не изменился
  ни на элемент: 25 511 и 24 331 описание до и после.

  Проверено, что справка честно старая: `СтрНайти`, `СтрРазделить`,
  `ЗаписьJSON`, `ЧтениеJSON` в ней отсутствуют — все появились в 8.3.6, — а
  `Найти` и `Массив` на месте. Загрузка через дашборд проходит за 3,2 с,
  соотношение версий с конфигурацией 8.3.5 — `exact`, скрытых элементов нет.

  Это делает возможным отдельный сервер под старые внедрения: своя справка,
  свои конфигурации, без методов, которых на их платформе не существует.

- **Проверка контейнера отвергала валидные файлы.** `is_container` требовала
  первых байт `FF FF FF 7F`, но это поле — `next_page_addr`, адрес свободной
  страницы, и оно тремя строками ниже так и читается. `0x7FFFFFFF` означает
  лишь «свободных страниц нет»; там, где список непуст, стоит настоящее
  смещение, и файл получал отказ «не контейнер 1С» — неверный по существу.
  Признаком формата стал `page_size`: он всегда степень двойки и в разумных
  пределах. Проверено на всей поставке 8.3.27.2130 — открываются **38 файлов
  из 38**, прежде четыре отвергались (`shquery_ru.hbk`, `shlang_ru.hbk`,
  `shclang_ru.hbk`, `1cv8_ru.hbk`).

  Проверка стала слабее намеренно: случайные данные иногда её пройдут, но
  разбор блоков дальше падает `V8ContainerError` — тем же исключением, которое
  ловит вызывающий, а не чем попало. Закреплено тестом.

  Отбраковка чужих справок при этом не ослабла: те же четыре файла теперь
  доходят до разбора и отвергаются по существу — «ни одного элемента» или «ни
  одного описания». Формулировка последней исправлена: она называла причиной
  `_root`, хотя `shlang_ru.hbk` — не языконезависимая часть, а другая справка
  с другой разметкой.

- **Слитный запрос строчными не находил ничего.** «стрнайти» возвращало
  пустоту, при том что «СтрНайти» и «стр найти» работали: слова в
  идентификаторе разделяет CamelCase, а строчными границы нет — токен один и
  в индексе его не существует. Единственный оставшийся сигнал, точное
  совпадение всей строки, разрешался **после** выхода по пустой выдаче.
  Ровно тот же дефект уже чинили для псевдонимов 2026-08-15 — тогда причину
  устранили в одном месте, а второе осталось. Теперь затравка даётся до
  выхода и только тем документам, что иначе не попали бы в выдачу вовсе:
  у остальных оценка уже набрана токенами и не трогается. Найдено владельцем
  на живой работе с дашбордом.

- **Стемминг: обрезка до шести символов заменена на Snowball.** Обрезка
  сводила «контрагент/контрагенты» → «контра», но короткие слова оставляла
  разными: «чек», «чека», «чеков» — три токена, «вид»/«виды» — два. Запрос
  «виды оплат чеков» не находил `ВидыОплатЧекаККМ` вовсе: слово «чеков» не
  совпадало ни с одним документом, и причина показывалась как «—».
  На восьми запросах со склонениями первым местом 1/8 → 4/8. На автоматических
  наборах изменений нет ни одного: 97,4 / 96,7 / 96,7% по метаданным (11 665
  запросов) и 97,1% по справке (50 572). Заодно быстрее: 0,097 → 0,085 мс по
  метаданным, 0,281 → 0,266 по справке — основ меньше (1844 → 1751), постинги
  короче. Обоснование зависимости — в `requirements.txt`.

  Попутно вскрылось, что расширение основы по префиксу («номенк» → 
  `Номенклатура`) держалось на совпадении обрезок: условие включало его лишь
  для токенов короче шести символов, а «номенк» — ровно шесть. С настоящими
  основами это перестало работать, и признак заменён на честный: основы нет в
  индексе — значит префикс единственный способ попасть.

- **Два промаха на Рознице вылечены локальными псевдонимами.**
  «инвентаризация» → `Документ.ПересчетТоваров` (в Рознице документ
  инвентаризации назван так, вперёд лезли отраслевые ВЕТИС и САТУРН),
  «подарочный сертификат» → `РегистрНакопления.ПодарочныеСертификаты`.
  Псевдонимы локальные: имя документа — решение конкретной конфигурации, а не
  правило 1С. Оба запроса записаны в `tests/queries/roznica-metadata.json`.
  Ручной набор 75,0 → 91,7% (9/12 → 11/12), MRR 0,833 → 0,958; автоматический
  не сдвинулся ни на одной из трёх конфигураций — 97,4 / 96,7 / 96,7 до и
  после. Оставшийся промах ручного набора — «виды оплат»
  (`Перечисление.ВидыОплатСерверЛояльности` 130,2 против
  `Справочник.ВидыОплатЧекаККМ` 115,8), он был и до этой правки.

- **Чужой `.hbk` больше не роняет загрузку в 500 и не вытесняет рабочую
  справку.** Три исхода вместо одного вразумительного, все воспроизведены на
  файлах из `/opt/1cv8/8.3.27.2130`:
  - `shlang_ru.hbk`, `shquery_ru.hbk`, `1cv8_ru.hbk` → **HTTP 500**.
    `V8ContainerError` наследует `Exception`, а обработчик загрузки ловил
    `(ExportError, RegistryError, ValueError)`. Сообщение парсера («Не
    контейнер 1С: ожидалась сигнатура FF FF FF 7F») до страницы не доходило.
  - `config_ru.hbk` → **HTTP 303, «успех»**, справка в реестре с нулём
    элементов. Хуже прочего: прежняя учётная запись оставалась в списке
    источников, так что на `/sources` были видны две справки, отвечала
    пустая, и по списку понять это было нельзя. Поиск по справке молча
    переставал находить что-либо.

  Лечение на трёх уровнях: `add_syntax` отвергает разбор с нулём элементов
  **до** копирования файла в `data/sources` и записи в реестр; замена справки
  снимает прежний источник вместе с его кэшем (справка одна на процесс);
  дашборд ловит `V8ContainerError` и показывает причину. Тесты — 4 штуки, 69 →
  73. Проверено на реальном `shcntx_ru.hbk`: 25 511 элементов за 4,9 с,
  платформа `8.3.27.2130` выведена из данных, в источниках одна справка.

- **`shcntx_root.hbk` принимался как справка и давал молчаливо пустой поиск.**
  Найдено загрузкой через дашборд: файл прошёл, в реестре появился источник,
  а `СтрНайти` не находится. Причина — `_root` несёт языконезависимую часть
  справки: те же 25 508 элементов и то же дерево страниц, но **ни одного
  описания**, ни одного русского имени и ни одной версии появления (значит и
  фильтр по платформе не работает). Проверка на ноль элементов такое не ловит,
  а по размеру не отличить: 33 МБ против 39 МБ у `shcntx_ru.hbk`.
  Теперь справка без единого описания отвергается с указанием нужного файла.
  Подсказка в форме загрузки исправлена: примета «десятки МБ» была неверной.

- **`.env` и копии каталога данных попадали в зону видимости git.** `.env` с
  `ADMIN_TOKEN` не игнорировался вовсе. Хуже: правило «`data/` вне git» было
  записано по имени каталога, копию переименовали ради проверки с чистого
  листа — и защита отвалилась. В копии `index/syntax/` лежал
  разобранный индекс справки «1С», untracked и готовый уехать первым же
  `git add -A`. Ровно то, на чём уже спотыкались (см. ниже про переписывание
  истории). Теперь игнорируются `.env` (кроме шаблонов), явные копии каталога
  (`data.old/`, `data.bak/`) и производные от справки по шаблону
  `**/index/syntax/`, `**/index/cache/` — где бы каталог ни лежал.

### Изменено

- **Загрузка источника больше не держит браузер.** Разбор справки занимает
  около пяти секунд, и всё это время страница не отвечала: человек смотрел на
  белый экран и не знал, идёт работа или всё зависло. Теперь ответ приходит
  сразу — на живом сервере **0,3 с вместо ~5 с** на файле 39 МБ, — а разбор
  уходит в фон.

  Состояние видно на той же странице: имя файла, сколько принято, что сейчас
  происходит — «принимается», «разбирается», «готово» или «ошибка» с причиной.
  Пока работа идёт, страница обновляет себя `meta refresh`; закончилась —
  обновление прекращается само, иначе страница дёргалась бы вечно. Без JS: это
  требование спеки дашборда.

  Ошибку разбора возвращать стало некуда — ответ уже отдан. Она ложится в
  задание и видна там же, поэтому битый архив и чужой `.hbk` по-прежнему
  объясняют причину, просто в другом месте. Список заданий показывается только
  вошедшему: имя файла тоже говорит, что за база.

  Временный каталог теперь удаляет фоновая задача, а не блок `with`: она
  переживает ответ, и файл нельзя убирать у неё из-под ног.

  Завершённые записи убираются кнопкой «очистить завершённые» — без неё журнал
  копился на экране и мешал смотреть на текущее. Идущие загрузки не трогаются:
  фоновая задача ещё пишет в своё задание, и выдёргивать его нечестно —
  состояние исчезло бы с экрана, а работа продолжилась. 8 тестов и три
  прежних, приведённых к новому поведению, 130 → 138.

  Журнал живёт в модуле и протекал между тестами: изоляцию дала автофикстура в
  `conftest.py`, а не переустройство модуля — это состояние процесса, а не
  данных, и глобальным оно остаётся намеренно.

### Добавлено

- **Карточка в дашборде разбирает markdown, с переключателем «как есть».**
  Текст показывался сырым — с `#`, `**` и обратными кавычками. Откладывали с
  формулировкой «делать по факту неудобства»; факт накопился.

  Разбор живёт только в `dashboard.py`. Ни агенту, ни CLI он не нужен:
  markdown — правильный формат ответа, разметка несёт для модели структуру
  (обратные кавычки выделяют имя, которое копируют в код без искажений, `>`
  помечает оговорку про источник). Символы мешают лишь в HTML.

  Конвертер свой, на полсотни строк, и покрывает ровно то, что `render.py`
  порождает. Замер вывода на живых данных: 308 пунктов списка, 296 фрагментов
  кода, 34 заголовка, 6 жирных, 2 цитаты — ни таблиц, ни вложенных списков.
  Разбирать то, чего не бывает, значит держать непроверяемый код; тянуть ради
  шести конструкций зависимость — тем более.

  Переключатель «как есть» показывает исходный текст. Дашборд — инструмент
  проверки: поехавшую разметку в разобранном виде было бы не видно, а так она
  видна одним щелчком. 11 тестов, 144 → 155.

- **Группа синонимов снимается — из кода, из CLI и из браузера.** Завести её
  можно было с самого начала, снять нельзя было никак, кроме правки
  `dictionary.json` руками. Асимметрия вылезла при разработке страницы словаря
  и висела задачей: неудачная группа тихо портит выдачу всем, слово начинает
  подставляться там, где его не ждут.

  Группа опознаётся **по составу целиком**, а не по номеру: номер сдвинется от
  правки соседей, а состав — то, что человек видит на экране. Порядок и
  регистр не важны, частичное совпадение не считается — «возчик, перевозчик»
  не та же группа, что «возчик, перевозчик, экспедитор».

  Встроенные группы не снимаются: они в `synonyms.py`, меняются вместе с кодом и
  приезжают релизом, а снятие на месте разошлось бы с поставкой и вернулось
  при обновлении. `dict-synonyms --remove` возвращает код 1 и говорит об этом
  прямо. 9 тестов, 121 → 130.

- **Копия `.hbk` больше не сохраняется.** Реестр клал исходник справки в
  `data/sources/hbk/` «чтобы пересобрать без 1С», но за всю жизнь проекта его
  ни разу не открывал: у справки `stored_path` указывает на разобранный
  `index/syntax/*.json.gz`, и восстановление читает именно его. Команды
  «переразобрать из сохранённого» не существует, а повторная загрузка того же
  файла отсекается по хешу — то есть 39 МБ на каждую загруженную справку
  копировались впустую. На рабочей установке так накопилось 137 МБ.

  У конфигураций иначе и остаётся как было: `stored_path` ведёт на
  `sources/configurations/*.zip`, и реестр восстанавливается именно из него.

  Понадобится другой разбор — файл берут из каталога установки платформы. Это
  цена решения, и она названа: справку от снятой с поддержки версии оттуда уже
  не возьмёшь, поэтому такие файлы стоит хранить самому.

- **Уборка разобранных справок и учёт исходных файлов.** Справка одна на
  процесс: загрузили новую — прежняя выбывает из реестра вместе со своим кэшем,
  а разобранный `.json.gz` оставался лежать. На рабочей установке накопилось
  три файла при одном используемом, плюс 138 МБ исходников от всех попыток,
  включая отвергнутый `shcntx_root.hbk`.

  Незаявленные индексы теперь сносятся на старте — как кэш, по тому же
  правилу: производное восстанавливается разбором. Проверено на живых данных:
  5,2 → 2,9 МБ, убраны `8.3.5.1570.json.gz` и `shcntx_root.json.gz`.

  Исходные `.hbk` **не удаляются**: справку от снятой с поддержки платформы
  взять заново негде. Вместо этого они перечислены на `/sources` с размерами и
  кнопкой удаления под `ADMIN_TOKEN`, а счётчик выводится в лог старта.

  Формулировка «не используются» заменена: `stored_path` источника указывает на
  разобранный индекс, а не на `.hbk`, из которого тот получен, и связь теряется
  при первом же восстановлении с диска. Поэтому реестр не берётся различать
  нужный исходник и лишний — он честно говорит, что не ссылается ни на один, и
  предупреждает проверить, можно ли взять файл снова.

- **Авторизация на чтение: `API_TOKEN`.** Раньше токен закрывал только запись,
  а инструменты MCP и страницы дашборда были открыты всем, кто дотянется до
  адреса, — вместе со структурой конфигураций клиента и его доработками.
  Поводом стали строки в логе рабочего сервера: запросы `GET /health` и
  `POST /sources` с внешнего адреса при пробросе на `0.0.0.0`.

  Токенов теперь два, разделены по цене ошибки. `API_TOKEN` — чтение (`/mcp`,
  страницы, карточки), лежит в конфиге каждого MCP-клиента и утекает вместе с
  ним. `ADMIN_TOKEN` — запись, и он же годится за токен чтения, чтобы владельцу
  не держать два заголовка. Клиенты передают токен через `X-Api-Token` или
  `Authorization: Bearer`, браузер — через форму входа: сессия помнит уровень,
  читателю формы правки не показываются вовсе.

  **Незаданный `API_TOKEN` оставляет всё открытым**, как было. Выставление в
  сеть — осознанное действие, а чистая установка и локальная разработка не
  ломаются.

  `/health` остаётся открытым для healthcheck контейнера, но имена
  конфигураций из него убраны: без токена отдаются только живость и счётчики,
  с токеном — прежний ответ целиком.

  Отказ выглядит по-разному для клиента и для человека: браузер (`Accept:
  text/html`) переадресуется на форму входа, клиент получает 401 с JSON.
  Первая версия отвечала JSON всем — владелец сразу упёрся в него в адресной
  строке, откуда войти было некуда.

- **Правка словаря через браузер — страница `/dictionary`.** Псевдонимы
  показываются вместе с происхождением, локальные снимаются кнопкой,
  встроенные — нет: они в коде и меняются вместе с поставкой, кнопка удаления тихо
  разошлась бы с поставкой. Заводятся псевдоним (по умолчанию для текущей
  конфигурации — знание про эту базу, а не про 1С) и группа синонимов (общая).
  Всё под `ADMIN_TOKEN`, чтение открыто: с чтения начинается разбор «почему
  поиск так себя ведёт».

  Правка применяется без перезапуска: `reload_dictionary()` подменяет ссылки
  на таблицы, индексы не пересобираются — ни синонимы, ни псевдонимы не
  участвуют в построении постингов. Проверяется наблюдаемым: тот же запрос до
  правки не находит объект, после — находит с причиной `псевдоним из словаря`.

  Со страницы прогона на каждый промах ведёт ссылка «не то — завести
  псевдоним» с подставленной фразой и конфигурацией. Цикл «спросил → понял
  почему → починил» замкнулся в браузере, ради чего дашборд и заводился.
  9 тестов, 75 → 84.

- **`.env.example`** — шаблон окружения (`ADMIN_TOKEN`, позже добавлен `API_TOKEN`),
  и без шаблона на новой машине это выясняется чтением исходников. Файл
  коммитится, реальных значений не содержит; `docker compose` подхватывает
  `.env` из каталога проекта сам.

### Найдено

- **Опровергнуто: «33 страницы языка запросов несут сигнатуру в первом
  абзаце».** Первая эвристика ошибалась в счёте самих функций и в том, где
  лежит сигнатура. Прямой прогон парсера по живому `shquery_ru.hbk`: функций
  52, сигнатура заполнена у 30 (27 — в `<PRE>`, 3 — первой строкой первого
  абзаца), у оставшихся 22 её нет вовсе. Подробности —
  `docs/query-language-design.md`, раздел «Разбор».

- **Опровергнуто: «уборка разобранных справок не тронет индекс языка
  запросов, раз источник лежит в `sources`».** Неверно — `sweep_syntax`
  собирает список разрешённых видов по `source.kind == KIND_SYNTAX`, и
  источник с новым видом (`KIND_QUERY`) в него не попадал: `query-ru.json.gz`
  сносился бы на первом же старте. Опровергнуто экспериментом, не чтением
  кода — откат одной строки красит тест. Фильтр уборки расширен до
  `(KIND_SYNTAX, KIND_QUERY)`. Подробности — `docs/query-language-design.md`,
  раздел «Хранение и ловушка со слиянием».

- **Уточнено: «55 имён из 128 совпадают с элементами платформы» — на самом
  деле 57 из 127.** Первый замер сделан до того, как парсер стал чистить
  служебные записи (`present.html` не попадает в элементы) и точнее собирать
  имена. Подробности — `docs/query-language-design.md`, раздел «Ответы
  инструментов».

- **У языка запросов нет версий: все 624 отметки в `__categories__` —
  `0.0.0`.** `since`/`until` у его элементов остаются пустыми, и это
  правильно — фильтр по версии платформы такие элементы не трогает вовсе, а
  не «трогает неверно».

- **57 имён из 127 в языке запросов совпадают с элементами платформы** (по
  русскому или английскому имени без учёта регистра, сверено со слитой
  справкой пяти версий: 8 совпадений только по русскому, 19 только по
  английскому, 30 по обоим). Это и есть источник единственной измеренной
  просадки: автонабор `cases_from_syntax_ambiguous` (голое имя → любой
  одноимённый элемент платформы) потерял 18 случаев из 10 549 на первом
  месте (99,1% → 98,9%) — короткие имена вроде «Количество», «Тип»,
  «Первые» и тригонометрические функции (`ACos`, `Sin`, `Log10`…) после
  слияния находят элемент языка запросов раньше платформенного тёзки.
  Ответ агенту при этом верный (запрос «Количество» и должен находить
  функцию КОЛИЧЕСТВО) — просадка в разметке автонабора, собранного до
  появления источника, а не в качестве поиска.

- **`shlang_ru.hbk` отличает от языка запросов отсутствие страниц-секций,
  не отсутствие `__categories__`.** У обоих файлов есть `__categories__`
  (`shlang_ru.hbk` — 62 файла всего), но ни одной из трёх страниц-маркеров
  (`SELECTSection`, `UNIONSection`, `ORDERBYSection`) в нём нет —
  `looks_like_query_help()` возвращает `False`, подтверждено прогоном на
  самом файле.

- **Из 52 функций языка запросов сигнатура в `<PRE>` у 27, не у 28.** Ещё у
  3 сигнатура берётся из первой строки первого абзаца, у оставшихся 22 её
  нет вовсе — только проза. Расхождение с ранее названным числом (28)
  проверено прямым прогоном парсера по живому файлу: 27+3+22 = 52 без
  остатка, совпадает с докстрингом `_сигнатура_функции` в самом
  `query_parser.py`.

- **Агент идёт по указанию, но не по намёку.** Пометка `· есть замена` рядом со
  скрытым элементом второго вызова не вызывала: агент отвечает на заданный
  вопрос буквально и сам решает, что раз элемент недоступен, спрашивать по нему
  нечего. Заработала прямая фраза «вызовите `get_syntax` и покажите рецепт» —
  как строка «указывайте `config` явно» в `list_configurations`, которая
  работала и раньше.

  Практический вывод для формата ответов: то, что сервер хочет получить от
  агента следующим шагом, пишется указанием, а не намёком в разметке.

- **`СтрСравнить` появилась в 8.3.5, а не в 8.3.6 — гипотеза опровергнута.**
  В `PLATFORM_836_TOKENS` сборщика обработки она лежала вместе с остальными
  строковыми функциями, и запрет на неё в XML-варианте строже, чем требует
  платформа: справка 8.3.27 говорит «с версии 8.3.5», отраслевая на 8.3.5.1570
  её видит. Рецепта замены ей не написано — заменять нечего. Из семи функций
  заготовки в таблицу пошли шесть.

- **Разделитель у `СтрРазделить` — набор символов, а не подстрока.** Каждый
  символ второго параметра разделяет сам по себе. Очевидная замена «цикл через
  `Найти(Строка, Разделитель)`» разбивает по всей подстроке целиком и даёт
  другой результат — рецепт пришлось писать посимвольным обходом. Выяснено
  сверкой со справкой до написания рецепта; без сверки в таблицу ушёл бы
  правдоподобный, но неверный код.

- **Проверка на живом сервере ловит то, чего не видят тесты в одном процессе.**
  Два дефекта слияния всплыли только после перезапуска контейнера и только на
  настоящих данных: потеря версионных фактов при чтении индекса с диска и
  молчание дашборда о скрытых по версии элементах. Оба прошли 212 тестов —
  первый потому, что слитый вид в тестах строится и проверяется в одном
  процессе, второй потому, что проверялся ответ инструмента, а не страницы.

  Практический вывод: всё, что переживает перезапуск, проверяется через
  перезапуск, а не через повторный вызов в том же процессе.

- **Слияние версий справки стоит +0,9%, а не удвоение памяти.** Разобраны
  четыре справки — 8.3.5 (18 936 страниц), 8.3.19 (23 627), 8.3.24 (24 803),
  8.3.27 (25 511), разбор 9,3 с суммарно. Старые версии почти целиком вложены
  в свежую, поэтому один слитый индекс на все четыре — 25 003 ключа против
  24 777 у одной 8.3.27:

  | версия | своих ключей | нет в 8.3.27 |
  |---|---|---|
  | 8.3.5 | 18 199 | 199 |
  | 8.3.19 | 22 912 | 44 |
  | 8.3.24 | 24 063 | 21 |

  Прежняя оценка «вторая справка удвоит память» верна только для раздельного
  хранения двух индексов по 195 МБ. При слиянии платит одна самая старая
  версия, промежуточные почти бесплатны. Версионных фактов в слитом индексе:
  167 методов с несколькими сигнатурами (8 — с тремя и более), 600 элементов с
  разными наборами доступности, 7 003 диапазона `since`/`until`.

- **Две крайние справки не заменяют промежуточные.** Расхождения по наличию
  элементов действительно копятся на больших разрывах, но сигнатуры и
  доступность меняются равномерно:

  | пара | переим. | исчезло | появилось | сигнатур | доступность |
  |---|---|---|---|---|---|
  | 8.3.5 → 8.3.19 | 23 | 187 | 4 912 | 73 | 371 |
  | 8.3.19 → 8.3.24 | 4 | 29 | 1 180 | 74 | 173 |
  | 8.3.24 → 8.3.27 | 1 | 21 | 735 | 30 | 138 |
  | 8.3.5 → 8.3.27 | 23 | 199 | 6 777 | 117 | 410 |

  Считая по краям, промежуточное состояние не восстанавливается: **58 методов**
  имеют сигнатуру, не совпадающую ни с 8.3.5, ни с 8.3.27
  (`ПодписатьАсинх(ИсходныеДанные, Сертификат)` в 8.3.19 против
  `(…, ТипПодписи)` в 8.3.27; `ИзбранноеРаботыПользователя.Добавить(Ключ,
  Значение, ИмяТипа)` против `Добавить(Элемент)`), **191 элемент** — набор
  контекстов доступности, **27 элементов** появились после 8.3.5 и исчезли до
  8.3.27, то есть с краёв невидимы вовсе. Практический вывод: справок нужно
  столько, сколько различных платформ у загруженных конфигураций; цена
  разрыва — примерно 10–15 сигнатур и 35–45 доступностей на каждый релиз.

- **Три четверти «пропавших» элементов — артефакт разметки плейсхолдеров.**
  В справке 8.3.5 плейсхолдер внутри английского имени написан по-русски
  (`CatalogRef.<Имя справочника>`), в 8.3.27 — по-английски
  (`CatalogRef.<Catalog name>`). Сравнение «как есть» даёт 2 318 исчезновений,
  после нормализации `<…>` остаётся 601, после снятия переименований — 199.
  То есть 1 717 «удалений» не существовало. Уточняет прежнюю запись про 563
  элемента: она сделана без нормализации и без учёта переименований.

- **Переименование объекта ловится составом членов.** Из 149 владельцев,
  исчезнувших между 8.3.5 и 8.3.27, преемник по пересечению множества членов
  находится у 122 (`ManagedForm` → `ClientApplicationForm`, 0,70; расширения
  управляемых форм → расширения формы клиентского приложения). Раздел
  «Встроенные функции языка» (`Script functions`, 68 функций) ловится только
  вложенностью `|src ∩ dst| / |src|`, а не Jaccard — большой «Глобальный
  контекст» размывает меру. Автоматика ошибается: 1 пара из 23 сошлась неверно
  (`Managed form extension for objects` → `…for chart of characteristics
  types`), поэтому таблица переименований должна быть курируемой.

- **Справка 8.3.5 не содержит сведений «начиная с версии» вообще** — 0 из
  18 936 страниц, при 22 519 из 23 627 в 8.3.19, 23 682 из 24 803 в 8.3.24,
  24 370 из 25 511 в 8.3.27. Значит `derived_platform()` для старой справки
  возвращает пустую строку, и версию приходится брать извне — из имени файла
  или от человека при загрузке.

  **Опровергнутая гипотеза:** вывести версию старой справки, сопоставив её
  элементы со свежей и взяв самый свежий `since` среди совпавших, — не
  работает. В 8.3.5 «находятся» 53 элемента с `since=8.3.6`…`8.3.26` из-за
  совпадения имён у разных сущностей; шум того же порядка, что сигнал, порогом
  не отсекается.

- **Русское имя элемента стабильнее английского в 3–10 раз.** Между соседними
  версиями английское имя меняется у 153 / 16 / 17 живых элементов, русское —
  у 15 / 8 / 5. Причина двоякая: 1С правит собственные опечатки
  (`VerifySignatreAsync` → `VerifySignatureAsync`, `GetMostAcurateProvider` →
  `GetMostAccurateProvider`) и делает настоящие переименования
  (`NotifyDescription` → `CallbackDescription`, `Multiselect` →
  `MultipleChoice`). Русские имена правятся так же, но реже
  (`Жирный` → `Полужирный`, `БлокироватьВеcьИнтерфейс` с латинской «c» →
  `БлокироватьВесьИнтерфейс`). Отсюда ключ сопоставления версий — русское имя,
  английское — подтверждение и запасной путь.

- **Внутри одной справки ключ «вид + владелец + имя» не уникален.** В 8.3.27 —
  232 дублирующихся ключа (734 страницы), в 8.3.5 — 176 (668 страниц). Почти
  все это поля таблиц запросов без владельца: `<>` ×123, `Recorder` ×28,
  `Ref` ×22, `LineNumber` ×21. При слиянии их нельзя сопоставлять по имени.

- **Справка почти не содержит указаний, чем заменить недоступный элемент.**
  Пометка об устаревании стоит на 15 страницах из 25 511 в 8.3.27 и на 2 из
  18 936 в 8.3.5. Идеальный случай один: `Глобальный контекст.Найти` — «Функция
  является устаревшей! Рекомендуется использовать функцию СтрНайти» плюс
  ссылка «см. также» на `StrFind`. Регулярными выражениями из всех страниц
  вытаскивается 3 пары, из них верна 1: резолв по голому имени притягивает не
  тех (`ИнтернетПрокси.Пользователь` советует метод
  `УстановитьПользователяИПароль`, а нашёлся
  `ИсточникКомандСистемыВзаимодействия.Пользователь`).

  **Опровергнутая гипотеза:** «см. также» — не мост замен. У 780 новых
  элементов есть такие ссылки, 307 указывают на элемент, живой в 8.3.5, но это
  соседи по теме (`МетодШифрованияZIP` → `ЗаписьZipФайла`), а не замены.
  Сопоставлять версии по `id` страницы тоже нельзя: он совпадает лишь у 70,9%
  элементов.

- **Асинхронность в платформе представлена тремя поколениями сразу.**
  В 8.3.27 — 231 метод с суффиксом `Асинх` (175 из них появились в одной
  версии, 8.3.18) и 187 методов с префиксом `Начать…`; в 8.3.5 — 0 и 65
  соответственно. Один и тот же результат достигается тремя способами с разной
  доступностью:

      Файл.Размер                  since 8.0     возврат Число     + Сервер, ВнешнееСоединение
      Файл.НачатьПолучениеРазмера  since 8.3.6   через оповещение  только клиентские
      Файл.РазмерАсинх             since 8.3.18  возврат Обещание  только клиентские

  184 из 231 «Асинх» имеют синхронного близнеца в той же версии, то есть группа
  собирается механически, по имени и владельцу.

- **Доступность между версиями меняется у 51,7% общих элементов, но по существу
  — у 2,9%.** Из 14 037 элементов, у которых доступность указана в обеих
  версиях, состав различается у 7 260; 6 850 из них — приписка мобильных
  контекстов, которых в 8.3.5 не существовало как понятия. Классические
  контексты расходятся у 410 элементов: добавлено `Сервер` 98, `ТонкийКлиент`
  82, `ВнешнееСоединение` 71, `ВебКлиент` 69, `ТолстыйКлиент` 8; убрано
  `ВнешнееСоединение` 186, `ВебКлиент` 33, `ТолстыйКлиент` 17, `ТонкийКлиент`
  15, `Сервер` 7. Пример: `Глобальный контекст.ЗаписатьXML` в 8.3.5 недоступна
  на тонком клиенте, в 8.3.27 доступна — то есть доступность такой же
  версионный факт, как сигнатура.

- **Память под справку в контейнере вдвое меньше, чем на macOS, а замер шумит
  на ±20 МБ.** Запись «+203 МБ на `load_syntax`, +118 на индекс» сделана на
  macOS; в Linux-контейнере справка целиком стоит 195,5 МБ против 300.

  Три ошибки методики, каждая давала свой неверный ответ: `ru_maxrss` на macOS
  отдаёт пик за всё время жизни процесса, а не текущее потребление;
  `tracemalloc` держит трассы аллокаций и раздувает RSS, поэтому RSS и живые
  объекты нельзя мерить одним прогоном; спайк, сериализовавший модель без
  `variants`, завысил выигрыш вдвое (8 702 варианта синтаксиса).

  Главное: **разброс между одинаковыми прогонами ±20 МБ** — тот же код давал
  «модель +96,1» и «модель +77,2». Ожидаемый эффект правок 19–28 МБ тонет в
  нём целиком, поэтому обе проверенные правки (`malloc_trim` после старта и
  подъём модели из `marshal`) отвергнуты: в одиночном прогоне эффект был, в
  серии не подтвердился. К повторной оптимизации можно возвращаться только
  после исправления методики.

- **Справка описывает только свою версию, и переход на новую теряет часть
  знания.** Сверка 8.3.5 против 8.3.27: 18 335 элементов против 24 851, общих
  17 783. В старой есть 563 элемента, которых нет в новой — из них 412
  исчезли вместе с объектом-владельцем (22 объекта, включая
  `ВариантОткрытияОкна` и расширения управляемых форм), 151 пропал при живом
  владельце (`ПреобразованиеXSL.ЗагрузитьИзФайла`).

  **Но часть «пропаж» — перестановка разделов справки, а не удаление из
  платформы.** Раздел «Встроенные функции языка» в 8.3.27 отсутствует, а его
  содержимое переехало в «Глобальный контекст»: `ACos`, `ASin`, `Цел`, `Окр`,
  `СтрДлина` живы. Значит сопоставлять версии нужно по имени, а не по полному
  пути, иначе перестановка выглядит удалением.

  Практическое следствие: со справкой от 8.3.27 достоверно писать код под
  8.3.5 нельзя — агент не увидит методов, которые там работают. Поэтому
  сервер сливает справки версий и хранит диапазоны применимости элементов.

- **Форма входа за авторизацией — тупик, который тесты не ловят.** Слой охраны
  ставится вокруг всего приложения, поэтому под него попал и `/login`: сервер
  отвечал 401 на страницу, которая эту самую авторизацию и выдаёт. Тесты
  дашборда этого не видели — они вешают маршруты на голый `Starlette`, без
  внешнего слоя, и там `/login` отвечал 200. Поймала живая проверка curl'ом по
  поднятому серверу. Вывод для практики: слой, добавленный снаружи маршрутов,
  проверяется снаружи же.

- **`ОпределяемыйТип` систематически перехватывает запросы в единственном
  числе.** Не отдельный промах, а класс: `ОпределяемыйТип` в 1С именуется в
  единственном числе (`ОпределяемыйТип.Контрагент`), справочники и регистры —
  во множественном (`Справочник.Контрагенты`), а человек спрашивает в
  единственном. На живой Рознице так перехватываются пять запросов из шести
  проверенных: «контрагент» (575,9), «склад» (954,3), «физическое лицо»
  (1940,1), «вид оплаты» (794,6) — все с причиной «точное совпадение».
  Во множественном числе тот же запрос отрабатывает верно: «подарочные
  сертификаты» → `РегистрНакопления.ПодарочныеСертификаты` (3818,0), а
  «подарочный сертификат» → `ОпределяемыйТип` (113,4).

- **Опровергнутая гипотеза: дело не в множителе точного совпадения.**
  Записанный ранее диагноз — «служебная сущность впереди хранилища, вес
  `ОпределяемыйТип` 0,45 и всё равно наверху» — верен как наблюдение, но
  указывал не на ту причину. Проверено правкой: множитель `×12` заменён на
  `1 + 11 × вес вида`, то есть у `ОпределяемыйТип` он падал до 5,95.
  Ручной набор не сдвинулся (75,0%), автоматический просел на 0,1 п.п. на
  всех трёх конфигурациях (97,4 → 97,3, 96,7 → 96,6, 96,7 → 96,6). Правка
  откачена. Замер объяснил почему: даже при полном снятии множителя
  `ОпределяемыйТип.Контрагент` даёт 13,6 против 10,3 у справочника — разрыв
  набирается раньше, качеством токена (точный 1.0 против основы 0.55), и
  входит по каждому полю. Лечится словарём, а не ранжированием.

- **В каталоге установки платформы 868 файлов `.hbk`: 38 разных справок на два
  десятка языков плюс языконезависимые `_root`.** Справка синтакс-помощника
  среди них одна — `shcntx_ru.hbk`, 39 МБ. Соседи по языку весят килобайты
  (`shlang_ru.hbk` 89 КБ, `shquery_ru.hbk` 169 КБ, `config_ru.hbk` 49 КБ),
  но **размер не признак**: `shcntx_root.hbk` весит 33 МБ. Проверено на
  8.3.27.2130.
- **Первые четыре байта контейнера 1С — не сигнатура, а смещение свободной
  страницы.** `is_container` требует `FF FF FF 7F`, и то же поле дальше
  читается как `next_page`. `0x7FFFFFFF` означает лишь «свободных страниц
  нет»; там, где список непустой, стоит реальное смещение — и валидный
  контейнер отвергается сообщением «Не контейнер 1С», которое неверно.
  Проверено: `shquery_ru.hbk` (`next_page=0x00028ccd`), `shlang_ru.hbk`
  (`0x00015725`), `shclang_ru.hbk` (`0x00011c76`), `1cv8_ru.hbk`
  (`0x001d2338`) — все читаются штатным кодом, если ослабить проверку до
  `page_size` (у всех 512). Настоящий признак формата — `page_size` в
  разумных пределах, а не первое поле. Опровергнутая гипотеза: ранее в этом
  же changelog записано «`.hbk` вопреки заблуждению не zip» — верно, но
  вывод «файл без `FF FF FF 7F` не контейнер» из этого не следует.
- **Из 38 справок в поставке под нашу модель ложится одна.** Остальные либо
  не несут страниц синтакс-помощника вовсе (`FileStorage` есть, html внутри
  ноль — `config_ru.hbk`, `mngbase_ru.hbk`, `ecsui_ru.hbk` и ещё десяток),
  либо размечены иначе. Ценное всё же есть, но мелкое и в другом формате:
  `shquery_ru.hbk` — 27 статей по языку запросов обычной разметкой (`<H1>`
  вместо `V8SH_*`), наш парсер даёт по ним 0; `shlang_ru.hbk` — 7 страниц с
  разметкой `V8SH_*` (парсер распознаёт все 7) плюс 55 файлов `.st` с
  конструкциями языка (`struct_For`, `struct_IfThenElif`, `struct_AddHandler`).
  Это документация прозой, а не справочник элементов с сигнатурами.
- **Суффикс `_root` — не язык, а справка без текстов.** `shcntx_root.hbk`
  разбирается в 25 508 элементов против 25 511 у `shcntx_ru.hbk`, дерево
  страниц и идентификаторы те же. Но описаний 0 против 24 331, русских имён
  0 против 24 866, версий появления 0 против 24 370. То есть локализованные
  файлы несут содержимое, а `_root` — только структуру. Единственный надёжный
  признак пригодности справки — **есть ли у элементов описания**, а не размер
  файла, не число элементов и не сигнатура контейнера.
- **Расширение `.hbk` не означает контейнер 1С.** `shlang_ru.hbk`,
  `shquery_ru.hbk`, `shclang_ru.hbk`, `1cv8_ru.hbk` не имеют сигнатуры
  `FF FF FF 7F` и не открываются ридером контейнера вовсе — при том, что
  расширение то же самое.
- **`config_ru.hbk` — настоящий контейнер 1С с элементом `FileStorage`, но
  страниц синтакс-помощника внутри нет.** Разбор проходит без единой ошибки и
  возвращает ноль элементов. Значит проверки «это контейнер?» и «`FileStorage`
  на месте?» недостаточно, чтобы считать файл справкой: единственный надёжный
  признак — сколько элементов дал разбор.

### Процесс

- **Правило изменений дашборда** записано в спеку: он инструмент проверки, а не
  продукт. Изменение оправдывается сокращением цикла «спросил → понял почему →
  починил», а не внешним видом. Разбор markdown в карточке отложен по этому же
  правилу — делать по факту неудобства.
- **Два промаха поиска собраны, но не вылечены** — сохранены в наборе с
  диагнозом: «инвентаризация» не находит `Документ.ПересчетТоваров` (в Рознице
  документ называется иначе), «подарочный сертификат» ставит впереди
  `ОпределяемыйТип` вместо регистра накопления. Оба найдены прогоном через
  дашборд за один заход — ровно то, ради чего страница делалась.

- **История git переписана: каталог `data/` убран из всех коммитов.** В
  репозитории лежал разобранный индекс справки платформы (2 файла по 2,9 МБ,
  24 331 описание и 1 502 примера кода фирмы «1С») — производное от `.hbk`,
  который в git не пускали. Правило было сформулировано по расширениям, и
  производное под него не подпало. Теперь правило по каталогу: всё, что в
  `data/`, вне git, независимо от происхождения. Заодно ушли `registry.json`
  и `dictionary.json` — состояние конкретной установки, дававшее diff после
  каждого старта. Репозиторий приватный, remote отсутствует, клонов не было —
  утечки не произошло. Размер `.git`: 23 → 1,2 МБ.

- Сбор промахов поиска принят как **постоянная практика**, а не разовая
  задача. Каждая живая задача по 1С даёт формулировку пользователя; промах
  сохраняется в `tests/queries/`, исправление проверяется ручным и
  автоматическим наборами без регрессии соседнего домена.
- Правило про зависимости смягчено и уточнено: «только stdlib» было прочитано
  как жёсткий запрет, чем не являлось. Внешнюю библиотеку следует предлагать
  самому, но с цифрами. Отдельно записано: заранее закладывается шов, а не
  зависимость.
- Возможные источники D (технологический журнал) и E (схемы обмена) отмечены
  в `data-sources.md` — чтобы граница источников продумывалась заранее. Обе
  описывают работу системы, а не её устройство, и меняются на порядки быстрее
  конфигурации: в одно хранилище с A и B они не складываются.

### Добавлено

- **Дашборд** (`dashboard.py`): обзор загруженного с предупреждениями из
  манифестов, загрузка `.zip` и `.hbk` через браузер с удалением, прогон списка
  формулировок с оценкой и причиной ранжирования. Спека —
  [docs/dashboard-design.md](docs/dashboard-design.md).
  Модуль отдаёт список маршрутов, а не приложение: `server.py` монтирует их
  рядом с `/health`, тесты вешают на голый `Starlette` и не поднимают протокол —
  тот же приём, что уже применён к `tools.py`.
  Чтение открыто, запись под `ADMIN_TOKEN`: в браузер уходит идентификатор
  сессии, а не токен; кука `HttpOnly` и `SameSite=Strict`, последний закрывает и
  CSRF. Индексация уходит в поток — разбор справки на 4 секунды остановил бы
  MCP-клиентов. Всё внешнее экранируется: манифест делает чужая обработка в
  чужой базе.
  Имя в результатах прогона — ссылка на карточку (`/object`, `/syntax`): состав
  объекта или описание элемента платформы, тот же текст, что получает агент, с
  переключателем детализации. У реквизита своей карточки нет — ведём на
  объект-владелец; у элемента справки в ссылку идёт `full_ru`, потому что
  идентификатор в индексе служебный. Markdown показывается моноширинно как
  есть: разбирать его значит либо тянуть зависимость, либо писать конвертер —
  ради страницы, которая нужна для проверок.
  Не вошло и вынесено в «Дальше»: правка словаря через браузер и статус фоновой
  индексации.
- **Кэш построенных поисковых индексов** (`index_cache.py`): старт с тремя
  конфигурациями и справкой — 4,3 с в первый раз, дальше 1,2 с. На диске
  9,4 МБ в `data/index/cache/`. Кэшируются индекс справки, её словарь имён,
  индексы объектов и реквизитов каждой конфигурации.
- **Первые тесты проекта**: `pytest`, 69 штук, `requirements-dev.txt`.
  От содержимого `data/` не зависят — конфигурация, выгрузка schema v1 и
  справка собираются в `tests/conftest.py`.
- `SearchIndex.export_state()` и `from_state()`: индекс сам знает, из чего
  состоит, а формат файла — забота кэша. Полезная нагрузка документов на диск
  не кладётся, подключается заново из уже загруженных данных.
- **MCP-сервер** (`server.py`) на официальном SDK: 7 инструментов,
  streamable-http и stdio. Проверен настоящим клиентом, протокол 2025-11-25.
- Слой инструментов (`tools.py`) без единой зависимости от MCP — те же функции
  понадобятся дашборду, а протокольный слой меняется чаще предметной области.
- Карточка элемента платформы в markdown: доступность выводится сразу после
  заголовка, потому что именно из-за неё сгенерированный код чаще всего не
  компилируется.
- Первая внешняя зависимость — `mcp>=2.0`, заперта в `server.py`.
- **Docker**: один контейнер, 260 МБ, без внешних баз. Каталог
  `data/bootstrap/` индексируется при старте — сервер поднимается готовым.
- `GET /health` для healthcheck и `POST /admin/reload` для подхвата новых
  источников без перезапуска. Перезагрузка включается только при заданном
  `ADMIN_TOKEN`: `custom_route` идёт в обход авторизации протокола, и открытая
  ручка, меняющая данные для генерации кода, — плохой умолчание.
- Описания параметров инструментов в JSON-схеме. Клиент получает их при
  `tools/list`, и агент понимает, что `config` — имя из `list_configurations`,
  а не путь к файлу, ещё до первой ошибки.
- README переписан как полное руководство: запуск, подключение к Claude Code,
  Codex, Cursor, VS Code и прочим клиентам, управление данными через CLI.

- Реестр источников (`registry.py`): учёт загруженного, сопоставление версии
  платформы конфигурации и справки, готовый контекст для инструментов сервера.
  Подъём с диска за 1,9 с вместо 4,4 с полного разбора.
- Восстановление после перезапуска: `registry.json` с хешами источников,
  каталог `data/bootstrap/` индексируется при старте — сервер поднимается
  готовым, без ручных действий в дашборде.
- Команды CLI поверх реестра: `reg-add`, `reg-list`, `reg-search`.

- Хранение разобранных индексов (`store.py`): gzip + JSON, не pickle — индекс
  должен переживать смену версии Python и читаться глазами при разборе
  проблем. Справка сжимается до 2,9 МБ и поднимается за 0,32 с вместо 1,7 с
  разбора `.hbk`.
- Лексический поиск (`search.py`), общий для метаданных и справки: разрезание
  идентификаторов по границам регистра, сопоставление словоформ по усечённой
  основе, двуязычные имена, веса полей, оценка по покрытию запроса.
- Стенд замеров качества поиска (`bench.py`): доля правильных ответов на первом
  месте, в первой пятёрке и средний обратный ранг. Наборы запросов собираются
  автоматически из данных и дополняются ручными трудными случаями.
- Отдельные виды для раздела `tables/` справки — таблицы и поля языка запросов.

- **Тексты документа освобождаются после заморозки индекса.** `Doc.fields` и
  `exact_keys` нужны только при индексации, поиск читает постинги. На справке
  это 26 МБ описаний по 400 символов и точных ключей, которые висели до
  перезапуска. Память 471 → 440 МБ; холодный и тёплый старт сравнялись
  (448 против 442 МБ), потому что поднятый из кэша индекс этих полей и так не
  имел. Вместо пустого словаря и списка на каждый документ — по одному объекту
  на процесс.
- **Постинги поискового индекса лежат массивами** (`numpy`), а не словарём
  словарей. Поиск: справка 2,44 → 0,54 мс, реквизиты 0,79 → 0,26, объекты
  0,35 → 0,19. Память 512 → 471 МБ, кэш на диске 9,4 → 7,8 МБ, образ
  260 → 350 МБ. Выдача побитово та же на 9 210 запросах.
  Наполнение осталось словарным — индекс строится по документу за раз;
  словари освобождаются сразу после заморозки в массивы. Ничьи разрешаются
  предвычисленным лексикографическим рангом идентификатора: без него порядок
  при равных оценках расходился со старым на 404 запросах из 500.
- **Поиск ускорен в 2,5–4,2 раза без единого изменения в выдаче**: справка
  10,9 → 2,8 мс, реквизиты 3,3 → 0,8 мс, объекты 0,99 → 0,39 мс. Три правки в
  `search()`: покрытие запроса копится битовой маской вместо множества слов;
  кандидаты складываются кортежами, а не объектами `Hit`, которых создавалось
  по пять тысяч на десять возвращаемых; отбор идёт кучей размером `limit`
  вместо полной сортировки. Сверено со старой реализацией на тех же индексах:
  9 210 запросов, совпадают идентификаторы, оценки и причины.

### Исправлено

- **Псевдоним из словаря не срабатывал без лексического совпадения.** `search()`
  возвращался через `if not scores: return []` раньше, чем доходил до
  псевдонимов, поэтому прямое указание человека («когда я говорю так — я имею
  в виду вот эти объекты») требовало того самого текстового совпадения, ради
  замены которого псевдоним и заводят. Ломалось это ровно на штатном сценарии
  локального слоя — жаргон внедрения, внутренние сокращения, свои соглашения:
  у них пересечения с именами объектов нет по определению. Разрешение
  псевдонимов поднято выше раннего выхода. В поставляемом словаре такой фразы
  не было, поэтому дефект оставался латентным; найден тестом.
- **Перечитывание словаря перестраивало все индексы** — около секунды на трёх
  конфигурациях, при каждом `POST /admin/reload` и каждой правке через
  `dict-*`. Работа была холостой: ни синонимы, ни псевдонимы не участвуют в
  построении постингов, они читаются только в момент поиска. Теперь
  подменяются две ссылки: 1 с → 0,1 мс.
- **Каждый вызов инструмента перебирал всю справку.** `resolve()` считал,
  сколько элементов скрыто фильтром по версии, обходя все 25 511 элементов —
  на каждый запрос, хотя ответ не меняется, пока не сменились источники.
  16 из 17 мс уходило туда. Соотношение версий кэшируется, разбор версии
  мемоизирован, целевая версия разбирается один раз на фильтр.
- **`get_syntax` искал точное совпадение перебором** всех элементов справки.
  Добавлен словарь имён: 17,3 мс → меньше 0,01 мс.
- **Справка разбиралась заново при каждом старте** — 4,3 секунды впустую,
  бесконечно. При восстановлении реестра хеш источника перезаписывался хешем
  сохранённого индекса, а не исходного `.hbk`, поэтому файл в `bootstrap/`
  каждый раз выглядел новым. Исходный хеш теперь сохраняется. Повторный старт
  реестра: 7,1 → 2,9 с.
- Справка платформы не работала без загруженной конфигурации: `search_syntax`
  и `get_syntax` отказывали, хотя справка — самостоятельный источник и полезна
  сама по себе. Теперь работают и в одиночку, честно предупреждая, что
  фильтрации по версии нет. Обратный случай (конфигурация без справки) работал
  и раньше.
- Пути в `registry.json` были относительны рабочему каталогу, из-за чего реестр
  не переносился между машиной разработчика (`./data`) и контейнером (`/data`).
  Теперь относительны каталогу данных.
- Версия справки бралась только из пути к файлу. Копия `shcntx_ru.hbk` без
  версии в имени регистрировалась без платформы, и фильтрация **молча
  выключалась**. Теперь версия выводится из самих данных — по самому свежему
  `Доступен, начиная с версии`; если определить не удалось, это отдельная
  оговорка в ответах, а не тишина.
- Запрос элемента, которого нет в версии платформы конфигурации, отвечал
  «точного совпадения нет» — агент делал вывод, что перепутал имя, и искал
  несуществующее. Теперь отвечает прямо: элемент есть, появился в 8.3.6, ваша
  платформа 8.3.5, использовать нельзя. Поиск по справке тоже показывает
  отфильтрованное с версиями, а не молча прячет.
- Русское и английское имя элемента справки склеивались в одно поле, из-за чего
  точное совпадение имени не срабатывало: запрос `УстановитьУзелАтрибута`
  проигрывал элементу `УстановитьУзелIdАтрибута`. Точные ключи вынесены
  из полей в отдельный список.

### Найдено

- **`hmac.compare_digest` не сравнивает строки с не-ASCII** — бросает
  `TypeError`. Токен администратора задаёт человек, и «секрет» кириллицей роняло
  бы вход. Сравнивать нужно байты. Поймано тестом при написании дашборда.
- **Десять конфигураций помещаются в 768 МБ, а не в 1,2–1,7 ГБ.** Проверено
  десятью копиями Документооборота КОРП с разными именами в манифесте: 45 960
  объектов плюс справка, загрузка 6,0 с с тёплым кэшем, кэш на диске 15 МБ.
  Прежний прогноз считался по 17 КБ на объект, сейчас 9,7 КБ. Поиск не
  деградирует — 0,17 мс на запрос против 0,19 при трёх конфигурациях, качество
  то же: индекс у каждой конфигурации свой, они друг другу не мешают.
  Отдельно: холодный прогон дал 683 МБ, тёплые — 766, 768, 769. Разница не в
  режиме, а в том, что аллокатор случайно вернул арены системе; мерить нужно
  повторными прогонами, одиночный результат врёт на десятки мегабайт.
- **Разбор справки стоит ~200 МБ сверх живых данных.** `load_syntax` из
  `json.gz` добавляет 203 МБ RSS, построение индекса — ещё 118 МБ, при том что
  живых объектов 62,8 МБ модель и ~23 МБ индекс. Разница — арены, занятые под
  разбор JSON и не возвращённые системе. После перехода постингов на массивы
  это единственная крупная позиция, и она не зависит от числа конфигураций.
- **Память индекса — это не только постинги, и спайк это скрыл.** Прототип на
  массивах мерился `tracemalloc` в отдельности и показал 89,4 → 22,4 МБ на
  индексе справки. Выигрыш оказался вчетверо меньше: 89,4 → 62,1 МБ. Причина
  в методике — прототип строился поверх уже существующего индекса и
  переиспользовал его объекты (идентификаторы, `Doc`, строки ключей), а
  `tracemalloc` засчитал их старому индексу. В реальной замене эти объекты
  никуда не делись.
  Разбор индекса справки после перехода: массивы 12,8 МБ, объекты Python
  49,3 МБ, из них `_exact` 13,3, `docs` 5,7, `_position` 4,4, `_doc_ids` 3,0,
  срезы 3,6. Постинги ужались со ~160 до 21 МБ на все семь индексов — то есть
  половина памяти индекса это строковые ключи и объекты, а не веса.
  **Вывод для будущих спайков:** дельту нельзя мерить на структуре, которая
  переиспользует объекты замеряемой. Нужен отдельный процесс с полной сборкой.
- **Медленный поиск по справке объяснялся неверно — третий такой случай.**
  Первоначальная гипотеза называла причиной «перебор основ по префиксу для
  коротких токенов». Профиль: `_prefix_stems` — 1 119 вызовов и 4 мс, то есть 0,1%
  времени. Настоящая причина — объём кандидатов: на запрос по справке
  обходится 12 374 пары (документ, поле) против 1 590 по метаданным, потому
  что на одну основу там приходится 77,6 документа против 19,7, а на самую
  частую — 13 687 из 25 511. Дальше эти тысячи кандидатов превращались в
  объекты `Hit` и сортировались целиком ради первых десяти.
  Предыдущие два случая: замедление старта списали на новый индекс, а виноват
  был повторный разбор справки; потолок 46 запросов/с списали на архитектуру,
  а виноват был перебор по 25 тысячам элементов.
- **Очевидный формат для кэша индексов не дал бы ничего.** `store.py` хранит
  разобранную справку в gzip + JSON, и повторить это казалось естественным.
  Замер на индексе справки: `json.gz` — 1037 мс, голый `json` — 958 мс,
  `marshal` — 117 мс, построить заново — 1266 мс. То есть JSON экономил 13%
  вместо десятикратного выигрыша.
  Причина не в скорости разбора, а в структуре: постинги — это словарь
  словарей, где `doc_id` повторяется около миллиона раз, и JSON пишет каждое
  вхождение отдельной строкой (106 МБ текста против 3,4 МБ). `marshal` версии
  4 хранит повторы ссылками и на подъёме переиспользует один объект. Отсюда же
  вывод про внешние библиотеки: `orjson` и `msgspec` ускоряют разбор, но
  создавать миллион строк им пришлось бы так же — при заявленных 2–3x это
  около 320 мс против 117 мс у стандартной библиотеки.
- **Словарь не участвует в построении индексов** — ни синонимы, ни псевдонимы:
  `SearchIndex.add()` их не читает, они нужны только в момент поиска. Отсюда
  два следствия: кэш от словаря не зависит и не обесценивается его правкой, а
  `reload_dictionary()` перестраивал индексы впустую.
- **Специализированные объекты вытесняют основной.** «Присоединенные файлы» в
  Документообороте выводил `СообщениеЭДОПрисоединенныеФайлы`,
  `СправкиСПАРКРискиПрисоединенныеФайлы` и подобные, а нужный
  `Справочник.Файлы` оказывался шестым: по словам они совпадают точнее.
  Случай не лечится весами — нужен именно псевдоним.
- **Векторная база решала бы одну задачу — разрыв «как говорят» против «как
  названо».** Проверено на живом примере: «заказ клиента» не находил
  `ЗаказПокупателя`. Закрыто словарём на три десятка групп плюс нормированием
  по длине имени: цель вышла на первое место, регресс на 7 тысячах
  автоматических запросов — 0,1 п.п.
- **У слова «клиент» в 1С два несовместимых смысла** — покупатель и
  клиентская часть модуля (`ОбщегоНазначенияКлиент`). Запрос «клиент» без
  словаря выдаёт общие модули. Векторная близость на этом спотыкается так же,
  но, в отличие от словаря, у неё нет ручки, чтобы поправить конкретную пару.
- **Пропускная способность выросла с 46 до ~300 запросов/с**, p95 при 20
  одновременных клиентах — с 655 до 124 мс. Узким местом была не архитектура
  «всё в памяти», а два перебора по 25 тысячам элементов на каждый вызов.
- **Ленивая загрузка конфигураций не нужна для общего сервера.** При 20
  разработчиках и 10 конфигурациях за день будут затронуты почти все, и
  ленивая загрузка лишь отложит неизбежное, добавив задержку первому
  обращению. Память при этом не умножается на пользователей: один процесс,
  один набор индексов.
- Удельная стоимость конфигурации, измерено на трёх: **17 КБ памяти и 0,13 мс
  подготовки на объект**. Прогноз на 10 конфигураций — 1,2–1,7 ГБ и 10–14 с
  старта; запросы не замедляются, поскольку индекс у каждой конфигурации свой.
- **Поиск по объектам не отвечает на вопрос «где хранится значение».** Нашлось
  на живой задаче: «контактная информация» не выводила ничего полезного,
  потому что искомое — табличная часть справочника, а индексировались только
  объекты. Типовой вопрос при написании запросов, теперь закрыт.
- **Поля форм обработок и отчётов совпадают по именам с настоящими данными**
  и без априорного веса вытесняют их из выдачи: «номер телефона» отдавал
  `Обработка.ФорматДокументПредприятия…` вперёд справочника контрагентов.
  Вес по виду объекта-владельца это снял.
- **Имя табличной части — часть адреса, по которому спрашивают.** «Контактная
  информация контрагента» описывает контейнер, а не поле; с именем ТЧ только
  в родителе (вес 1,5) нужное не находилось.
- `docker compose up -d --build` пересобирает образ, но контейнер может
  оставить прежним, а `restart` перезапускает старый контейнер на старом
  образе. Для проверки изменений нужен `--force-recreate`, иначе легко
  «проверить» код, который в контейнере не запущен.
- Справка знает свою версию без внешних подсказок: максимум по полю «доступен
  начиная с версии» даёт нижнюю границу версии платформы, и её достаточно для
  фильтрации.
- Оба транспорта проверены официальным клиентом, а не предположением:
  streamable-http и stdio, рукопожатие и вызов инструментов, протокол
  `2025-11-25`.
- **Фильтрация по версии работает на живом запросе.** «разделить строку» для
  конфигурации на 8.3.5 не возвращает `СтрРазделить` — метод появился в 8.3.6
  и отсекается до выдачи. Для той же фразы на 8.3.23 он первый в списке.
- **Лексического поиска достаточно.** На реальных выгрузках: 96,8% правильных
  ответов первой строкой и 100% в первой пятёрке для метаданных, 97,2% и 98,7%
  для справки. Векторный слой пока нечем обосновать.
- **Автоматический набор запросов может мерить невозможное.** Первый замер по
  справке дал 34,7% — оказалось, что запросом было голое имя члена
  (`OnWrite`, `Данные`), а правильным считался один конкретный элемент из
  десятков одноимённых. После исправления стенда — 97,2% на тех же данных и
  том же движке. Цифра, полученная неверным стендом, хуже отсутствия цифры.
- Раздел `tables/` справки — поля таблиц языка запросов. У них нет
  объекта-родителя, а имена (`Ссылка`, `НомерСтроки`, `Активность`) совпадают
  с десятками свойств объектов.

## [0.1.0] — 2026-08-15

Первый рабочий контур: выгрузка структуры конфигурации из 1С, её разбор,
граф связей и разобранная справка платформы.

### Добавлено

**Обработка выгрузки структуры конфигурации** (`exporter-1c/`)

- Ядро обхода метаданных в единственном экземпляре (`src/core.bsl`) и сборщик
  вариантов (`build_modules.py`): четыре модуля под обычную и управляемую форму,
  XML и JSON. Расхождение вариантов невозможно по построению.
- Формат выгрузки `schema_version 1` — одна логическая модель, две
  сериализации, чанкование по видам метаданных, манифест с метаданными
  конфигурации ([docs/schema-v1.md](docs/schema-v1.md)).
- Охват расширен с 7 видов метаданных до 20: добавлены регистры бухгалтерии и
  расчёта, планы счетов и видов расчёта, планы обмена, бизнес-процессы, задачи,
  определяемые типы, общие модули, подписки на события, регламентные задания,
  отчёты, обработки.
- Выгружаются движения документов, основания ввода, владельцы справочников,
  предопределённые элементы, квалификаторы типов, комментарии и свойства видов —
  ничего этого в исходной обработке не было.
- Предупреждения выгрузки пишутся в манифест: тихая деградация заменена на
  видимую. Поля `truncated` и `predefined_available` — машиночитаемые признаки
  неполноты для загрузчика.
- Проверка совместимости с 8.3.5 при сборке: если в XML-вариант просочился
  `ЗаписьJSON` или функция 8.3.6+, сборка падает.

**Загрузчик и модель** (`src/mcp1c/`)

- `loader.py` — чтение ZIP или каталога, XML и JSON приводятся к одной модели.
  XML читается потоково, память не зависит от размера чанка.
- `model.py` — формат-независимая модель конфигурации.
- `graph.py` — граф связей из типов реквизитов, движений, оснований ввода,
  владельцев, обработчиков подписок и методов регламентных заданий.
- `render.py` — markdown с уровнями детализации `brief` / `fields` / `full`.
- `cli.py` — отладочные команды `info`, `stats`, `show`, `related`, `find`.

**Справка платформы** (`src/mcp1c/`)

- `v8container.py` — ридер внутреннего контейнера 1С. Общий для `.hbk`, `.cf`,
  `.epf`, без внешних зависимостей.
- `syntax_parser.py` + `syntax_model.py` — разбор синтакс-помощника: 25 511
  элементов за 1,9 с, с доступностью, версией появления, параметрами, типами
  возврата и примерами кода.
- Фильтрация по версии платформы: для конфигурации на 8.3.5 из выдачи
  исчезают 6 539 элементов, появившихся позже.

**Документация**

- [docs/schema-v1.md](docs/schema-v1.md) — контракт формата выгрузки.
- [docs/data-sources.md](docs/data-sources.md) — правило источников данных.

### Изменено

- Составные типы больше не схлопываются при выгрузке. Реквизит `Регистратор`
  или `Субконто` отдаёт полный список типов (до 1098 штук), а схлопывание
  делается при рендере. Раньше терялись именно те рёбра графа, ради которых он
  строится.
- `ЛимитОбъектов = 0` означает «выгрузить всё». Раньше ноль молча превращался
  в 10 объектов — выгрузка выглядела успешной, но была огрызком.
- Markdown больше не формируется в 1С. Обработка отдаёт структуру, рендер —
  задача сервера. Отсюда бесплатно берутся уровни детализации и граф.

### Исправлено

- `date_parts` писался у всех реквизитов подряд, включая строки и ссылки:
  `КвалификаторыДаты` возвращаются платформой всегда. Теперь только при наличии
  типа `Дата` — 8 703 мусорных атрибута убрано, файлы похудели на 15–20%.
- Предопределённые элементы брались с менеджера объекта, где такого метода нет.
  Перенесено на объект метаданных.
- Обращение за предопределёнными к планам обмена, бизнес-процессам и задачам,
  у которых их не бывает, выставляло глобальный флаг «платформа не умеет» и
  глушило предопределённые во всей выгрузке.
- `СтрРазделить` и `ПолучитьРазделительПути()` в ядре обработки — заменены на
  совместимые с 8.3.5 конструкции.
- `<br>` не съедался целиком при разборе справки: описание параметра начиналось
  с обрывка тега.
- Слабые рёбра графа: реквизиты вроде `ЗначениеДоступа` с сотнями типов
  связывали всё со всем. Помечены и скрыты по умолчанию — у
  `Справочник.Организации` осталось 416 настоящих входящих ссылок вместо 538.

### Найдено

- **`.hbk` — не zip-архив.** Это внутренний контейнер 1С (сигнатура
  `FF FF FF 7F`, page_size 512), тот же формат, что у `.cf` и `.epf`. Настоящий
  zip лежит внутри, отдельным элементом `FileStorage`. Ловушка:
  `zipfile.is_zipfile()` возвращает `True`, потому что ищет сигнатуру
  центрального каталога где угодно в файле. Реальное открытие падает с
  `BadZipFile`. Внешний `7z` не нужен и по этому файлу не сработает.
- **`ПолучитьИменаПредопределенных()` — метод объекта метаданных**
  (`Метаданные.Справочники.X`), а не менеджера (`Справочники.X`). Поле запроса
  называется `ИмяПредопределенныхДанных`, а не `ПредопределенноеИмяДанных`.
  Установлено пробником `exporter-1c/probe/`.
- **Опровергнуто:** «до 8.3.6 предопределённые недоступны в принципе». Вывод
  был сделан по одной платформе. На 8.3.23 отказ оказался тем же самым — то
  есть причиной было обращение не к тому объекту. После исправления
  предопределённые собираются и на 8.3.5, и на 8.3.23.
- **Справка платформы знает версию появления каждого элемента** — 24 370 из
  25 511 страниц несут «Доступен, начиная с версии X». Поэтому одной справки от
  свежей платформы достаточно, чтобы корректно обслуживать старые конфигурации:
  нужна фильтрация, а не отдельный `.hbk` под каждую версию.
- **Доступность в справке есть** (79% элементов): тонкий клиент, веб-клиент,
  толстый клиент, сервер, внешнее соединение, мобильные варианты. Пишется
  вразнобой по регистру, требует нормализации.
- `Метаданные.НайтиПоТипу().ПолноеИмя()` отдаёт `Справочник.Номенклатура`, а не
  `СправочникСсылка.Номенклатура` — типы напрямую соединяются с `full_name`,
  нормализация имён при построении графа не нужна.
- Планы обмена ссылаются на `Конфигурация.<Имя>` — это не объект метаданных,
  такие ссылки загрузчик обязан отбрасывать штатно.
- **Тексты модулей из встроенного языка недоступны.** Обработка их получить не
  может в принципе; источник — выгрузка конфигурации в файлы.
- Векторный поиск и внешняя БД пока не обоснованы: две конфигурации целиком —
  85 МБ и 0,25 с загрузки, справка — 1,9 с разбора. Исходный шаблон тянул
  Qdrant с torch и cuda на 6+ ГБ образа, разобранный аналог — Elasticsearch на
  2 ГБ памяти ради 32 МБ индекса.

### Проверено на реальных данных

| Конфигурация | Платформа | Формат | Объектов | Рёбер | Загрузка |
|---|---|---|---|---|---|
| Отраслевая конфигурация | 8.3.5.1570 | XML | 1 616 | 26 750 | 0,11 с |
| Розница для Казахстана | 8.3.23.1997 | JSON | 5 637 | 44 034 | 0,14 с |

Неразрешённых ссылок — ноль. Ошибок приведения типов — ноль. Оба формата дают
одинаковый набор из 30 ключей модели.

[Unreleased]: https://github.com/AzeevAN/mcp-1c/compare/v1.3.0...HEAD
[1.3.0]: https://github.com/AzeevAN/mcp-1c/compare/v1.2.0...v1.3.0
[1.2.0]: https://github.com/AzeevAN/mcp-1c/compare/v1.1.1...v1.2.0
[1.1.1]: https://github.com/AzeevAN/mcp-1c/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/AzeevAN/mcp-1c/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/AzeevAN/mcp-1c/compare/v0.8.0...v1.0.0
[0.8.0]: https://github.com/AzeevAN/mcp-1c/compare/v0.7.0...v0.8.0
[0.7.0]: https://github.com/AzeevAN/mcp-1c/compare/v0.6.0...v0.7.0
[0.6.0]: https://github.com/AzeevAN/mcp-1c/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/AzeevAN/mcp-1c/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/AzeevAN/mcp-1c/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/AzeevAN/mcp-1c/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/AzeevAN/mcp-1c/releases/tag/v0.2.0

<!-- Тега v0.1.0 нет: версия описана до того, как репозиторий стал публичным,
     и историю с тех пор переписывали `git filter-repo`. Ссылку на релиз
     ставить не на что. -->
